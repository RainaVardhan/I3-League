# CLAUDE.md — i3League Build Notes

This file orients any Claude Code session working in this repo. Read this before making architectural decisions or adding new features — it exists so context isn't lost between sessions.

## What this project is

i3League is a year-round student innovation platform (middle/high school). Students move through a 6-stage journey, submitting work at each stage, and can qualify for a national competition. Full product spec lives in the project's source documents — this file is the *build* context, not the product spec. When in doubt about product behavior, defer to the spec; when in doubt about *how* to build something, follow this file.

**The person building this is solo and non-technical.** They are directing Claude Code to write all the code. Prioritize working, well-commented, conventional code over clever code. Avoid unexplained abstractions. When you make a nontrivial architectural choice, leave a comment saying why.

## Timeline context

This is being built as a 2-week MVP, not the full spec. If a task looks like it belongs to a feature listed under "Deferred (Phase 2)" below, don't build it — note it and move on. Scope creep is the biggest risk to this timeline.

## Tech stack (do not deviate without discussion)

| Layer | Choice |
|---|---|
| Framework | Next.js (App Router) |
| Database | Postgres via Supabase (or Neon) |
| ORM | Prisma |
| Auth | Supabase Auth — role-based: `student`, `parent`, `coach`, `judge`, `admin` |
| File storage | Cloudflare R2 (deferred setup — use local/mock storage until Cloudflare step begins) |
| Deployment target | Cloudflare Workers via `@opennextjs/cloudflare` (deferred — build and run locally for now, do not spend time on Cloudflare config yet) |
| Payments | **Manual** — PayPal / Venmo / Zelle. No Stripe, no payment processor API. See "Payment flow & pricing model" below. |
| Error monitoring | Sentry (add once core flows work, not blocking for early days) |
| Version control | GitHub — this repo |

**Local dev for now:** the app should run fully on `localhost` against a cloud Supabase/Neon Postgres instance. Do not wire up Cloudflare deployment until explicitly asked — it's a later step, not a Day 1 blocker.

## Payment flow & pricing model (important — per-participant, not per-team)

**Pricing is per participant, not per project/team.** A team of N pays $111 × N at checkout, but underneath, billing and access are entirely individual:

| Participation | Registration fee |
|---|---|
| Individual | $111 |
| Team of 2 | $222 total ($111/student) |
| Team of 3 | $333 total ($111/student) |
| Team of 4 | $444 total ($111/student) |

Each student — regardless of team size — gets their own: account, curriculum access, assessments, Innovation Journal, speaking/character/ethics challenges, Innovator Profile, badges, and certificate. **The project can be shared across a team; the learning and certification never are.**

Data model implication: `Enrollment` and `Payment` are **per-student**, not per-team. `Team` is a lightweight grouping entity linking students to a shared `Project` — it has no billing or curriculum logic of its own.

No payment processor integration exists. The flow is:

1. Registration/checkout screen totals `$111 × participant count` and displays the org's PayPal.me link, Venmo handle, and Zelle info as static text (pulled from `Season` config, not hard-coded).
2. Each student/parent pays their individual share externally (or the team pays together and it's reconciled manually — don't build logic assuming a single team-wide transaction reference covers everyone; allow each student's confirmation to be entered separately even if the reference number is shared).
3. Each student submits a **payment confirmation form**: payment method, transaction reference/confirmation number, optional screenshot upload. This creates that student's `Enrollment` with `paymentStatus = SUBMITTED`.
4. An admin manually reviews and sets `paymentStatus = VERIFIED` **per student** — a teammate's unverified payment must never block or unlock another teammate's dashboard.
5. `paymentStatus` values: `PENDING`, `SUBMITTED`, `VERIFIED`, `REJECTED`, `REFUNDED`.

Do not build Stripe/payment-API integration unless explicitly asked to add automated PayPal payments later.

## Season-driven config — do not hard-code

Nothing about the 2026–2027 season should be hard-coded into templates or logic. All of the following live on a `Season` record and are read at render/runtime:

- Per-participant pricing (currently $111)
- Spring qualification deadline (April 30, 2027)
- Summer qualification deadline (July 30, 2027)
- National Finals date
- Curriculum version
- Team size limit (currently supports up to 4)

This is what lets a `2027–2028` season exist later without a rebuild.

## Core business rules (enforce server-side, not just in the UI)

- **Sequential stage unlocking**: INSIGHT → INVESTIGATE → IMAGINE → IP Checkpoint → ITERATE → IMPACT → INFLUENCE. A student cannot access a stage via direct URL before completing the prior one. Admin can override/unlock manually.
- **Safety screening** (in INVESTIGATE): any high-risk answer (humans, animals, health info, chemicals, biological materials, electricity, machinery, PII, environmental sampling, drones, AI, other) sets the project to `ADMIN_REVIEW_REQUIRED` and blocks progression until an admin clears it.
- **IP Checkpoint** (gate between IMAGINE and ITERATE): sets project visibility to `PUBLIC` or `CONFIDENTIAL`. **Every project defaults to `CONFIDENTIAL` at creation** — this is the value from the moment a `Project` record is made, not just the answer selected at the IP Checkpoint gate. A project only becomes `PUBLIC` if the student explicitly requests it, at or after the IP Checkpoint — never as a fallback, never inferred from an unanswered question. If the IP Checkpoint flow is skipped, abandoned, or answered ambiguously, the project stays `CONFIDENTIAL`. **Confidential projects must never appear in any public-facing query** — galleries, marketing, public judge views. Treat this as a hard security requirement, not a UI filter (filter at the query/API layer, not just hide it in the frontend; the database default itself should be `CONFIDENTIAL`, so a code path that forgets to set visibility explicitly still fails safe).
- **Role-based access**: enforce at the database/API layer (Supabase RLS or equivalent), not just by hiding UI elements. A parent should never be able to fetch another family's data even via direct API call. A coach has read-only access to their roster — no write access to submissions, tests, or grades.
- **Team billing/access independence**: one teammate's unpaid or unverified status must never lock another teammate out of their own dashboard, curriculum, or certification track.
- **Innovation Journal is append-only**: edits create a new version; don't overwrite/delete history.
- **AI-use disclosure** is required on every stage's SUBMIT step (major submissions), not optional.
- **Project ownership is exactly one of individual or team**: a `Project` row must have exactly one of `individualStudentId` / `teamId` set, never both, never neither. Prisma can't express this as a native schema constraint, so it's enforced twice: (1) a zod refinement in application code runs before every `Project` creation and rejects a request with both or neither set, with a clear error message; (2) a hand-written raw SQL `CHECK` constraint on the `Project` table (`num_nonnulls("individualStudentId", "teamId") = 1`), added directly in the migration file that creates the table, as a fail-safe in case the app-layer check is ever bypassed or has a bug. Do not rely on the app-layer check alone — that was the whole point of catching this before schema lock. If a migration is ever reset/regenerated, re-add this constraint by hand; Prisma will not recreate it from `schema.prisma`.

## Data model — build all of these now, even Phase 2 ones

Building the full schema up front avoids migrations mid-build. Core entities (see product spec Section 40 for full field-level detail):

`User`, `Student`, `Parent`, `School`, `Team`, `Season`, `Enrollment`, `CurriculumModule`, `Lesson`, `Assignment`, `Submission`, `Assessment`, `Question`, `Attempt`, `Project`, `JournalEntry`, `SafetyReview`, `IPReview`, `AIDisclosure`, `SpeakingSubmission`, `CharacterChallenge`, `EthicsChallenge`, `QualificationRecord`, `JudgeAssignment`, `JudgeScore`, `Certificate`, `Payment`, `Notification`, `MediaConsent`

Don't collapse these into one giant table (the spec explicitly calls this out). Remember: `Enrollment` and `Payment` key off `Student`, not `Team`. `Project.visibility` must default to `CONFIDENTIAL` at the schema level (e.g. Prisma `@default(CONFIDENTIAL)`), not just as an application-layer default — this ensures fail-safe behavior even if a code path creates a `Project` without explicitly setting visibility.

## Build sequence — 14-day sprint plan (2-day sprints)

Follow this order. Each sprint builds on the previous one's infrastructure — don't jump ahead even if a later feature looks quick, because most of them depend on auth, the stage template, or the payment/enrollment model landing first.

**Sprint 1 (Days 1–2):** Login & Authentication · Public Homepage · How It Works

**Sprint 2 (Days 3–4):** Curriculum Overview · Divisions & Eligibility · Pricing (per-participant table) · National Finals (static) · FAQs · Contact Us · Terms/Privacy/Competition Policies

**Sprint 3 (Days 5–6):** Participant Registration · Parent/Guardian Consent · Team Creation & Team Management · Payment Processing (manual) · Payment & Enrollment Records (per-student)

**Sprint 4 (Days 7–8):** Student Dashboard · Six-Stage Learning Path template · Stage Progress Tracking · Sequential Stage Unlocking · Online Learning Materials · Practice Activities · File & Media Uploads · build INSIGHT + INVESTIGATE fully

**Sprint 5 (Days 9–10) — heaviest sprint, budget slack here first if anything slips:** Project Submission Portal · Innovation Journal · Speaking Challenge Submissions · Character Challenges · Ethics Challenges · AI-Use Disclosure · Safety Screening · IP Checkpoint · Public/Confidential Project Controls · Team Contribution Tracking · build IMAGINE, IP Checkpoint, ITERATE, IMPACT, INFLUENCE

**Sprint 6 (Days 11–12):** Online Assessments · Assessment Retakes · Deadline Tracking · Qualification Eligibility Tracker · April 30 / July 30 Qualification Windows · Admin Dashboard core · Submission Review · IP Review Workflow · Safety Review Workflow · User & Role Management · School Management · Season Management

**Sprint 7 (Days 13–14):** Parent Dashboard · Coach/Teacher Dashboard · Email Notifications (welcome/stage-complete/next-stage-unlocked) · Digital Badges · Certified Innovator Credential (basic, no QR yet) · Final Innovation Portfolio (auto-compiled) · Mobile-responsive pass · Accessibility pass · Privacy/consent audit · Search & Filtering · bug bash · deploy to staging

## Deferred (Phase 2) — schema-ready, UI not built yet

Don't build UI/workflow for these yet. The data model should already account for them so no migration is needed later:

- Judge Portal, Judge Assignment, Conflict-of-Interest Declaration, Judging Rubrics & Scoring
- Semifinalist/Finalist Management
- National Finals Management (dynamic, beyond the static info page)
- Public Innovation Gallery
- Winner & Award Pages
- National Qualification Submission (full package form)
- Innovator Competency Transcript (depends on judge-entered ratings)
- Certificate Verification (public QR/link verification page)
- Curriculum Management (admin authoring UI — V1 content is built directly, not admin-editable)
- Assessment Question Bank Management (admin authoring UI — V1 banks are seeded, not admin-editable)
- Reporting & Analytics
- Audit Logs UI (basic action logging still happens in the background)
- Help & Support beyond the FAQ/Contact Us pages
- Full notification automation (deadline countdowns, inactivity nudges)

## Working conventions

- Prefer explicit, readable code over abstraction for its own sake — this codebase needs to be maintainable by a non-technical owner working with AI assistance across many sessions.
- Comment non-obvious business logic (especially unlock rules, confidentiality filtering, per-student payment/enrollment logic, and team-vs-individual boundaries) — these are the places a future session is most likely to accidentally break something.
- When a decision in this file conflicts with something a person asks for in a session, flag the conflict rather than silently overriding this file.
- Update this file when a real architectural decision changes (e.g., "we switched from Neon to Supabase") so it stays a reliable source of truth.

## Status log

Keep a running note here of what's actually done vs. planned, so a new session doesn't have to guess.

- **[Aug 10, 2026]** Repo created. Stack decided. Pricing model corrected to per-participant ($111/student; team of N = $111×N, individual billing/access per student). Full 70-item feature backlog sequenced into 7 two-day sprints. Project visibility rule locked: defaults to `CONFIDENTIAL`, only becomes `PUBLIC` on explicit student request at/after IP Checkpoint. Prisma schema not yet drafted. No code written yet.
- **[Aug 13, 2026]** Prisma schema drafted and finalized (`schema.prisma`, 30+ models). Key decisions locked during review:
  - `StudentProject` join table added — links every student to every project they belong to (one row per student per project), independent of `Team`/`TeamMembership`. This is the correct path for "all of this student's projects," including confidentiality checks, rather than traversing through `Team`.
  - When a student leaves a team mid-project, their `StudentProject` row is **never deleted** — `isActiveMember` is set to `false` instead. Past contributions stay permanently on a student's record (Innovator Profile, journey history) even after they leave. `isActiveMember = false` only gates future edit access, not visibility of past work.
  - `Submission.content` and the `SafetyReview`/`IPReview` `answers` fields are JSON, validated in application code (zod per stage), not typed columns — a deliberate speed tradeoff for a solo 2-week build; revisit if the team grows.
  - `Enrollment`/`Payment` remain hard-keyed to `Student` (never `Team`), with `@@unique([studentId, seasonId])` so a team can't accidentally share a payment record.
  - `Project.visibility` defaults to `CONFIDENTIAL` at the Prisma level, with a separate `visibilitySetByStudent` boolean so the database can prove a project only went public because a student explicitly asked.
  - No update path modeled on `JournalEntry` — edits are new rows sharing `entryGroupId` with an incremented `version`. Must not expose PATCH/PUT on this model at the API layer.
  - **Note:** `i3League_2Week_Build_Plan.md` (the original 2–4 person team, Stripe-based, $149 flat-fee draft) is now superseded by `i3League_Sprint_Plan.md` (solo build, manual PayPal/Venmo/Zelle, $111/participant) and this file. Treat the 2-week plan doc as historical only — don't pull scope or payment details from it.
  - Next up: seed data / migration commands, or start Sprint 1 (auth + public site shell).
- **[Aug 13, 2026, later]** Resolved the "exactly one of individualStudent/team" open item on `Project`: enforced at both the app layer (zod refinement before every `Project.create`) and the DB layer (hand-written raw SQL `CHECK` constraint — `num_nonnulls("individualStudentId", "teamId") = 1` — added to the migration that creates the `Project` table). Same fail-safe philosophy as the `visibility` default. See "Core business rules" above and the `Project` model comment in `schema.prisma`. This constraint must be manually re-added if a migration is ever reset, since Prisma doesn't generate it from `schema.prisma`.
- **[Aug 13, 2026, later still]** Repo initialized: scaffolded with `create-next-app` (App Router, TypeScript, ESLint, `src/` dir, no Tailwind). Installed `prisma`, `@prisma/client`, `ts-node`. Ran `npx prisma init` to generate `prisma/schema.prisma` placeholder and `.env`, then replaced the schema with the finalized version and added `prisma/seed.ts`. `package.json` `prisma.seed` entry added. **Not yet done:** no `DATABASE_URL` configured, so `prisma migrate dev` has not been run — the DB has no tables yet, and the `project_exactly_one_owner_check` CHECK constraint has not been added to any migration. Next session needs a Supabase/Neon connection string before continuing to migration + seed.
  - Two bugs found and fixed while validating the schema: (1) `Project.individualStudent` had no opposite relation field on `Student` — added `individualProject Project?` (singular, since `Project.individualStudentId` is `@unique`) to `Student` in `schema.prisma`. (2) this Prisma version (6.19) generates a `prisma.config.ts` on `prisma init` which is now authoritative over `package.json#prisma.seed` — added a matching `migrations.seed` entry to `prisma.config.ts` so `prisma db seed` actually finds `prisma/seed.ts`; kept the `package.json` entry too since it's harmless (just ignored, with a warning).
  - Also set `turbopack.root` in `next.config.ts` to silence a spurious "ignored package-lock.json" warning caused by an unrelated stray lockfile in the parent home directory — not a project issue, just workspace-root ambiguity for Turbopack.
- **[Aug 13, 2026, even later]** Built the "Blueprint" design system while waiting on a `DATABASE_URL`, using the marketing "coming soon" page as the visual reference: dark navy/grid background, Space Grotesk (display) + Inter (body) + JetBrains Mono (labels/mono), ignition-orange + spark-teal accent pair. This is a committed brand look, not a light/dark user preference — no `prefers-color-scheme` switch.
  - Tokens live in `src/app/globals.css` (`:root` custom properties for color/type/spacing/radius). Fonts are loaded via `next/font/google` in `src/app/layout.tsx`, with the font CSS variables (`--font-display` / `--font-body` / `--font-mono`) doubling as the type tokens — no separate font-name mapping layer.
  - Reusable components under `src/components/design-system/`: `GridBackground`, `Eyebrow` (pill/label variants), `Button` (primary/secondary, polymorphic via `as`), `Panel`, `Cube` (the rotating hero visual, parameterized by `size`/`faces` so it's reusable, not hardcoded to the teaser copy), and `StepGrid` (the dashed-divider 3-column "How It Works" layout pattern — color-cycles paper/accent/accent-2 for exactly 3 steps, matching the source page).
  - `src/app/design-system` is a style-guide page showing every token/component together — not a real app route, just a reference. The actual public homepage (nav, hero, stage overview, etc.) has **not** been built yet; scope for this session was tokens + components only. `src/app/page.tsx` is still the default Next.js starter page.
  - Verified via `tsc --noEmit`, `eslint`, and an actual `next dev` run (fetched `/design-system`, HTTP 200, no console/server errors).
- **[Aug 14, 2026]** The dark "Blueprint" design system above was superseded same-day by a canonical, more complete spec the user handed over directly: **`docs/design-system.md`** ("I³ League — Canonical Design System v1"), sourced from an already-approved reference homepage (`i3-homepage-v20-lower-3-logo.html`). This is now the single source of truth for visual design — treat the "Design tokens"/"Reusable components" notes in the entry above as historical only.
  - New direction: **light** "innovation-lab / competition editorial" look — cool mineral canvas (`#E6ECF5`), warm off-white paper (`#FFFDF8`), ink-navy text/authority color (`#10213D`), collegiate cobalt (`#3158D8`) for progress/navigation, coral (`#E35E49`) reserved for action + the IP Checkpoint only. Depth comes from hard offset shadows/planes, never glow or gradients — see doc Section 19 for explicit anti-patterns to avoid.
  - Fonts swapped: Chakra Petch (display), Hanken Grotesk (body), IBM Plex Mono (mono/metadata) — replacing Space Grotesk/Inter/JetBrains Mono. Loaded via `next/font/google` in `src/app/layout.tsx`; export names are `Chakra_Petch`, `Hanken_Grotesk`, `IBM_Plex_Mono`.
  - `src/app/globals.css` rewritten with the full v1 token set (color/type-scale/spacing/radius/container/header-height), including responsive `--header-h`/`--gutter` overrides at the doc's canonical breakpoints (900px/820px).
  - All prior dark-theme components under `src/components/design-system/` were deleted and rebuilt from the new spec: `Logo` (Section 4 — square ink mark, soft-blue "I", coral superscript "3", LEAGUE wordmark), `Button` (Section 9 — primary/ghost, hard bottom-shadow lift, trailing chevron), `Cube` (Section 11 — the six-faced journey visual, canonical face tones/edges, slow idle yaw with `prefers-reduced-motion` fallback), `Eyebrow` (two mono label sizes), `Panel` (standard/selected variants per Section 17), `GridBackground` (Section 8's faint 72px drafting grid).
  - **Deliberately not built yet:** the homepage's full scroll-jacking cube choreography (Section 11's stage-by-stage scroll sequence, journey rail, journey masthead/watermark/progress bar) and the editorial page sections (proof grid, field band, final CTA, footer) from the reference HTML. Those are homepage-construction work, not design-system primitives — matches the earlier "design system only" scope decision. The static `Cube` component is ready to be wired into that hero once homepage building starts.
  - Verified via `tsc --noEmit`, `eslint`, and an actual `next dev` run (fetched `/design-system`, HTTP 200, correct fonts + all component content present, no console/server errors).
- **[Aug 15, 2026]** First migration run against a live Supabase Postgres instance — the schema is no longer just a file, it's a real database.
  - Created the Supabase project. In the project's Security settings (Data API section), deliberately left **Data API disabled** and **"Automatically expose new tables" disabled** — the app talks to Postgres via Prisma from server-side code only, never via `supabase-js`/PostgREST, so there's no reason to expose tables over REST at all. **"Enable automatic RLS" is on**, so every table Prisma creates gets Row Level Security turned on automatically (verified: all 41 app tables show `relrowsecurity = true`), even though Prisma itself has no concept of RLS and would never set this. No per-role policies exist yet — that's correct for now, not an oversight: real policies need `auth.uid()` mapped to `User.supabaseUid` → role, which requires Supabase Auth (Sprint 1, not built yet). RLS-enabled-with-no-policies is fail-closed for the Data API path in the meantime; it's not what protects the app itself, since Prisma connects as the `postgres` role, which bypasses RLS by design — the app-layer role checks (per CLAUDE.md "Role-based access") are still the real enforcement once API routes exist.
  - `.env` now holds real credentials: `DATABASE_URL` (pooled, port 6543, PgBouncer, used at app runtime) and `DIRECT_URL` (unpooled, port 5432, used for migrations — PgBouncer transaction mode can't run the DDL/shadow-db operations `prisma migrate` needs, confirmed by a hung `migrate dev` before this was fixed). Both `schema.prisma`'s `datasource` block and `prisma.config.ts`'s `datasource` block needed `directUrl` added — Prisma reads both, and the config file's datasource takes precedence over the schema file's when both exist.
  - Ran `prisma migrate dev --create-only` first specifically so the `project_exactly_one_owner_check` CHECK constraint (see "Core business rules" above) could be hand-inserted into the generated `migration.sql` — right after `CREATE TABLE "Project"` — before applying. Applied and confirmed live via `pg_get_constraintdef`. This is the pattern to repeat for any future migration that touches `Project`: always `--create-only` first, never let `migrate dev` apply directly, so this constraint doesn't get silently dropped by a regenerated migration.
  - 42 tables now exist in `public` (41 app models + Prisma's own `_prisma_migrations`).
  - Next up: Sprint 1 — Supabase Auth wiring (login/session, mapping `auth.uid()` to `User.supabaseUid`/`Role`), then real RLS policies once that identity model exists, then the public homepage.
- **[Aug 15, 2026, later]** Correction to the note directly above: the public homepage referenced in the Aug 14 design-system entry ("the actual public homepage... has not been built yet") **was actually built** at some point between that entry and now — `src/app/page.tsx` and a full `src/components/homepage/` set (`Header`, `JourneyHero`, `CurriculumBridge`, `ProblemSection`, `ProofSection`, `FinalCta`, `Footer`, `Checkerboard`) already exist and are wired up. That work happened without a matching status log entry (visible only via `git log`, commit "I3League Prisma schema and homepage"). Flagging this here so a future session doesn't waste time rebuilding it or get confused by the stale note above — treat the Aug 14 entry's "not built yet" line as outdated.
- **[Aug 15, 2026, even later]** Sprint 1 — Supabase Auth (login/session, `auth.uid()` → `User`/`Role` mapping) built and verified end-to-end against the live Supabase project.
  - Locked decisions: Student/Parent/Coach self-serve via a public signup form with a role selector; Judge/Admin are never self-serve (enforced server-side, not just by the UI omitting them as options — see below). Email confirmation is required before login. `auth.users` → `public.User` sync happens via a hand-written Postgres trigger, not application code, for the same fail-safe reason as the `project_exactly_one_owner_check` constraint: it can't be bypassed by a bug in a Server Action.
  - Installed `@supabase/supabase-js` + `@supabase/ssr` — had to bump both to matching versions (`^2.112.3` / `^0.12.4`) since the versions npm resolved by default didn't satisfy `@supabase/ssr`'s peer requirement.
  - New env vars in `.env`: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` (server-only, unused this sprint — reserved for a later admin-created-account flow for Judge/Admin).
  - New `src/lib/prisma.ts` — the Prisma client singleton this repo never had (only `prisma/seed.ts` did a bare `new PrismaClient()`). Without it, every dev hot-reload opens a fresh pool of connections against the pooled `DATABASE_URL` and eventually exhausts Supabase's PgBouncer limit.
  - New `src/lib/supabase/{client,server,middleware}.ts` following the standard `@supabase/ssr` App Router pattern (browser client, async server client using `next/headers` cookies, and a request/response-bound variant for the proxy). Server-side code always calls `getUser()`, never `getSession()` — the latter trusts the cookie without revalidating against Supabase and is spoofable in server contexts.
  - **`src/proxy.ts`, not `middleware.ts`**: Next.js 16.3.1 (the version this repo is on) deprecated the `middleware.ts` file convention in favor of `proxy.ts` (rename the file, rename the export from `middleware` to `proxy`; the `config`/`matcher` export is unchanged). Discovered this via a real warning in the dev server log during testing, not by guessing — confirmed via Next's own migration doc. If a future session copies a `@supabase/ssr` tutorial that still says `middleware.ts`, rename it to `proxy.ts` on the way in.
  - Trigger migration: `prisma migrate dev --create-only --name add_auth_user_sync_trigger` produced an **empty** migration file (no `schema.prisma` diff exists to scaffold from, since the trigger lives entirely outside Prisma's model — this is expected, not an error). Hand-wrote the trigger SQL into it (function `public.handle_new_auth_user()`, `SECURITY DEFINER`, `search_path = ''` to prevent search-path hijacking, reads `role` from `raw_user_meta_data`, generates the `User.id` itself via `gen_random_uuid()` since Prisma's `@default(uuid())` is client-side only and never reaches the migration SQL). **Applying it required `prisma migrate deploy`, not `prisma migrate dev`** — `migrate dev` diffs against a temporary shadow database that doesn't have Supabase's `auth` schema, so it fails with `schema "auth" does not exist`. `migrate deploy` applies directly with no shadow db and is the correct tool any time a migration references `auth.*`. Same rule as the CHECK constraint: review the hand-written SQL before applying, never let it auto-apply.
  - Built `/signup` (role radios limited to Student/Parent/Coach — Server Action independently re-validates this allowlist so a crafted `role=ADMIN` POST is rejected even with the UI bypassed entirely, confirmed by testing exactly that), `/login`, `/auth/callback` (Route Handler, exchanges the confirmation code for a session), `/auth/auth-code-error`, `/signup/check-email`, and a minimal `/dashboard` proving the full identity chain resolves (`getUser()` → `prisma.user.findUnique({ where: { supabaseUid } })` → renders email + role). Four of these share one new `src/components/auth/AuthCard.tsx` (extracted after noticing the same centered-card layout would otherwise be copy-pasted four times — real duplication, not premature abstraction).
  - New design-system primitive: `src/components/design-system/Input.tsx` (+ `Input.module.css`), built to the already-written but previously unimplemented Section 17 "Form inputs" spec in `docs/design-system.md`.
  - `Student`/`Parent`/`Coach`/`Judge`/`Admin` profile rows are **not** created at signup — they're already-optional 1:1 relations on `User` in the schema, so "signed up but hasn't completed registration" is just `user.role === "STUDENT" && user.student === null`, no schema change needed. `/dashboard` shows a "Registration incomplete" notice in that state. Sprint 3 (Participant Registration) is what actually creates that profile row.
  - **Verified end-to-end against the live Supabase project**, not just locally reasoned about: real `curl` POSTs replaying the exact multipart form + Next.js Server Action hidden fields a browser sends (confirmed this works because these forms use real progressive-enhancement `<form action={...}>` submissions, not fetch-only RSC calls) — proved the login wrong-password path renders the generic error inline with no crash, and that `role=ADMIN` is rejected server-side. Used the Supabase Admin API (service-role key, with `node --experimental-websocket` — see Node/WebSocket note below) to create real `auth.users` rows for STUDENT/PARENT/COACH and confirmed the trigger produced correct `public.User` rows for all three, then fetched `/dashboard` with a real forged session cookie built from a real access/refresh token pair and confirmed it rendered the correct email/role/registration-incomplete notice/logout button. All test accounts cleaned up afterward (verified via `listUsers`, zero left over).
  - Two things worth knowing but **not bugs**: (1) Supabase's built-in email provider is rate-limited (a handful of sends/hour) and also rejects `@example.com`-style addresses as invalid — hit both while testing; neither is an app bug, just constraints of the shared dev mailer. For repeated local testing, prefer the Admin API (`email_confirm: true`) over triggering real signup emails. (2) `@supabase/supabase-js` warns on Node ≤20 (this machine runs 20.18.0) because its Realtime client needs a global `WebSocket`, which Node 20 doesn't have by default — this **did** crash a raw standalone Node test script, but did **not** affect the actual Next.js app in any of the live tests above (Next's dev server process has a WebSocket global available some other way, likely via its own HMR setup). Not a blocker; worth revisiting if a real crash ever surfaces in the app itself, and resolved by upgrading to Node 22+ eventually.
  - RLS still has zero policies (per the Aug 15 entry above) — now that `auth.uid()` ↔ `User.supabaseUid`/`role` actually resolves, writing real per-role RLS policies is unblocked but still deliberately not done this sprint. The Data API is also still off, so this isn't reachable externally yet regardless.
  - Next up: real RLS policies now that the identity model exists (optional — Prisma still connects as `postgres` and bypasses RLS regardless, so this is defense in depth, not blocking), then continuing the Sprint 1 build sequence.
- **[Aug 16, 2026]** **Sprint 1 confirmed complete.** All three items are built and committed: Login & Authentication (Aug 15 entry above), Public Homepage (`src/app/page.tsx` + `src/components/homepage/`, per the Aug 15 correction entry), and How It Works (`src/app/how-it-works`, commit `ca35a4a` — also extracted `NavLink` as a shared component so Header/Footer stop keeping separate copies of the nav list). Starting Sprint 2 next: Curriculum Overview, Divisions & Eligibility, Pricing, National Finals (static), FAQs, Contact Us, Terms/Privacy/Competition Policies.
- **[Aug 16, 2026, later]** **Scope change from the client: no standalone Divisions & Eligibility page.** A full Eligibility page (`/eligibility`) was built this session — editorial hero with a stat "snapshot" card, a "who it's for" three-up grid, an on-ink "what the program asks of you" panel, and a "joining vs. qualifying" comparison with real Season deadlines — then the client decided against a separate page for this content. Rather than deleting the content, it was redistributed into the pages it fits best contentwise, and `/eligibility` (route + `src/components/eligibility/`) was removed along with its nav entry in `src/lib/site-nav.ts`:
  - **How It Works page** (`src/app/how-it-works/page.tsx`) gained `DistinctionSection` (`src/components/how-it-works/DistinctionSection.tsx`) — the "joining is not the same as qualifying" participation-vs-qualification comparison, with real `springQualifyDeadline`/`summerQualifyDeadline` from Season — placed after the 8-step path, since it's the natural follow-up question once a reader understands the steps. This required converting the page component to `async` to fetch Season (it wasn't before).
  - **Pricing page** (`src/app/pricing/page.tsx`) gained a new "Who can register" `InfoBand` (grade range, team size from Season, choose-your-own-problem) placed before the pricing table, since "can I register" is the natural question right before looking at price.
  - Divisions & eligibility content is therefore **done**, just not as its own page — don't recreate `/eligibility` or re-add it to `SITE_NAV_LINKS` without checking with the client first, and don't duplicate this content again elsewhere without first checking whether Curriculum/How It Works/Pricing already cover it.
  - **[Same day, later still]** `WhoItsFor` (the "who it's for" three-up grid — grade divisions, solo/team, choose-your-own-problem) briefly landed on the Curriculum page per the note above, then moved again to `src/components/how-it-works/WhoItsFor.tsx`, rendered on How It Works right after the hero and before the 8-step path — same "is this for me before what are the steps" reasoning, just a different page. Now that `WhoItsFor` and `DistinctionSection` both live on How It Works, their copy was trimmed to remove overlap: `WhoItsFor` no longer mentions "judged separately at Nationals" or the six-stage framework (both now owned by `DistinctionSection`'s cards below it), and `DistinctionSection`'s Participation card no longer restates "middle and high school students working independently or with a team" (already covered by `WhoItsFor` above it) — it now just describes what changes once you've registered. Curriculum page is back to just Hero → StageDetailList → StageWorkflow.
- **[Aug 16, 2026, whole-site audit]** Ran a full-repo audit (security/business-rules, code quality/stale code, design/UX) across three parallel review passes, then fixed what came back. Findings and outcomes:
  - **Critical, fixed and live-verified: auth role-escalation hole.** The Postgres trigger that syncs `auth.users` → `public.User` (see the Aug 15 Sprint 1 entry) cast `raw_user_meta_data->>'role'` straight to the `Role` enum with zero allowlist. `raw_user_meta_data` is client-settable at signup via Supabase's public GoTrue REST endpoint (`POST {SUPABASE_URL}/auth/v1/signup`), reachable with just the public anon key — so a request straight to that endpoint, bypassing the Next.js `/signup` form and its allowlist in `src/app/signup/actions.ts` entirely, could set `role: "ADMIN"` and the old trigger would honor it. This is exactly what "Judge/Admin are never self-serve... enforced server-side, not just by the UI" (Aug 15 entry) was supposed to prevent, and wasn't — the trigger, not the Server Action, is the real server-side boundary here, since it fires regardless of which client hit `auth.users`.
    - Fixed in two migrations: `20260816120000_harden_auth_role_trigger` (first pass — allowlisted `user_metadata` to STUDENT/PARENT/COACH, and added a second branch trusting `app_metadata` for a hypothetical future ADMIN/JUDGE admin-creation flow), then `20260816130000_simplify_auth_role_trigger` (correction — see below).
    - **Live-testing the first fix surfaced a second bug in the fix itself**: the `app_metadata` branch never fires. Verified directly against the real Supabase Admin API (`POST /auth/v1/admin/users`) that GoTrue writes `raw_user_meta_data` atomically on the `auth.users` INSERT, but applies a request's custom `app_metadata` via a separate UPDATE afterward — so this `AFTER INSERT` trigger never sees it. Confirmed via a live test that passed both `app_metadata.role=ADMIN` and `user_metadata.role=STUDENT` together: the resulting row landed as `STUDENT`, not `ADMIN`, proving the `app_metadata` branch was silently dead (fail-safe, not a security hole — it never granted anything, just didn't do what its comment claimed). Corrected in `20260816130000_simplify_auth_role_trigger`, which drops that branch entirely — the trigger now only ever assigns STUDENT/PARENT/COACH from `user_metadata`, rejects everything else (NULL role → NOT NULL violation → whole `auth.users` insert rolls back, same fail-safe pattern as before). **Admin/Judge account provisioning is still unbuilt (Phase 2, per "Deferred" section) and will need its own mechanism when that sprint starts** — don't reintroduce an `app_metadata`-at-insert-time approach without first confirming GoTrue actually populates it at INSERT time for whatever call is used (it doesn't for `admin.createUser`).
    - Both migrations applied live via `prisma migrate deploy` (required, not `migrate dev` — same reason as the Aug 15 trigger migration: no `auth` schema in the shadow DB) and verified end-to-end against the real Supabase Admin API: `role=ADMIN`/`JUDGE`/no-role-at-all via `user_metadata` all correctly rejected (500, no `public.User` row created, no orphaned `auth.users` row either since the whole transaction rolls back); `role=STUDENT`/`PARENT`/`COACH` all still correctly succeed. Test rows cleaned up via the Admin API afterward, zero left over — same rigor as the Aug 15 Sprint 1 verification.
    - Also confirmed: Supabase's project already has server-side Turnstile captcha enforcement on the public signup endpoint (rejects any request without a valid `captcha_token`, independent of this trigger) — a real curl/fetch straight to `/auth/v1/signup` is blocked by that first. This doesn't reduce the trigger fix's value (defense in depth — captcha config could change, and the trigger is now correct regardless), but it's why direct testing had to go through the Admin API (service-role key, bypasses captcha by design) instead.
  - **Fixed:** `src/components/how-it-works/steps.ts` hardcoded "April 30"/"July 30" as static copy in the "Qualify" step, even though `DistinctionSection` on the same page correctly pulls those dates live from `Season` — violated "Season-driven config — do not hard-code." `steps.ts` now exports `buildSteps(springDeadline, summerDeadline)`; `ProcessSection` takes the dates as props from `src/app/how-it-works/page.tsx`'s `getActiveSeason()` call, same pattern `DistinctionSection` already used.
  - **Fixed:** decorative coral misuse — `ProofSection`, `WhoItsFor`, and `ThreePhases` all recolored the middle item of their 3-up grids' underline accent coral purely for visual variety, with no action/IP relationship. Per `docs/design-system.md`, coral is reserved for actions + the IP Checkpoint specifically. Removed the overrides (and the now-pointless `itemAccent` conditional class in the two `.tsx` files) — all three underlines are uniformly cobalt now.
  - **Fixed:** pricing page skipped a heading level (h1 → h3, no h2 anywhere on the page) — `InfoBand`'s title is now an `h2` (it was the only heading-bearing shared component on that page, so this was safe to change without checking other call sites — it currently has none besides pricing).
  - **Fixed:** `FinalCta`'s button used a hardcoded `#8290a6` box-shadow with no matching token — swapped for `var(--dim)` (`#8d98a7`), the token it was clearly approximating.
  - **Confirmed correct, left alone:** `Project.visibility` default, the `project_exactly_one_owner_check` CHECK constraint, `Enrollment`/`Payment` per-student keys, the Aug 15 trigger's `SECURITY DEFINER`/`search_path = ''` hardening, no `getSession()` usage anywhere, open-redirect protection on `/auth/callback`, the password-recovery cookie flow, `tsc`/`eslint` both clean.
  - **Deliberately not touched:** `src/components/legal/LegalLayout.tsx` + `src/lib/legal-nav.ts` (unwired — no Terms/Privacy route exists yet) and `src/components/marketing/FaqAccordion.tsx` (unwired — no FAQ route yet) are pre-built-ahead-of-schedule for Sprint 2, not stale code — don't delete them, just wire them up when Sprint 2's Terms/Privacy/FAQ pages get built. A larger refactor consolidating `PageHero`/`CurriculumHero`/`PageIntro` into one shared hero primitive (they're structurally near-identical) was flagged by the audit but not done — bigger-surface-area change than the rest of this pass, worth doing but deliberately deferred rather than rushed in.
  - Also noted but not acted on (lower-value, broad, low-risk-of-actual-bug): scattered raw hex/px values across several `.module.css` files duplicate existing tokens (e.g. several exact-value duplicates of `--text-body`/`--text-nav`/`--text-eyebrow`, and 10+ near-identical grays that could collapse to `--muted`/`--dim`) — token-discipline drift, not a visual bug today, worth a dedicated cleanup pass later rather than a scattershot edit across a dozen files in this one.
- **[Aug 17, 2026]** **Sprint 2 confirmed complete.** Built the four remaining static/content pages — National Finals, FAQs, Contact Us, and Terms/Privacy/Competition Policies — finishing the Sprint 2 list (Curriculum Overview, Divisions & Eligibility, and Pricing were already done per the Aug 16 entries above).
  - **`/national-finals`** (`src/components/national-finals/`): built as a standalone page — `NationalFinalsHero` (SplitHero shell, stat box showing the real `Season.nationalFinalsDate`), `FinalsOverview` (Pattern C, deliberately high-level — judging rubrics/scoring and the Judge Portal itself are Phase 2, so this describes what a visitor sees at the event, not judging mechanics), and `QualificationPath` (wires up `InfoBand`/Pattern B, which existed unused since the Pricing page's "who can register" band was removed — see Aug 16 entry). **Superseded same day — see the Aug 17 "later" entry below: the site owner asked for this folded into How It Works instead and the standalone page removed.** `QualificationPath`'s InfoBand usage didn't survive the fold-in (see below); `FinalsOverview`'s three-card content did, moved into How It Works.
  - **`/faqs`**: `src/components/faq/faq-data.ts` exports `buildFaqSections(...)`, same `build*()`-takes-Season-values pattern as `how-it-works/steps.ts`'s `buildSteps` — every answer that states a price, deadline, or team size reads it from `Season`, never hard-coded copy. `FaqSections` renders four grouped categories (Registration & Pricing, Teams & Projects, Curriculum & Progress, Qualification & Finals) through the existing `FaqAccordion` (previously built but unwired, per the Aug 16 audit's "deliberately not touched" note).
  - **`/contact`**: `ContactChannels` — three static `mailto:` panels (general/registration/partnerships). No working contact form: there's no `ContactSubmission` model in the schema and no email-sending service configured (see CLAUDE.md tech stack), so a real form would need infrastructure this MVP doesn't have yet. Static mailto links match this page's Sprint 2 scope; revisit if a real inbound-message flow gets prioritized later.
  - **`/terms`, `/privacy`, `/competition-policies`**: real placeholder content wired into the existing (previously unwired) `LegalLayout`/`legal-nav.ts`, keeping `LegalLayout`'s "Draft — pending legal review" notice intact per its own comment (i3League deals with minors/parental consent, so this can't read as reviewed legal copy yet). Content reflects business rules already locked elsewhere in this file (per-student payment/refund handling, confidential-by-default projects, safety screening, AI-use disclosure, append-only Innovation Journal) rather than inventing new policy; `/competition-policies` also pulls `maxTeamSize` and both qualification deadlines from `Season` instead of hard-coding them.
  - **New shared primitive:** `src/components/design-system/PageIntro.tsx` — the "lighter, single-column editorial opener" the Aug 15/16 entries already anticipated by name (`SplitHero.tsx`'s own comment, and the Aug 16 audit's "deliberately not touched" note) but never built. Used by `/faqs` and `/contact`, which have no right-slot widget worth a full `SplitHero`.
  - **Nav updates:** `SITE_NAV_LINKS` (`src/lib/site-nav.ts`) gained `/national-finals` (later reverted — see below); a new `SUPPORT_NAV_LINKS` export (FAQs, Contact Us) surfaces in a new "Support" column in `Footer.tsx`, and the footer's legal line now links out to all three `LEGAL_PAGES` instead of being plain text.
  - Verified via `tsc --noEmit` (clean), `eslint` on every new/changed file (clean after removing one unused `formatSeasonDate` import in `/terms`), and real `curl` fetches of all ten public routes against the running dev server — all 200s, with `/national-finals`, `/faqs`, `/competition-policies`, and `/terms` spot-checked to confirm they render live `Season` values (`$111 per student`, `August 15, 2027` Finals date, `maxTeamSize: 4`, `April 30, 2027` / `July 30, 2027` deadlines) rather than placeholders.
  - **Not done / still open from the Aug 16 audit:** the `PageHero`/`CurriculumHero`/`PageIntro` consolidation the audit flagged (now three near-identical hero-shell patterns instead of two, since this session added `PageIntro`) is still deliberately deferred — bigger-surface-area refactor, not blocking. The scattered raw hex/px token-drift cleanup is also still untouched.
  - **Sprint 2 is now fully done.** Next up: Sprint 3 — Participant Registration, Parent/Guardian Consent, Team Creation & Team Management, Payment Processing (manual), Payment & Enrollment Records (per-student).
- **[Aug 17, 2026, later]** Site owner asked for National Finals content folded into How It Works instead of living on its own page, with no overlap against what How It Works already said. Removed `src/app/national-finals/` and `src/components/national-finals/` entirely, dropped `/national-finals` back out of `SITE_NAV_LINKS`, and added `src/components/how-it-works/NationalFinalsSection.tsx` (+ its `.module.css`), rendered on `/how-it-works` right after `DistinctionSection`.
  - Checked for overlap deliberately before writing this: `steps.ts`'s step 07 ("Compete") already briefly names the Finals, and `DistinctionSection` already owns the qualification-windows/join-vs-qualify explanation — so `NationalFinalsSection` states only what neither of those does: the actual `Season.nationalFinalsDate`, and the three-card "what happens at the event" breakdown (Showcase/Defense/Recognition) carried over from the deleted `FinalsOverview`. It does not restate the spring/summer deadlines or the join-vs-qualify distinction.
  - `InfoBand`/Pattern B is therefore unused again (same status as before the Aug 17 morning entry — the deleted `QualificationPath` was its only caller) — still a real, previously-built component worth reusing before writing a new one, just nothing currently calls it.
  - Verified via `tsc --noEmit` (clean), `eslint` (clean), and live `curl` checks: `/national-finals` now 404s, `/how-it-works` still 200s and renders the new section's heading/date/three cards, and the real `Season.nationalFinalsDate` (`August 15, 2027`) appears correctly. Confirmed the Header/Footer primary nav no longer link to `/national-finals` anywhere on the homepage.
- **[Aug 19, 2026]** **Sprint 3 confirmed complete.** Built Participant Registration, Parent/Guardian Consent, Team Creation & Management, and manual Payment Processing/Enrollment Records — the first sprint that writes real `Student`/`Parent`/`Team`/`Consent`/`Enrollment`/`Payment` rows through actual app flows instead of only the seed script.
  - **Schema addition** (migration `20260819090000_add_registration_fields`): `Student.guardianEmail` (required), `Team.joinCode` (`@unique`), and `Season.paypalLink`/`venmoHandle`/`zelleInfo` (nullable). Same shadow-DB problem as the Aug 16 trigger migrations hit again — `prisma migrate dev --create-only` fails with P3006 because the shadow database replay chokes on the earlier `auth.*` trigger migrations (no `auth` schema there). Hand-wrote this migration's SQL instead and applied with `prisma migrate deploy`, same workaround as before. **This is now the established pattern for any future migration in this repo, not a one-off**: always `--create-only` first for anything that doesn't touch `auth.*`, but if it P3006s, hand-write the SQL and `migrate deploy` it. `Student.guardianEmail` and `Team.joinCode` both needed a backfill-then-constrain sequence in the SQL (temporary default / generated value, then `SET NOT NULL`) since the seeded rows already existed.
  - **How Student and Parent get linked**, since both roles self-serve independently (Sprint 1) and the schema requires `Student.userId`/`Parent.userId` to already point at a real `User` — neither can be created "on behalf of" the other: a student enters their parent's email as `guardianEmail` at `/register`. If a `Parent` profile with that email already exists at that moment, the link (`StudentParent`) is created immediately; otherwise the *parent's* `/register` step (fullName/phone) does the same lookup in reverse when it runs, auto-linking any already-registered student whose `guardianEmail` matches. Order-independent by design — works whichever of the two registers first. Verified live: a student with a guardian email matching an *unrelated* second student's different guardian email correctly did **not** get cross-linked, and a parent could not reach `/consent/[studentId]` for a student they have no `StudentParent` link to (redirected to `/dashboard`, not a 403 page — deliberately doesn't confirm or deny the student id exists).
  - **Team joining**: `Team.joinCode` is a 6-character code (excludes `0/O/1/I/L` — see `src/lib/join-code.ts`) generated at team creation and shown once on `/register/payment` via a query param right after creation. Verified live: a second student joining via that code lands on the same `Team`, and `Season.maxTeamSize` is re-checked at membership-creation time (not just at page load) to close the obvious TOCTOU gap for two students joining at once near the limit.
  - **Payment/Enrollment timing** deliberately matches CLAUDE.md's payment-flow steps literally: `Enrollment` and `Payment` are **not** created at `/register` — only at `/register/payment` submission, together, in one write, with `Payment.status: SUBMITTED` immediately (there's no window where a `PENDING` `Payment` row exists, since `Payment.method` is a required non-nullable field and isn't known until that form is submitted). A `REJECTED` payment reuses the same `/register/payment` form to update the existing 1:1 `Payment` row (method/reference/screenshot, status back to `SUBMITTED`, `rejectionReason` cleared) rather than creating a second row. Verified live end-to-end: two teammates' `Enrollment`/`Payment` rows are fully independent (confirmed via direct DB read after the run) — exactly the "one teammate's unverified payment never blocks the other" rule.
  - **Payment screenshot storage**: `src/lib/storage.ts` (`saveUploadedFile`) writes to `public/uploads/payment-screenshots/` on local disk — the explicit stand-in for the still-deferred Cloudflare R2 setup (CLAUDE.md tech stack). Every call site goes through this one function so swapping to R2 later is a one-file change. `public/uploads/` added to `.gitignore`.
  - **New shared design-system primitives**: `RadioGroup` (extracted from what was previously duplicated inline in `SignupForm.tsx`'s role selector — `SignupForm.tsx` was migrated onto it too, so there's one implementation, not two) and `Checkbox`, both under `src/components/design-system/`. `AuthCard` gained a `wide` prop (680px vs. the original 440px) for this sprint's longer forms rather than a second card component. `src/lib/innovation-fields.ts` holds the fixed interest list (also intended for `Project.category` in Sprint 5, so a student's stated interests and their eventual project category stay drawn from the same list). `src/lib/auth.ts` (`getCurrentAppUser`) extracts the Supabase-session-to-`public.User` lookup that was about to be duplicated across seven new call sites.
  - `src/proxy.ts` now also gates `/register` and `/consent` behind the same "redirect to `/login` if no session" check `/dashboard` already had.
  - `src/app/dashboard/page.tsx` changed from a pure identity-chain proof into the real "what's my next step" hub for Student/Parent, reading state off which rows exist (no `Student` → register; `Student` but no `Enrollment` → pay; payment `SUBMITTED`/`VERIFIED`/`REJECTED` → matching notice) rather than a status enum. Parent view lists linked students with per-student consent status. Coach branch untouched — Coach registration is explicitly Sprint 7, out of scope here.
  - **Verified end-to-end against the live Supabase project in a real headless browser** (Playwright, not curl-replayed forms — this sprint's forms don't progressively-enhance to a guessable static hidden-field POST the way the Sprint 1 auth forms did), with real Admin-API-created STUDENT/STUDENT/PARENT accounts. One new wrinkle worth recording: `signInWithPassword` (and therefore a real `/login` form submission) is captcha-gated in this Supabase project — confirmed by hitting it directly and getting `captcha_failed` even for an admin-created, already-confirmed user — so login for automated testing has to go a different route: `admin.generateLink({type: 'magiclink'})` + `verifyOtp` with the returned `email_otp` (not `hashed_token`, which came back "expired" immediately — `email_otp` is the one that actually works), which isn't captcha-gated. The resulting access/refresh tokens were injected into the browser via the real `@supabase/ssr` `createBrowserClient` (loaded from esm.sh at the matching installed version, `0.12.4`) calling `auth.setSession()` from inside the page context, so the cookie written is byte-for-byte what the app's own client produces — no hand-rolled cookie format. Full flow walked and passed: student1 registered + created a team + saw the join-code banner + paid; student2 registered + joined via that exact code + paid; parent registered + was auto-linked to student1 only (not student2, different guardian email) + submitted consent with all 8 required boxes checked and media consent deliberately left unchecked; dashboard reflected every state transition correctly. Confirmed via a direct Prisma read afterward that every row (`Student`, `Team`/`TeamMembership`, `Enrollment`/`Payment` ×2 independent, `StudentParent`, `Consent`, `MediaConsent`) matched expectations exactly. All test rows and auth users cleaned up afterward (`userCount`/`studentCount`/`teamCount` confirmed back to the exact pre-test seed baseline: 23/4/1).
  - **Also caught and fixed mid-session**: a stale `next dev` process left running from earlier in the day was still bound to port 3000 and serving a pre-migration Prisma Client, which surfaced as `Unknown argument guardianEmail` the first time the flow was exercised — not a code bug, but worth remembering: Next's dev server doesn't pick up a `prisma generate` that happened after it started (it doesn't watch `node_modules`), so always restart `next dev` after any schema/migration change made mid-session rather than assuming a long-running dev server will pick it up.
  - **Not done / deliberately out of scope this sprint**: Coach registration (Sprint 7), admin payment-verification UI (Sprint 6 — `Payment.status` can currently only reach `SUBMITTED` through the app; flipping to `VERIFIED`/`REJECTED` still requires a direct DB write until then), `StageProgress` row creation (still deliberately left for Sprint 4, which is what actually builds the six-stage unlock logic that would consume it).
  - **Sprint 3 is now fully done.** Next up: Sprint 4 — Student Dashboard, Six-Stage Learning Path template, Stage Progress Tracking, Sequential Stage Unlocking, Online Learning Materials, Practice Activities, File & Media Uploads, and building INSIGHT + INVESTIGATE fully.
- **[Aug 19, 2026, later]** Diagnosed and fixed a "Database error saving new user" reported on the real `/signup` form (not the Sprint 3 flow — plain signup, any role). Root cause: **every row in `public.User` (all 27, including the real `rainaartist@gmail.com` test account from Aug 15) had gone orphaned — Supabase's `auth.users` table was completely empty (confirmed via `admin.listUsers()`, 0 results) while `public.User` still had 27 rows referencing those now-nonexistent accounts.** The `handle_new_auth_user()` trigger itself was fine (verified via `admin.createUser` calls for all three self-serve roles, all succeeded) — the failure only happens when a signup's email collides with one of these orphaned rows: the new `auth.users` insert succeeds (table's empty, no conflict there), but the trigger's `insert into public."User"` then hits the `email` unique constraint against the orphaned row and rolls back the whole transaction, which GoTrue reports as the generic "Database error saving new user".
  - **Cause of `auth.users` being empty is still unknown** — not something this session's code or test cleanup did (test cleanup only ever deleted specific accounts it created by id, never bulk-deleted). Likely a Supabase dashboard action (manually clearing users, a project pause/restore, etc.) that happened outside any Claude Code session. If this recurs, check the Supabase dashboard's Authentication → Users page and audit log before assuming it's an app bug.
  - **Fix applied**: since literally nothing could log into any of the 27 orphaned rows anymore either way, deleted every row across all 40 app tables in dependency order (children before parents) and re-ran `npx prisma db seed` to restore the documented baseline (Maya/Jordan+Priya/Sam, admin/coach/judge, one Season) — same seed data as every previous entry in this log, just freshly inserted. Verified via `admin.createUser` afterward that a fresh signup no longer collides, and via `prisma.user.count()` (12, matching the reseed) that no orphans remain. **The real `rainaartist@gmail.com` account is gone and needs to sign up again from scratch** — that was unavoidable once its `auth.users` row was already gone; there was no way to recover it, only to stop it from blocking future signups.
- **[Aug 19, 2026, security review]** Ran a security review over all of Sprint 3's uncommitted code (login/signup/register/payment/consent/dashboard) — two-phase: identify candidates, then independently re-verify each before trusting it. One candidate (a TOCTOU race letting a team exceed `maxTeamSize` by one under concurrent joins) was investigated and correctly rejected — real race, but a soft capacity edge case, not a security boundary. One was confirmed real and fixed:
  - **Finding, HIGH: self-consent bypass via the guardian-email auto-link.** The `StudentParent` auto-link (Aug 19 entry above) matches on a self-reported email string with zero verification that the matched `Parent` account actually belongs to this student's real guardian. A minor could register a Student account, list a *second email address they also personally control* as `guardianEmail`, then register that second email as a `PARENT` account — auto-linking immediately and granting them authority to submit their own required consent (participation, safety, AI-use, and the photo/video release) with no real guardian ever involved.
  - **First proposed fix was itself wrong, caught before building it**: an email-confirmation-link step was initially proposed, but on reflection this doesn't work — the confirmation would be emailed to the *Parent account's own inbox*, which in the exact attack described is an inbox the attacker already controls. A bilateral challenge between "the student side" and "the parent side" can't distinguish a real separate guardian from one person with two email addresses, since the attacker *is* both parties and can satisfy any such challenge trivially. Worth remembering for any future guardian/age-verification work: self-serve email loops don't close this class of gap; only an independent third party (a human admin, or eventually a coach) can.
  - **Actual fix**: `StudentParent` gained `verifiedAt`/`verifiedByAdminId` (migration `20260819150000_add_student_parent_verification`, same nullable pending→admin-approved pattern as `Payment.verifiedAt`/`verifiedByAdminId` — mirrored deliberately for consistency). The auto-link logic in `src/app/register/actions.ts` (both directions) now deliberately leaves `verifiedAt` unset — the row is created (so the parent shows up on the dashboard at all) but is **not** consent-eligible. Both `src/app/consent/[studentId]/page.tsx` (render-time) and `actions.ts` (defense-in-depth, re-checked independently — a direct POST bypassing the page must not be able to submit consent through an unverified link) now require `verifiedAt != null`, showing a "pending review" notice instead of the consent form otherwise. Dashboard's parent view shows a third state ("Link pending admin review") alongside the existing "Give consent"/"Consent recorded". No admin UI exists yet to flip `verifiedAt` — same as `Payment.status`, it's a direct DB write until Sprint 6's Admin Dashboard exists; noting this explicitly so a future session doesn't assume it's automated.
  - Seed data (`prisma/seed.ts`) sets `verifiedAt: new Date()` on its demo `StudentParent` rows — those represent already-established relationships (and already have real `Consent` rows), not the self-reported-match flow, so they shouldn't land in the same pending state a real new signup would.
  - **Verified live**: reproduced the exact attack end-to-end in a real browser (two real accounts, student listing the attacker's own second email as guardian) — confirmed the `StudentParent` row lands with `verifiedAt: null`, the dashboard shows "pending" not "Give consent", `/consent/[studentId]` refuses to render the form, and zero `Consent` rows get created. Then simulated the admin-approval step (direct DB update, matching how payment verification works today) and confirmed the exact same student/parent pair could then successfully complete consent — proving the fix blocks the attack without breaking the legitimate path.

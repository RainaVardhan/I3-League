# CLAUDE.md — i3League Build Notes

This file orients any Claude Code session working in this repo. Read this before making architectural decisions or adding new features — it exists so context isn't lost between sessions.

## What this project is

i3League is a year-round student innovation platform (middle/high school). Students move through a 6-stage journey, submitting work at each stage, and can qualify for a national competition. Full product spec lives in the project's source documents — this file is the *build* context, not the product spec. When in doubt about product behavior, defer to the spec; when in doubt about *how* to build something, follow this file.

**The person building this is solo and non-technical.** They are directing Claude Code to write all the code. Prioritize working, well-commented, conventional code over clever code. Avoid unexplained abstractions. When you make a nontrivial architectural choice, leave a comment saying why.

## Timeline context

This is being built as a 2-week MVP, not the full spec. If a task looks like it belongs to a feature listed under "Deferred (Phase 2)" below, don't build it — note it and move on. Scope creep is the biggest risk to this timeline.

## Tech stack (do not deviate without discussion)

| Layer | Choice |
|---|---|
| Framework | Next.js (App Router) |
| Database | Postgres via Supabase (or Neon) |
| ORM | Prisma |
| Auth | Supabase Auth — role-based: `student`, `parent`, `coach`, `judge`, `admin` |
| File storage | Cloudflare R2 (deferred setup — use local/mock storage until Cloudflare step begins) |
| Deployment target | Cloudflare Workers via `@opennextjs/cloudflare` (deferred — build and run locally for now, do not spend time on Cloudflare config yet) |
| Payments | **Manual** — PayPal / Venmo / Zelle. No Stripe, no payment processor API. See "Payment flow & pricing model" below. |
| Error monitoring | Sentry (add once core flows work, not blocking for early days) |
| Version control | GitHub — this repo |

**Local dev for now:** the app should run fully on `localhost` against a cloud Supabase/Neon Postgres instance. Do not wire up Cloudflare deployment until explicitly asked — it's a later step, not a Day 1 blocker.

## Payment flow & pricing model (important — per-participant, not per-team)

**Pricing is per participant, not per project/team.** A team of N pays $111 × N at checkout, but underneath, billing and access are entirely individual:

| Participation | Registration fee |
|---|---|
| Individual | $111 |
| Team of 2 | $222 total ($111/student) |
| Team of 3 | $333 total ($111/student) |
| Team of 4 | $444 total ($111/student) |

Each student — regardless of team size — gets their own: account, curriculum access, assessments, Innovation Journal, speaking/character/ethics challenges, Innovator Profile, badges, and certificate. **The project can be shared across a team; the learning and certification never are.**

Data model implication: `Enrollment` and `Payment` are **per-student**, not per-team. `Team` is a lightweight grouping entity linking students to a shared `Project` — it has no billing or curriculum logic of its own.

No payment processor integration exists. The flow is:

1. Registration/checkout screen totals `$111 × participant count` and displays the org's PayPal.me link, Venmo handle, and Zelle info as static text (pulled from `Season` config, not hard-coded).
2. Each student/parent pays their individual share externally (or the team pays together and it's reconciled manually — don't build logic assuming a single team-wide transaction reference covers everyone; allow each student's confirmation to be entered separately even if the reference number is shared).
3. Each student submits a **payment confirmation form**: payment method, transaction reference/confirmation number, optional screenshot upload. This creates that student's `Enrollment` with `paymentStatus = SUBMITTED`.
4. An admin manually reviews and sets `paymentStatus = VERIFIED` **per student** — a teammate's unverified payment must never block or unlock another teammate's dashboard.
5. `paymentStatus` values: `PENDING`, `SUBMITTED`, `VERIFIED`, `REJECTED`, `REFUNDED`.

Do not build Stripe/payment-API integration unless explicitly asked to add automated PayPal payments later.

## Season-driven config — do not hard-code

Nothing about the 2026–2027 season should be hard-coded into templates or logic. All of the following live on a `Season` record and are read at render/runtime:

- Per-participant pricing (currently $111)
- Spring qualification deadline (April 30, 2027)
- Summer qualification deadline (July 30, 2027)
- National Finals date
- Curriculum version
- Team size limit (currently supports up to 4)

This is what lets a `2027–2028` season exist later without a rebuild.

## Core business rules (enforce server-side, not just in the UI)

- **Sequential stage unlocking**: INSIGHT → INVESTIGATE → IMAGINE → ITERATE → IMPACT → INFLUENCE. A student cannot access a stage via direct URL before completing the prior one. Admin can override/unlock manually.
- **Safety screening** (in INVESTIGATE): any high-risk answer (humans, animals, health info, chemicals, biological materials, electricity, machinery, PII, environmental sampling, drones, AI, other) sets the project to `ADMIN_REVIEW_REQUIRED` and blocks progression until an admin clears it.
- **Projects are confidential-only** (the IP Checkpoint gate that used to sit between IMAGINE and ITERATE was removed — see the Aug 25, 2026 status log entry). **Every project defaults to `CONFIDENTIAL` at creation** at the schema level, and there is currently no flow that ever sets it to `PUBLIC`. **Confidential projects must never appear in any public-facing query** — galleries, marketing, public judge views. Treat this as a hard security requirement, not a UI filter (filter at the query/API layer, not just hide it in the frontend; the database default itself is `CONFIDENTIAL`, so a code path that forgets to set visibility explicitly still fails safe).
- **Role-based access**: enforce at the database/API layer (Supabase RLS or equivalent), not just by hiding UI elements. A parent should never be able to fetch another family's data even via direct API call. A coach has read-only access to their roster — no write access to submissions, tests, or grades.
- **Team billing/access independence**: one teammate's unpaid or unverified status must never lock another teammate out of their own dashboard, curriculum, or certification track.
- **Innovation Journal is append-only**: edits create a new version; don't overwrite/delete history.
- **AI-use disclosure** is required on every stage's SUBMIT step (major submissions), not optional.
- **Project ownership is exactly one of individual or team**: a `Project` row must have exactly one of `individualStudentId` / `teamId` set, never both, never neither. Prisma can't express this as a native schema constraint, so it's enforced twice: (1) a zod refinement in application code runs before every `Project` creation and rejects a request with both or neither set, with a clear error message; (2) a hand-written raw SQL `CHECK` constraint on the `Project` table (`num_nonnulls("individualStudentId", "teamId") = 1`), added directly in the migration file that creates the table, as a fail-safe in case the app-layer check is ever bypassed or has a bug. Do not rely on the app-layer check alone — that was the whole point of catching this before schema lock. If a migration is ever reset/regenerated, re-add this constraint by hand; Prisma will not recreate it from `schema.prisma`.

## Data model — build all of these now, even Phase 2 ones

Building the full schema up front avoids migrations mid-build. Core entities (see product spec Section 40 for full field-level detail):

`User`, `Student`, `Parent`, `School`, `Team`, `Season`, `Enrollment`, `CurriculumModule`, `Lesson`, `Assignment`, `Submission`, `Assessment`, `Question`, `Attempt`, `Project`, `JournalEntry`, `SafetyReview`, `AIDisclosure`, `SpeakingSubmission`, `CharacterChallenge`, `EthicsChallenge`, `QualificationRecord`, `JudgeAssignment`, `JudgeScore`, `Certificate`, `Payment`, `Notification`, `MediaConsent`

Don't collapse these into one giant table (the spec explicitly calls this out). Remember: `Enrollment` and `Payment` key off `Student`, not `Team`. `Project.visibility` must default to `CONFIDENTIAL` at the schema level (e.g. Prisma `@default(CONFIDENTIAL)`), not just as an application-layer default — this ensures fail-safe behavior even if a code path creates a `Project` without explicitly setting visibility.

## Build sequence — 14-day sprint plan (2-day sprints)

Follow this order. Each sprint builds on the previous one's infrastructure — don't jump ahead even if a later feature looks quick, because most of them depend on auth, the stage template, or the payment/enrollment model landing first.

**Sprint 1 (Days 1–2):** Login & Authentication · Public Homepage · How It Works

**Sprint 2 (Days 3–4):** Curriculum Overview · Divisions & Eligibility · Pricing (per-participant table) · National Finals (static) · FAQs · Contact Us · Terms/Privacy/Competition Policies

**Sprint 3 (Days 5–6):** Participant Registration · Parent/Guardian Consent · Team Creation & Team Management · Payment Processing (manual) · Payment & Enrollment Records (per-student)

**Sprint 4 (Days 7–8):** Student Dashboard · Six-Stage Learning Path template · Stage Progress Tracking · Sequential Stage Unlocking · Online Learning Materials · Practice Activities · File & Media Uploads · build INSIGHT + INVESTIGATE fully

**Sprint 5 (Days 9–10) — heaviest sprint, budget slack here first if anything slips:** Project Submission Portal · Innovation Journal · Speaking Challenge Submissions · Character Challenges · Ethics Challenges · AI-Use Disclosure · Safety Screening · Team Contribution Tracking · build IMAGINE, ITERATE, IMPACT, INFLUENCE (the IP Checkpoint gate that used to sit between IMAGINE and ITERATE was removed from scope — see the Aug 25, 2026 status log entry)

**Sprint 6 (Days 11–12):** Online Assessments · Assessment Retakes · Deadline Tracking · Qualification Eligibility Tracker · April 30 / July 30 Qualification Windows · Admin Dashboard core · Submission Review · IP Review Workflow · Safety Review Workflow · User & Role Management · School Management · Season Management

**Sprint 7 (Days 13–14):** Parent Dashboard · Coach/Teacher Dashboard · Email Notifications (welcome/stage-complete/next-stage-unlocked) · Digital Badges · Certified Innovator Credential (basic, no QR yet) · Final Innovation Portfolio (auto-compiled) · Mobile-responsive pass · Accessibility pass · Privacy/consent audit · Search & Filtering · bug bash · deploy to staging

## Deferred (Phase 2) — schema-ready, UI not built yet

Don't build UI/workflow for these yet. The data model should already account for them so no migration is needed later:

- Judge Portal, Judge Assignment, Conflict-of-Interest Declaration, Judging Rubrics & Scoring
- Semifinalist/Finalist Management
- National Finals Management (dynamic, beyond the static info page)
- Public Innovation Gallery
- Winner & Award Pages
- National Qualification Submission (full package form)
- Innovator Competency Transcript (depends on judge-entered ratings)
- Certificate Verification (public QR/link verification page)
- Curriculum Management (admin authoring UI — V1 content is built directly, not admin-editable)
- Assessment Question Bank Management (admin authoring UI — V1 banks are seeded, not admin-editable)
- Reporting & Analytics
- Audit Logs UI (basic action logging still happens in the background)
- Help & Support beyond the FAQ/Contact Us pages
- Full notification automation (deadline countdowns, inactivity nudges)

## Working conventions

- Prefer explicit, readable code over abstraction for its own sake — this codebase needs to be maintainable by a non-technical owner working with AI assistance across many sessions.
- Comment non-obvious business logic (especially unlock rules, confidentiality filtering, per-student payment/enrollment logic, and team-vs-individual boundaries) — these are the places a future session is most likely to accidentally break something.
- When a decision in this file conflicts with something a person asks for in a session, flag the conflict rather than silently overriding this file.
- Update this file when a real architectural decision changes (e.g., "we switched from Neon to Supabase") so it stays a reliable source of truth.
- **No em dashes ("—") in any user-visible or generated content** (JSX copy, string literals that render, `title`/`aria-label`, metadata, seed content, docs for the user). Absolute rule, set by the owner Sep 8 2026 (an earlier "code comments only" carve-out was rejected). Rewrite the sentence instead: period + two sentences, semicolon, colon, "and"/"but", parentheses, or a comma pair. En dashes in numeric ranges and `→` arrows are fine.

## Stage page standard (Insight is the reference; set by the owner Sep 21 2026)

**`/dashboard/insight` and the `/dashboard` hub are the template for every other stage (Investigate, Imagine, Iterate, Impact, Influence).** When you build or rework a stage, copy Insight's design and structure; don't invent a new layout for it. If a stage's curriculum really doesn't fit the pattern, flag that to the owner rather than quietly departing from it. Visual specs are in `docs/design-system.md` Section 22. This section is the build checklist.

**Content comes from the curriculum docs, word for word.** Use `docs/curriculum/` (01_LEARN notes, 02_DO activities, 03_SHOW requirements, 04_REVIEW rubric, 05_BUILD worksheet pack and submission reviewer) for that stage. Every field, required flag, and list item should trace back to a line in those docs. Don't paraphrase or add fields the docs don't have, and remove em dashes (per the rule above).

**Page structure** (all under `AppShell` → `StageLayout`):
1. `StageHero` (SplitHero shell and stat box), showing the stage's `guidingQuestion` when it has one.
2. The navy page bar and the sidebar section list, both from `StageSections`. The open page is kept in `?page=`.
3. One page per curriculum topic, built with `TaskSection` in alternating bands: Learn (`LessonPanel`, a connected plane with numbered rows and a highlighted definition), Do (`ActivityPanel`: activity plus Worksheet/Output boxes), Show (a `Must include` heading plus one column of fields). A topic can leave out Do or Show, e.g. Insight's Consent page has no Show. Every panel stays mounted and is switched with `hidden`.
4. A Back / autosave status / Next row. Band tones alternate all the way down (set `pagerTone`), and every neighbouring pair has to be re-checked.
5. A Review page (`StageReviewPage`) with these bands in order: Your answers (`SubmissionAnswers`, mirroring the live form) → How it's reviewed → Full rubric (`StageRubricDetail`) → Submit band (AI-use disclosure in a coral box, closing reflection, reassurance). The Submit button sits in the pager row as `aria-disabled` and opens `MissingItemsDialog` until everything is done. Nothing on Review repeats another page.
6. Once the stage is submitted, the pages are read-only recaps, Review has no Submit band, and the closing reflection is its own final band.

**Per-stage files a new stage needs** (named after Insight's):
- `src/lib/stage-copy.ts`: fill the stage's `StageCopy` in full (`guidingQuestion`, `submissionChecklist`, `reviewerChecks`, `advancesWhen`, `revisesWhen`, `detailedRubric`, `finalSubmission`), plus a `<STAGE>_SECTION_DETAILS` list (Do detail and Must include per topic, **in page order**).
- `src/lib/<stage>-requirements.ts`: **the one list** of required and optional fields, one entry per form field. The hub's "Finish {Stage}" checklist (`getStageChecklist`), the Submit popup, and the server's final-submit check all read from it, so they can't drift apart. Checklist rows deep-link to the exact field (`?page=…#field`).
- `src/lib/stage-field-limits.ts`: a `TEXT_LIMITS` entry for every new text field. The server refuses over-long input rather than cutting it off, and each input sets a matching `maxLength`.
- `src/app/dashboard/[stage]/<Stage>Form.tsx`: one `<form>` across every page, with autosave (a `persist<Stage>()` shared by `save<Stage>Action` and `autosave<Stage>Action`; saves 900ms after typing stops and on blur; no Save draft button; AI disclosure written only at final submit). Server actions re-check the student, `requireCurrentStage`, the category allowlist, and the length limits.
- `summary-sections.ts`: `build<Stage>Sections` / `<stage>AnswersFromSaved` for the Review answers box.
- `prisma/seed.ts`: the stage's real Lessons and Assignments. A live DB needs its old rows for that module deleted first, because the seed skips a module that already has rows. Lesson/Assignment rows follow the docs' order, while section details follow page order, so pair them by index in `page.tsx` (see the Insight page-order gotcha in the status log).

**Hub (`/dashboard`)**: the hero, deadline band, current-stage node track, "What's left" (`StageProgressPanel`), and next-action sections already work for any stage. A new stage only needs its `getStageChecklist` branch (built from its requirements file) and its copy.

**Imagine, Iterate, Impact and Influence are built on one shared guided-stage engine (Sep 22 2026)**, not four more copies of `InvestigateForm`: `src/lib/guided-stage.ts` (types + helpers), one config file per stage under `src/lib/stages/` (`imagine.ts`, `iterate.ts`, `impact.ts`, `influence.ts`: pages, lessonIndex, activity, Must include, reviewer checks, show notes, field groups, `hsOnly`, `optionalTag`), `src/lib/stages/activities.ts` and `rubrics.ts` and `prisma/stage-content-3-6.ts` (all GENERATED from `docs/curriculum` by a one-off script; regenerate rather than hand-edit), `GuidedStageForm.tsx` + `guided-parts.tsx` (shared form and Learn/Do bands), and `persistGuided` / `saveGuidedStageAction(stageName, ...)` in `actions.ts`. The hub checklist, Submit popup, Review answers box and server check all derive from the same config, so for these stages there is no separate `<stage>-requirements.ts` or `TEXT_LIMITS` entries: each field's `limit` lives on the field (default 2000, 300 for short/choice). Grade rules: an HS CORE page is shown to high school only; a "when relevant" / "where applicable" / STRETCH page is shown (kicker carries the tag) but never gates Submit. A page with no fields (Iterate's Usability, Task Tracking) is Learn + Do only. To change a page or field on these stages, edit the config file only.

**Investigate follows the standard as of Sep 21 2026** (see the status log). Its two deliberate differences from Insight: the safety screening page (its fields join a separate `<form>` through the `form` attribute, since it has its own action and admin gate), and pages that group several lessons or activities (`PartStack`), with one page (Stakeholders & Gap) that has no LEARN note and opens on its Do band. Reuse these patterns rather than reinventing them.

## Status log

Keep a running note here of what's actually done vs. planned, so a new session doesn't have to guess.

- **[Aug 10, 2026]** Repo created. Stack decided. Pricing model corrected to per-participant ($111/student; team of N = $111×N, individual billing/access per student). Full 70-item feature backlog sequenced into 7 two-day sprints. Project visibility rule locked: defaults to `CONFIDENTIAL`, only becomes `PUBLIC` on explicit student request at/after IP Checkpoint. Prisma schema not yet drafted. No code written yet.
- **[Aug 13, 2026]** Prisma schema drafted and finalized (`schema.prisma`, 30+ models). Key decisions locked during review:
  - `StudentProject` join table added — links every student to every project they belong to (one row per student per project), independent of `Team`/`TeamMembership`. This is the correct path for "all of this student's projects," including confidentiality checks, rather than traversing through `Team`.
  - When a student leaves a team mid-project, their `StudentProject` row is **never deleted** — `isActiveMember` is set to `false` instead. Past contributions stay permanently on a student's record (Innovator Profile, journey history) even after they leave. `isActiveMember = false` only gates future edit access, not visibility of past work.
  - `Submission.content` and the `SafetyReview`/`IPReview` `answers` fields are JSON, validated in application code (zod per stage), not typed columns — a deliberate speed tradeoff for a solo 2-week build; revisit if the team grows.
  - `Enrollment`/`Payment` remain hard-keyed to `Student` (never `Team`), with `@@unique([studentId, seasonId])` so a team can't accidentally share a payment record.
  - `Project.visibility` defaults to `CONFIDENTIAL` at the Prisma level, with a separate `visibilitySetByStudent` boolean so the database can prove a project only went public because a student explicitly asked.
  - No update path modeled on `JournalEntry` — edits are new rows sharing `entryGroupId` with an incremented `version`. Must not expose PATCH/PUT on this model at the API layer.
  - **Note:** `i3League_2Week_Build_Plan.md` (the original 2–4 person team, Stripe-based, $149 flat-fee draft) is now superseded by `i3League_Sprint_Plan.md` (solo build, manual PayPal/Venmo/Zelle, $111/participant) and this file. Treat the 2-week plan doc as historical only — don't pull scope or payment details from it.
  - Next up: seed data / migration commands, or start Sprint 1 (auth + public site shell).
- **[Aug 13, 2026, later]** Resolved the "exactly one of individualStudent/team" open item on `Project`: enforced at both the app layer (zod refinement before every `Project.create`) and the DB layer (hand-written raw SQL `CHECK` constraint — `num_nonnulls("individualStudentId", "teamId") = 1` — added to the migration that creates the `Project` table). Same fail-safe philosophy as the `visibility` default. See "Core business rules" above and the `Project` model comment in `schema.prisma`. This constraint must be manually re-added if a migration is ever reset, since Prisma doesn't generate it from `schema.prisma`.
- **[Aug 13, 2026, later still]** Repo initialized: scaffolded with `create-next-app` (App Router, TypeScript, ESLint, `src/` dir, no Tailwind). Installed `prisma`, `@prisma/client`, `ts-node`. Ran `npx prisma init` to generate `prisma/schema.prisma` placeholder and `.env`, then replaced the schema with the finalized version and added `prisma/seed.ts`. `package.json` `prisma.seed` entry added. **Not yet done:** no `DATABASE_URL` configured, so `prisma migrate dev` has not been run — the DB has no tables yet, and the `project_exactly_one_owner_check` CHECK constraint has not been added to any migration. Next session needs a Supabase/Neon connection string before continuing to migration + seed.
  - Two bugs found and fixed while validating the schema: (1) `Project.individualStudent` had no opposite relation field on `Student` — added `individualProject Project?` (singular, since `Project.individualStudentId` is `@unique`) to `Student` in `schema.prisma`. (2) this Prisma version (6.19) generates a `prisma.config.ts` on `prisma init` which is now authoritative over `package.json#prisma.seed` — added a matching `migrations.seed` entry to `prisma.config.ts` so `prisma db seed` actually finds `prisma/seed.ts`; kept the `package.json` entry too since it's harmless (just ignored, with a warning).
  - Also set `turbopack.root` in `next.config.ts` to silence a spurious "ignored package-lock.json" warning caused by an unrelated stray lockfile in the parent home directory — not a project issue, just workspace-root ambiguity for Turbopack.
- **[Aug 13, 2026, even later]** Built the "Blueprint" design system while waiting on a `DATABASE_URL`, using the marketing "coming soon" page as the visual reference: dark navy/grid background, Space Grotesk (display) + Inter (body) + JetBrains Mono (labels/mono), ignition-orange + spark-teal accent pair. This is a committed brand look, not a light/dark user preference — no `prefers-color-scheme` switch.
  - Tokens live in `src/app/globals.css` (`:root` custom properties for color/type/spacing/radius). Fonts are loaded via `next/font/google` in `src/app/layout.tsx`, with the font CSS variables (`--font-display` / `--font-body` / `--font-mono`) doubling as the type tokens — no separate font-name mapping layer.
  - Reusable components under `src/components/design-system/`: `GridBackground`, `Eyebrow` (pill/label variants), `Button` (primary/secondary, polymorphic via `as`), `Panel`, `Cube` (the rotating hero visual, parameterized by `size`/`faces` so it's reusable, not hardcoded to the teaser copy), and `StepGrid` (the dashed-divider 3-column "How It Works" layout pattern — color-cycles paper/accent/accent-2 for exactly 3 steps, matching the source page).
  - `src/app/design-system` is a style-guide page showing every token/component together — not a real app route, just a reference. The actual public homepage (nav, hero, stage overview, etc.) has **not** been built yet; scope for this session was tokens + components only. `src/app/page.tsx` is still the default Next.js starter page.
  - Verified via `tsc --noEmit`, `eslint`, and an actual `next dev` run (fetched `/design-system`, HTTP 200, no console/server errors).
- **[Aug 14, 2026]** The dark "Blueprint" design system above was superseded same-day by a canonical, more complete spec the user handed over directly: **`docs/design-system.md`** ("I³ League — Canonical Design System v1"), sourced from an already-approved reference homepage (`i3-homepage-v20-lower-3-logo.html`). This is now the single source of truth for visual design — treat the "Design tokens"/"Reusable components" notes in the entry above as historical only.
  - New direction: **light** "innovation-lab / competition editorial" look — cool mineral canvas (`#E6ECF5`), warm off-white paper (`#FFFDF8`), ink-navy text/authority color (`#10213D`), collegiate cobalt (`#3158D8`) for progress/navigation, coral (`#E35E49`) reserved for action + the IP Checkpoint only. Depth comes from hard offset shadows/planes, never glow or gradients — see doc Section 19 for explicit anti-patterns to avoid.
  - Fonts swapped: Chakra Petch (display), Hanken Grotesk (body), IBM Plex Mono (mono/metadata) — replacing Space Grotesk/Inter/JetBrains Mono. Loaded via `next/font/google` in `src/app/layout.tsx`; export names are `Chakra_Petch`, `Hanken_Grotesk`, `IBM_Plex_Mono`.
  - `src/app/globals.css` rewritten with the full v1 token set (color/type-scale/spacing/radius/container/header-height), including responsive `--header-h`/`--gutter` overrides at the doc's canonical breakpoints (900px/820px).
  - All prior dark-theme components under `src/components/design-system/` were deleted and rebuilt from the new spec: `Logo` (Section 4 — square ink mark, soft-blue "I", coral superscript "3", LEAGUE wordmark), `Button` (Section 9 — primary/ghost, hard bottom-shadow lift, trailing chevron), `Cube` (Section 11 — the six-faced journey visual, canonical face tones/edges, slow idle yaw with `prefers-reduced-motion` fallback), `Eyebrow` (two mono label sizes), `Panel` (standard/selected variants per Section 17), `GridBackground` (Section 8's faint 72px drafting grid).
  - **Deliberately not built yet:** the homepage's full scroll-jacking cube choreography (Section 11's stage-by-stage scroll sequence, journey rail, journey masthead/watermark/progress bar) and the editorial page sections (proof grid, field band, final CTA, footer) from the reference HTML. Those are homepage-construction work, not design-system primitives — matches the earlier "design system only" scope decision. The static `Cube` component is ready to be wired into that hero once homepage building starts.
  - Verified via `tsc --noEmit`, `eslint`, and an actual `next dev` run (fetched `/design-system`, HTTP 200, correct fonts + all component content present, no console/server errors).
- **[Aug 15, 2026]** First migration run against a live Supabase Postgres instance — the schema is no longer just a file, it's a real database.
  - Created the Supabase project. In the project's Security settings (Data API section), deliberately left **Data API disabled** and **"Automatically expose new tables" disabled** — the app talks to Postgres via Prisma from server-side code only, never via `supabase-js`/PostgREST, so there's no reason to expose tables over REST at all. **"Enable automatic RLS" is on**, so every table Prisma creates gets Row Level Security turned on automatically (verified: all 41 app tables show `relrowsecurity = true`), even though Prisma itself has no concept of RLS and would never set this. No per-role policies exist yet — that's correct for now, not an oversight: real policies need `auth.uid()` mapped to `User.supabaseUid` → role, which requires Supabase Auth (Sprint 1, not built yet). RLS-enabled-with-no-policies is fail-closed for the Data API path in the meantime; it's not what protects the app itself, since Prisma connects as the `postgres` role, which bypasses RLS by design — the app-layer role checks (per CLAUDE.md "Role-based access") are still the real enforcement once API routes exist.
  - `.env` now holds real credentials: `DATABASE_URL` (pooled, port 6543, PgBouncer, used at app runtime) and `DIRECT_URL` (unpooled, port 5432, used for migrations — PgBouncer transaction mode can't run the DDL/shadow-db operations `prisma migrate` needs, confirmed by a hung `migrate dev` before this was fixed). Both `schema.prisma`'s `datasource` block and `prisma.config.ts`'s `datasource` block needed `directUrl` added — Prisma reads both, and the config file's datasource takes precedence over the schema file's when both exist.
  - Ran `prisma migrate dev --create-only` first specifically so the `project_exactly_one_owner_check` CHECK constraint (see "Core business rules" above) could be hand-inserted into the generated `migration.sql` — right after `CREATE TABLE "Project"` — before applying. Applied and confirmed live via `pg_get_constraintdef`. This is the pattern to repeat for any future migration that touches `Project`: always `--create-only` first, never let `migrate dev` apply directly, so this constraint doesn't get silently dropped by a regenerated migration.
  - 42 tables now exist in `public` (41 app models + Prisma's own `_prisma_migrations`).
  - Next up: Sprint 1 — Supabase Auth wiring (login/session, mapping `auth.uid()` to `User.supabaseUid`/`Role`), then real RLS policies once that identity model exists, then the public homepage.
- **[Aug 15, 2026, later]** Correction to the note directly above: the public homepage referenced in the Aug 14 design-system entry ("the actual public homepage... has not been built yet") **was actually built** at some point between that entry and now — `src/app/page.tsx` and a full `src/components/homepage/` set (`Header`, `JourneyHero`, `CurriculumBridge`, `ProblemSection`, `ProofSection`, `FinalCta`, `Footer`, `Checkerboard`) already exist and are wired up. That work happened without a matching status log entry (visible only via `git log`, commit "I3League Prisma schema and homepage"). Flagging this here so a future session doesn't waste time rebuilding it or get confused by the stale note above — treat the Aug 14 entry's "not built yet" line as outdated.
- **[Aug 15, 2026, even later]** Sprint 1 — Supabase Auth (login/session, `auth.uid()` → `User`/`Role` mapping) built and verified end-to-end against the live Supabase project.
  - Locked decisions: Student/Parent/Coach self-serve via a public signup form with a role selector; Judge/Admin are never self-serve (enforced server-side, not just by the UI omitting them as options — see below). Email confirmation is required before login. `auth.users` → `public.User` sync happens via a hand-written Postgres trigger, not application code, for the same fail-safe reason as the `project_exactly_one_owner_check` constraint: it can't be bypassed by a bug in a Server Action.
  - Installed `@supabase/supabase-js` + `@supabase/ssr` — had to bump both to matching versions (`^2.112.3` / `^0.12.4`) since the versions npm resolved by default didn't satisfy `@supabase/ssr`'s peer requirement.
  - New env vars in `.env`: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` (server-only, unused this sprint — reserved for a later admin-created-account flow for Judge/Admin).
  - New `src/lib/prisma.ts` — the Prisma client singleton this repo never had (only `prisma/seed.ts` did a bare `new PrismaClient()`). Without it, every dev hot-reload opens a fresh pool of connections against the pooled `DATABASE_URL` and eventually exhausts Supabase's PgBouncer limit.
  - New `src/lib/supabase/{client,server,middleware}.ts` following the standard `@supabase/ssr` App Router pattern (browser client, async server client using `next/headers` cookies, and a request/response-bound variant for the proxy). Server-side code always calls `getUser()`, never `getSession()` — the latter trusts the cookie without revalidating against Supabase and is spoofable in server contexts.
  - **`src/proxy.ts`, not `middleware.ts`**: Next.js 16.3.1 (the version this repo is on) deprecated the `middleware.ts` file convention in favor of `proxy.ts` (rename the file, rename the export from `middleware` to `proxy`; the `config`/`matcher` export is unchanged). Discovered this via a real warning in the dev server log during testing, not by guessing — confirmed via Next's own migration doc. If a future session copies a `@supabase/ssr` tutorial that still says `middleware.ts`, rename it to `proxy.ts` on the way in.
  - Trigger migration: `prisma migrate dev --create-only --name add_auth_user_sync_trigger` produced an **empty** migration file (no `schema.prisma` diff exists to scaffold from, since the trigger lives entirely outside Prisma's model — this is expected, not an error). Hand-wrote the trigger SQL into it (function `public.handle_new_auth_user()`, `SECURITY DEFINER`, `search_path = ''` to prevent search-path hijacking, reads `role` from `raw_user_meta_data`, generates the `User.id` itself via `gen_random_uuid()` since Prisma's `@default(uuid())` is client-side only and never reaches the migration SQL). **Applying it required `prisma migrate deploy`, not `prisma migrate dev`** — `migrate dev` diffs against a temporary shadow database that doesn't have Supabase's `auth` schema, so it fails with `schema "auth" does not exist`. `migrate deploy` applies directly with no shadow db and is the correct tool any time a migration references `auth.*`. Same rule as the CHECK constraint: review the hand-written SQL before applying, never let it auto-apply.
  - Built `/signup` (role radios limited to Student/Parent/Coach — Server Action independently re-validates this allowlist so a crafted `role=ADMIN` POST is rejected even with the UI bypassed entirely, confirmed by testing exactly that), `/login`, `/auth/callback` (Route Handler, exchanges the confirmation code for a session), `/auth/auth-code-error`, `/signup/check-email`, and a minimal `/dashboard` proving the full identity chain resolves (`getUser()` → `prisma.user.findUnique({ where: { supabaseUid } })` → renders email + role). Four of these share one new `src/components/auth/AuthCard.tsx` (extracted after noticing the same centered-card layout would otherwise be copy-pasted four times — real duplication, not premature abstraction).
  - New design-system primitive: `src/components/design-system/Input.tsx` (+ `Input.module.css`), built to the already-written but previously unimplemented Section 17 "Form inputs" spec in `docs/design-system.md`.
  - `Student`/`Parent`/`Coach`/`Judge`/`Admin` profile rows are **not** created at signup — they're already-optional 1:1 relations on `User` in the schema, so "signed up but hasn't completed registration" is just `user.role === "STUDENT" && user.student === null`, no schema change needed. `/dashboard` shows a "Registration incomplete" notice in that state. Sprint 3 (Participant Registration) is what actually creates that profile row.
  - **Verified end-to-end against the live Supabase project**, not just locally reasoned about: real `curl` POSTs replaying the exact multipart form + Next.js Server Action hidden fields a browser sends (confirmed this works because these forms use real progressive-enhancement `<form action={...}>` submissions, not fetch-only RSC calls) — proved the login wrong-password path renders the generic error inline with no crash, and that `role=ADMIN` is rejected server-side. Used the Supabase Admin API (service-role key, with `node --experimental-websocket` — see Node/WebSocket note below) to create real `auth.users` rows for STUDENT/PARENT/COACH and confirmed the trigger produced correct `public.User` rows for all three, then fetched `/dashboard` with a real forged session cookie built from a real access/refresh token pair and confirmed it rendered the correct email/role/registration-incomplete notice/logout button. All test accounts cleaned up afterward (verified via `listUsers`, zero left over).
  - Two things worth knowing but **not bugs**: (1) Supabase's built-in email provider is rate-limited (a handful of sends/hour) and also rejects `@example.com`-style addresses as invalid — hit both while testing; neither is an app bug, just constraints of the shared dev mailer. For repeated local testing, prefer the Admin API (`email_confirm: true`) over triggering real signup emails. (2) `@supabase/supabase-js` warns on Node ≤20 (this machine runs 20.18.0) because its Realtime client needs a global `WebSocket`, which Node 20 doesn't have by default — this **did** crash a raw standalone Node test script, but did **not** affect the actual Next.js app in any of the live tests above (Next's dev server process has a WebSocket global available some other way, likely via its own HMR setup). Not a blocker; worth revisiting if a real crash ever surfaces in the app itself, and resolved by upgrading to Node 22+ eventually.
  - RLS still has zero policies (per the Aug 15 entry above) — now that `auth.uid()` ↔ `User.supabaseUid`/`role` actually resolves, writing real per-role RLS policies is unblocked but still deliberately not done this sprint. The Data API is also still off, so this isn't reachable externally yet regardless.
  - Next up: real RLS policies now that the identity model exists (optional — Prisma still connects as `postgres` and bypasses RLS regardless, so this is defense in depth, not blocking), then continuing the Sprint 1 build sequence.
- **[Aug 16, 2026]** **Sprint 1 confirmed complete.** All three items are built and committed: Login & Authentication (Aug 15 entry above), Public Homepage (`src/app/page.tsx` + `src/components/homepage/`, per the Aug 15 correction entry), and How It Works (`src/app/how-it-works`, commit `ca35a4a` — also extracted `NavLink` as a shared component so Header/Footer stop keeping separate copies of the nav list). Starting Sprint 2 next: Curriculum Overview, Divisions & Eligibility, Pricing, National Finals (static), FAQs, Contact Us, Terms/Privacy/Competition Policies.
- **[Aug 16, 2026, later]** **Scope change from the client: no standalone Divisions & Eligibility page.** A full Eligibility page (`/eligibility`) was built this session — editorial hero with a stat "snapshot" card, a "who it's for" three-up grid, an on-ink "what the program asks of you" panel, and a "joining vs. qualifying" comparison with real Season deadlines — then the client decided against a separate page for this content. Rather than deleting the content, it was redistributed into the pages it fits best contentwise, and `/eligibility` (route + `src/components/eligibility/`) was removed along with its nav entry in `src/lib/site-nav.ts`:
  - **How It Works page** (`src/app/how-it-works/page.tsx`) gained `DistinctionSection` (`src/components/how-it-works/DistinctionSection.tsx`) — the "joining is not the same as qualifying" participation-vs-qualification comparison, with real `springQualifyDeadline`/`summerQualifyDeadline` from Season — placed after the 8-step path, since it's the natural follow-up question once a reader understands the steps. This required converting the page component to `async` to fetch Season (it wasn't before).
  - **Pricing page** (`src/app/pricing/page.tsx`) gained a new "Who can register" `InfoBand` (grade range, team size from Season, choose-your-own-problem) placed before the pricing table, since "can I register" is the natural question right before looking at price.
  - Divisions & eligibility content is therefore **done**, just not as its own page — don't recreate `/eligibility` or re-add it to `SITE_NAV_LINKS` without checking with the client first, and don't duplicate this content again elsewhere without first checking whether Curriculum/How It Works/Pricing already cover it.
  - **[Same day, later still]** `WhoItsFor` (the "who it's for" three-up grid — grade divisions, solo/team, choose-your-own-problem) briefly landed on the Curriculum page per the note above, then moved again to `src/components/how-it-works/WhoItsFor.tsx`, rendered on How It Works right after the hero and before the 8-step path — same "is this for me before what are the steps" reasoning, just a different page. Now that `WhoItsFor` and `DistinctionSection` both live on How It Works, their copy was trimmed to remove overlap: `WhoItsFor` no longer mentions "judged separately at Nationals" or the six-stage framework (both now owned by `DistinctionSection`'s cards below it), and `DistinctionSection`'s Participation card no longer restates "middle and high school students working independently or with a team" (already covered by `WhoItsFor` above it) — it now just describes what changes once you've registered. Curriculum page is back to just Hero → StageDetailList → StageWorkflow.
- **[Aug 16, 2026, whole-site audit]** Ran a full-repo audit (security/business-rules, code quality/stale code, design/UX) across three parallel review passes, then fixed what came back. Findings and outcomes:
  - **Critical, fixed and live-verified: auth role-escalation hole.** The Postgres trigger that syncs `auth.users` → `public.User` (see the Aug 15 Sprint 1 entry) cast `raw_user_meta_data->>'role'` straight to the `Role` enum with zero allowlist. `raw_user_meta_data` is client-settable at signup via Supabase's public GoTrue REST endpoint (`POST {SUPABASE_URL}/auth/v1/signup`), reachable with just the public anon key — so a request straight to that endpoint, bypassing the Next.js `/signup` form and its allowlist in `src/app/signup/actions.ts` entirely, could set `role: "ADMIN"` and the old trigger would honor it. This is exactly what "Judge/Admin are never self-serve... enforced server-side, not just by the UI" (Aug 15 entry) was supposed to prevent, and wasn't — the trigger, not the Server Action, is the real server-side boundary here, since it fires regardless of which client hit `auth.users`.
    - Fixed in two migrations: `20260816120000_harden_auth_role_trigger` (first pass — allowlisted `user_metadata` to STUDENT/PARENT/COACH, and added a second branch trusting `app_metadata` for a hypothetical future ADMIN/JUDGE admin-creation flow), then `20260816130000_simplify_auth_role_trigger` (correction — see below).
    - **Live-testing the first fix surfaced a second bug in the fix itself**: the `app_metadata` branch never fires. Verified directly against the real Supabase Admin API (`POST /auth/v1/admin/users`) that GoTrue writes `raw_user_meta_data` atomically on the `auth.users` INSERT, but applies a request's custom `app_metadata` via a separate UPDATE afterward — so this `AFTER INSERT` trigger never sees it. Confirmed via a live test that passed both `app_metadata.role=ADMIN` and `user_metadata.role=STUDENT` together: the resulting row landed as `STUDENT`, not `ADMIN`, proving the `app_metadata` branch was silently dead (fail-safe, not a security hole — it never granted anything, just didn't do what its comment claimed). Corrected in `20260816130000_simplify_auth_role_trigger`, which drops that branch entirely — the trigger now only ever assigns STUDENT/PARENT/COACH from `user_metadata`, rejects everything else (NULL role → NOT NULL violation → whole `auth.users` insert rolls back, same fail-safe pattern as before). **Admin/Judge account provisioning is still unbuilt (Phase 2, per "Deferred" section) and will need its own mechanism when that sprint starts** — don't reintroduce an `app_metadata`-at-insert-time approach without first confirming GoTrue actually populates it at INSERT time for whatever call is used (it doesn't for `admin.createUser`).
    - Both migrations applied live via `prisma migrate deploy` (required, not `migrate dev` — same reason as the Aug 15 trigger migration: no `auth` schema in the shadow DB) and verified end-to-end against the real Supabase Admin API: `role=ADMIN`/`JUDGE`/no-role-at-all via `user_metadata` all correctly rejected (500, no `public.User` row created, no orphaned `auth.users` row either since the whole transaction rolls back); `role=STUDENT`/`PARENT`/`COACH` all still correctly succeed. Test rows cleaned up via the Admin API afterward, zero left over — same rigor as the Aug 15 Sprint 1 verification.
    - Also confirmed: Supabase's project already has server-side Turnstile captcha enforcement on the public signup endpoint (rejects any request without a valid `captcha_token`, independent of this trigger) — a real curl/fetch straight to `/auth/v1/signup` is blocked by that first. This doesn't reduce the trigger fix's value (defense in depth — captcha config could change, and the trigger is now correct regardless), but it's why direct testing had to go through the Admin API (service-role key, bypasses captcha by design) instead.
  - **Fixed:** `src/components/how-it-works/steps.ts` hardcoded "April 30"/"July 30" as static copy in the "Qualify" step, even though `DistinctionSection` on the same page correctly pulls those dates live from `Season` — violated "Season-driven config — do not hard-code." `steps.ts` now exports `buildSteps(springDeadline, summerDeadline)`; `ProcessSection` takes the dates as props from `src/app/how-it-works/page.tsx`'s `getActiveSeason()` call, same pattern `DistinctionSection` already used.
  - **Fixed:** decorative coral misuse — `ProofSection`, `WhoItsFor`, and `ThreePhases` all recolored the middle item of their 3-up grids' underline accent coral purely for visual variety, with no action/IP relationship. Per `docs/design-system.md`, coral is reserved for actions + the IP Checkpoint specifically. Removed the overrides (and the now-pointless `itemAccent` conditional class in the two `.tsx` files) — all three underlines are uniformly cobalt now.
  - **Fixed:** pricing page skipped a heading level (h1 → h3, no h2 anywhere on the page) — `InfoBand`'s title is now an `h2` (it was the only heading-bearing shared component on that page, so this was safe to change without checking other call sites — it currently has none besides pricing).
  - **Fixed:** `FinalCta`'s button used a hardcoded `#8290a6` box-shadow with no matching token — swapped for `var(--dim)` (`#8d98a7`), the token it was clearly approximating.
  - **Confirmed correct, left alone:** `Project.visibility` default, the `project_exactly_one_owner_check` CHECK constraint, `Enrollment`/`Payment` per-student keys, the Aug 15 trigger's `SECURITY DEFINER`/`search_path = ''` hardening, no `getSession()` usage anywhere, open-redirect protection on `/auth/callback`, the password-recovery cookie flow, `tsc`/`eslint` both clean.
  - **Deliberately not touched:** `src/components/legal/LegalLayout.tsx` + `src/lib/legal-nav.ts` (unwired — no Terms/Privacy route exists yet) and `src/components/marketing/FaqAccordion.tsx` (unwired — no FAQ route yet) are pre-built-ahead-of-schedule for Sprint 2, not stale code — don't delete them, just wire them up when Sprint 2's Terms/Privacy/FAQ pages get built. A larger refactor consolidating `PageHero`/`CurriculumHero`/`PageIntro` into one shared hero primitive (they're structurally near-identical) was flagged by the audit but not done — bigger-surface-area change than the rest of this pass, worth doing but deliberately deferred rather than rushed in.
  - Also noted but not acted on (lower-value, broad, low-risk-of-actual-bug): scattered raw hex/px values across several `.module.css` files duplicate existing tokens (e.g. several exact-value duplicates of `--text-body`/`--text-nav`/`--text-eyebrow`, and 10+ near-identical grays that could collapse to `--muted`/`--dim`) — token-discipline drift, not a visual bug today, worth a dedicated cleanup pass later rather than a scattershot edit across a dozen files in this one.
- **[Aug 17, 2026]** **Sprint 2 confirmed complete.** Built the four remaining static/content pages — National Finals, FAQs, Contact Us, and Terms/Privacy/Competition Policies — finishing the Sprint 2 list (Curriculum Overview, Divisions & Eligibility, and Pricing were already done per the Aug 16 entries above).
  - **`/national-finals`** (`src/components/national-finals/`): built as a standalone page — `NationalFinalsHero` (SplitHero shell, stat box showing the real `Season.nationalFinalsDate`), `FinalsOverview` (Pattern C, deliberately high-level — judging rubrics/scoring and the Judge Portal itself are Phase 2, so this describes what a visitor sees at the event, not judging mechanics), and `QualificationPath` (wires up `InfoBand`/Pattern B, which existed unused since the Pricing page's "who can register" band was removed — see Aug 16 entry). **Superseded same day — see the Aug 17 "later" entry below: the site owner asked for this folded into How It Works instead and the standalone page removed.** `QualificationPath`'s InfoBand usage didn't survive the fold-in (see below); `FinalsOverview`'s three-card content did, moved into How It Works.
  - **`/faqs`**: `src/components/faq/faq-data.ts` exports `buildFaqSections(...)`, same `build*()`-takes-Season-values pattern as `how-it-works/steps.ts`'s `buildSteps` — every answer that states a price, deadline, or team size reads it from `Season`, never hard-coded copy. `FaqSections` renders four grouped categories (Registration & Pricing, Teams & Projects, Curriculum & Progress, Qualification & Finals) through the existing `FaqAccordion` (previously built but unwired, per the Aug 16 audit's "deliberately not touched" note).
  - **`/contact`**: `ContactChannels` — three static `mailto:` panels (general/registration/partnerships). No working contact form: there's no `ContactSubmission` model in the schema and no email-sending service configured (see CLAUDE.md tech stack), so a real form would need infrastructure this MVP doesn't have yet. Static mailto links match this page's Sprint 2 scope; revisit if a real inbound-message flow gets prioritized later.
  - **`/terms`, `/privacy`, `/competition-policies`**: real placeholder content wired into the existing (previously unwired) `LegalLayout`/`legal-nav.ts`, keeping `LegalLayout`'s "Draft — pending legal review" notice intact per its own comment (i3League deals with minors/parental consent, so this can't read as reviewed legal copy yet). Content reflects business rules already locked elsewhere in this file (per-student payment/refund handling, confidential-by-default projects, safety screening, AI-use disclosure, append-only Innovation Journal) rather than inventing new policy; `/competition-policies` also pulls `maxTeamSize` and both qualification deadlines from `Season` instead of hard-coding them.
  - **New shared primitive:** `src/components/design-system/PageIntro.tsx` — the "lighter, single-column editorial opener" the Aug 15/16 entries already anticipated by name (`SplitHero.tsx`'s own comment, and the Aug 16 audit's "deliberately not touched" note) but never built. Used by `/faqs` and `/contact`, which have no right-slot widget worth a full `SplitHero`.
  - **Nav updates:** `SITE_NAV_LINKS` (`src/lib/site-nav.ts`) gained `/national-finals` (later reverted — see below); a new `SUPPORT_NAV_LINKS` export (FAQs, Contact Us) surfaces in a new "Support" column in `Footer.tsx`, and the footer's legal line now links out to all three `LEGAL_PAGES` instead of being plain text.
  - Verified via `tsc --noEmit` (clean), `eslint` on every new/changed file (clean after removing one unused `formatSeasonDate` import in `/terms`), and real `curl` fetches of all ten public routes against the running dev server — all 200s, with `/national-finals`, `/faqs`, `/competition-policies`, and `/terms` spot-checked to confirm they render live `Season` values (`$111 per student`, `August 15, 2027` Finals date, `maxTeamSize: 4`, `April 30, 2027` / `July 30, 2027` deadlines) rather than placeholders.
  - **Not done / still open from the Aug 16 audit:** the `PageHero`/`CurriculumHero`/`PageIntro` consolidation the audit flagged (now three near-identical hero-shell patterns instead of two, since this session added `PageIntro`) is still deliberately deferred — bigger-surface-area refactor, not blocking. The scattered raw hex/px token-drift cleanup is also still untouched.
  - **Sprint 2 is now fully done.** Next up: Sprint 3 — Participant Registration, Parent/Guardian Consent, Team Creation & Team Management, Payment Processing (manual), Payment & Enrollment Records (per-student).
- **[Aug 17, 2026, later]** Site owner asked for National Finals content folded into How It Works instead of living on its own page, with no overlap against what How It Works already said. Removed `src/app/national-finals/` and `src/components/national-finals/` entirely, dropped `/national-finals` back out of `SITE_NAV_LINKS`, and added `src/components/how-it-works/NationalFinalsSection.tsx` (+ its `.module.css`), rendered on `/how-it-works` right after `DistinctionSection`.
  - Checked for overlap deliberately before writing this: `steps.ts`'s step 07 ("Compete") already briefly names the Finals, and `DistinctionSection` already owns the qualification-windows/join-vs-qualify explanation — so `NationalFinalsSection` states only what neither of those does: the actual `Season.nationalFinalsDate`, and the three-card "what happens at the event" breakdown (Showcase/Defense/Recognition) carried over from the deleted `FinalsOverview`. It does not restate the spring/summer deadlines or the join-vs-qualify distinction.
  - `InfoBand`/Pattern B is therefore unused again (same status as before the Aug 17 morning entry — the deleted `QualificationPath` was its only caller) — still a real, previously-built component worth reusing before writing a new one, just nothing currently calls it.
  - Verified via `tsc --noEmit` (clean), `eslint` (clean), and live `curl` checks: `/national-finals` now 404s, `/how-it-works` still 200s and renders the new section's heading/date/three cards, and the real `Season.nationalFinalsDate` (`August 15, 2027`) appears correctly. Confirmed the Header/Footer primary nav no longer link to `/national-finals` anywhere on the homepage.
- **[Aug 19, 2026]** **Sprint 3 confirmed complete.** Built Participant Registration, Parent/Guardian Consent, Team Creation & Management, and manual Payment Processing/Enrollment Records — the first sprint that writes real `Student`/`Parent`/`Team`/`Consent`/`Enrollment`/`Payment` rows through actual app flows instead of only the seed script.
  - **Schema addition** (migration `20260819090000_add_registration_fields`): `Student.guardianEmail` (required), `Team.joinCode` (`@unique`), and `Season.paypalLink`/`venmoHandle`/`zelleInfo` (nullable). Same shadow-DB problem as the Aug 16 trigger migrations hit again — `prisma migrate dev --create-only` fails with P3006 because the shadow database replay chokes on the earlier `auth.*` trigger migrations (no `auth` schema there). Hand-wrote this migration's SQL instead and applied with `prisma migrate deploy`, same workaround as before. **This is now the established pattern for any future migration in this repo, not a one-off**: always `--create-only` first for anything that doesn't touch `auth.*`, but if it P3006s, hand-write the SQL and `migrate deploy` it. `Student.guardianEmail` and `Team.joinCode` both needed a backfill-then-constrain sequence in the SQL (temporary default / generated value, then `SET NOT NULL`) since the seeded rows already existed.
  - **How Student and Parent get linked**, since both roles self-serve independently (Sprint 1) and the schema requires `Student.userId`/`Parent.userId` to already point at a real `User` — neither can be created "on behalf of" the other: a student enters their parent's email as `guardianEmail` at `/register`. If a `Parent` profile with that email already exists at that moment, the link (`StudentParent`) is created immediately; otherwise the *parent's* `/register` step (fullName/phone) does the same lookup in reverse when it runs, auto-linking any already-registered student whose `guardianEmail` matches. Order-independent by design — works whichever of the two registers first. Verified live: a student with a guardian email matching an *unrelated* second student's different guardian email correctly did **not** get cross-linked, and a parent could not reach `/consent/[studentId]` for a student they have no `StudentParent` link to (redirected to `/dashboard`, not a 403 page — deliberately doesn't confirm or deny the student id exists).
  - **Team joining**: `Team.joinCode` is a 6-character code (excludes `0/O/1/I/L` — see `src/lib/join-code.ts`) generated at team creation and shown once on `/register/payment` via a query param right after creation. Verified live: a second student joining via that code lands on the same `Team`, and `Season.maxTeamSize` is re-checked at membership-creation time (not just at page load) to close the obvious TOCTOU gap for two students joining at once near the limit.
  - **Payment/Enrollment timing** deliberately matches CLAUDE.md's payment-flow steps literally: `Enrollment` and `Payment` are **not** created at `/register` — only at `/register/payment` submission, together, in one write, with `Payment.status: SUBMITTED` immediately (there's no window where a `PENDING` `Payment` row exists, since `Payment.method` is a required non-nullable field and isn't known until that form is submitted). A `REJECTED` payment reuses the same `/register/payment` form to update the existing 1:1 `Payment` row (method/reference/screenshot, status back to `SUBMITTED`, `rejectionReason` cleared) rather than creating a second row. Verified live end-to-end: two teammates' `Enrollment`/`Payment` rows are fully independent (confirmed via direct DB read after the run) — exactly the "one teammate's unverified payment never blocks the other" rule.
  - **Payment screenshot storage**: `src/lib/storage.ts` (`saveUploadedFile`) writes to `public/uploads/payment-screenshots/` on local disk — the explicit stand-in for the still-deferred Cloudflare R2 setup (CLAUDE.md tech stack). Every call site goes through this one function so swapping to R2 later is a one-file change. `public/uploads/` added to `.gitignore`.
  - **New shared design-system primitives**: `RadioGroup` (extracted from what was previously duplicated inline in `SignupForm.tsx`'s role selector — `SignupForm.tsx` was migrated onto it too, so there's one implementation, not two) and `Checkbox`, both under `src/components/design-system/`. `AuthCard` gained a `wide` prop (680px vs. the original 440px) for this sprint's longer forms rather than a second card component. `src/lib/innovation-fields.ts` holds the fixed interest list (also intended for `Project.category` in Sprint 5, so a student's stated interests and their eventual project category stay drawn from the same list). `src/lib/auth.ts` (`getCurrentAppUser`) extracts the Supabase-session-to-`public.User` lookup that was about to be duplicated across seven new call sites.
  - `src/proxy.ts` now also gates `/register` and `/consent` behind the same "redirect to `/login` if no session" check `/dashboard` already had.
  - `src/app/dashboard/page.tsx` changed from a pure identity-chain proof into the real "what's my next step" hub for Student/Parent, reading state off which rows exist (no `Student` → register; `Student` but no `Enrollment` → pay; payment `SUBMITTED`/`VERIFIED`/`REJECTED` → matching notice) rather than a status enum. Parent view lists linked students with per-student consent status. Coach branch untouched — Coach registration is explicitly Sprint 7, out of scope here.
  - **Verified end-to-end against the live Supabase project in a real headless browser** (Playwright, not curl-replayed forms — this sprint's forms don't progressively-enhance to a guessable static hidden-field POST the way the Sprint 1 auth forms did), with real Admin-API-created STUDENT/STUDENT/PARENT accounts. One new wrinkle worth recording: `signInWithPassword` (and therefore a real `/login` form submission) is captcha-gated in this Supabase project — confirmed by hitting it directly and getting `captcha_failed` even for an admin-created, already-confirmed user — so login for automated testing has to go a different route: `admin.generateLink({type: 'magiclink'})` + `verifyOtp` with the returned `email_otp` (not `hashed_token`, which came back "expired" immediately — `email_otp` is the one that actually works), which isn't captcha-gated. The resulting access/refresh tokens were injected into the browser via the real `@supabase/ssr` `createBrowserClient` (loaded from esm.sh at the matching installed version, `0.12.4`) calling `auth.setSession()` from inside the page context, so the cookie written is byte-for-byte what the app's own client produces — no hand-rolled cookie format. Full flow walked and passed: student1 registered + created a team + saw the join-code banner + paid; student2 registered + joined via that exact code + paid; parent registered + was auto-linked to student1 only (not student2, different guardian email) + submitted consent with all 8 required boxes checked and media consent deliberately left unchecked; dashboard reflected every state transition correctly. Confirmed via a direct Prisma read afterward that every row (`Student`, `Team`/`TeamMembership`, `Enrollment`/`Payment` ×2 independent, `StudentParent`, `Consent`, `MediaConsent`) matched expectations exactly. All test rows and auth users cleaned up afterward (`userCount`/`studentCount`/`teamCount` confirmed back to the exact pre-test seed baseline: 23/4/1).
  - **Also caught and fixed mid-session**: a stale `next dev` process left running from earlier in the day was still bound to port 3000 and serving a pre-migration Prisma Client, which surfaced as `Unknown argument guardianEmail` the first time the flow was exercised — not a code bug, but worth remembering: Next's dev server doesn't pick up a `prisma generate` that happened after it started (it doesn't watch `node_modules`), so always restart `next dev` after any schema/migration change made mid-session rather than assuming a long-running dev server will pick it up.
  - **Not done / deliberately out of scope this sprint**: Coach registration (Sprint 7), admin payment-verification UI (Sprint 6 — `Payment.status` can currently only reach `SUBMITTED` through the app; flipping to `VERIFIED`/`REJECTED` still requires a direct DB write until then), `StageProgress` row creation (still deliberately left for Sprint 4, which is what actually builds the six-stage unlock logic that would consume it).
  - **Sprint 3 is now fully done.** Next up: Sprint 4 — Student Dashboard, Six-Stage Learning Path template, Stage Progress Tracking, Sequential Stage Unlocking, Online Learning Materials, Practice Activities, File & Media Uploads, and building INSIGHT + INVESTIGATE fully.
- **[Aug 19, 2026, later]** Diagnosed and fixed a "Database error saving new user" reported on the real `/signup` form (not the Sprint 3 flow — plain signup, any role). Root cause: **every row in `public.User` (all 27, including the real `rainaartist@gmail.com` test account from Aug 15) had gone orphaned — Supabase's `auth.users` table was completely empty (confirmed via `admin.listUsers()`, 0 results) while `public.User` still had 27 rows referencing those now-nonexistent accounts.** The `handle_new_auth_user()` trigger itself was fine (verified via `admin.createUser` calls for all three self-serve roles, all succeeded) — the failure only happens when a signup's email collides with one of these orphaned rows: the new `auth.users` insert succeeds (table's empty, no conflict there), but the trigger's `insert into public."User"` then hits the `email` unique constraint against the orphaned row and rolls back the whole transaction, which GoTrue reports as the generic "Database error saving new user".
  - **Cause of `auth.users` being empty is still unknown** — not something this session's code or test cleanup did (test cleanup only ever deleted specific accounts it created by id, never bulk-deleted). Likely a Supabase dashboard action (manually clearing users, a project pause/restore, etc.) that happened outside any Claude Code session. If this recurs, check the Supabase dashboard's Authentication → Users page and audit log before assuming it's an app bug.
  - **Fix applied**: since literally nothing could log into any of the 27 orphaned rows anymore either way, deleted every row across all 40 app tables in dependency order (children before parents) and re-ran `npx prisma db seed` to restore the documented baseline (Maya/Jordan+Priya/Sam, admin/coach/judge, one Season) — same seed data as every previous entry in this log, just freshly inserted. Verified via `admin.createUser` afterward that a fresh signup no longer collides, and via `prisma.user.count()` (12, matching the reseed) that no orphans remain. **The real `rainaartist@gmail.com` account is gone and needs to sign up again from scratch** — that was unavoidable once its `auth.users` row was already gone; there was no way to recover it, only to stop it from blocking future signups.
- **[Aug 19, 2026, security review]** Ran a security review over all of Sprint 3's uncommitted code (login/signup/register/payment/consent/dashboard) — two-phase: identify candidates, then independently re-verify each before trusting it. One candidate (a TOCTOU race letting a team exceed `maxTeamSize` by one under concurrent joins) was investigated and correctly rejected — real race, but a soft capacity edge case, not a security boundary. One was confirmed real and fixed:
  - **Finding, HIGH: self-consent bypass via the guardian-email auto-link.** The `StudentParent` auto-link (Aug 19 entry above) matches on a self-reported email string with zero verification that the matched `Parent` account actually belongs to this student's real guardian. A minor could register a Student account, list a *second email address they also personally control* as `guardianEmail`, then register that second email as a `PARENT` account — auto-linking immediately and granting them authority to submit their own required consent (participation, safety, AI-use, and the photo/video release) with no real guardian ever involved.
  - **First proposed fix was itself wrong, caught before building it**: an email-confirmation-link step was initially proposed, but on reflection this doesn't work — the confirmation would be emailed to the *Parent account's own inbox*, which in the exact attack described is an inbox the attacker already controls. A bilateral challenge between "the student side" and "the parent side" can't distinguish a real separate guardian from one person with two email addresses, since the attacker *is* both parties and can satisfy any such challenge trivially. Worth remembering for any future guardian/age-verification work: self-serve email loops don't close this class of gap; only an independent third party (a human admin, or eventually a coach) can.
  - **Actual fix**: `StudentParent` gained `verifiedAt`/`verifiedByAdminId` (migration `20260819150000_add_student_parent_verification`, same nullable pending→admin-approved pattern as `Payment.verifiedAt`/`verifiedByAdminId` — mirrored deliberately for consistency). The auto-link logic in `src/app/register/actions.ts` (both directions) now deliberately leaves `verifiedAt` unset — the row is created (so the parent shows up on the dashboard at all) but is **not** consent-eligible. Both `src/app/consent/[studentId]/page.tsx` (render-time) and `actions.ts` (defense-in-depth, re-checked independently — a direct POST bypassing the page must not be able to submit consent through an unverified link) now require `verifiedAt != null`, showing a "pending review" notice instead of the consent form otherwise. Dashboard's parent view shows a third state ("Link pending admin review") alongside the existing "Give consent"/"Consent recorded". No admin UI exists yet to flip `verifiedAt` — same as `Payment.status`, it's a direct DB write until Sprint 6's Admin Dashboard exists; noting this explicitly so a future session doesn't assume it's automated.
  - Seed data (`prisma/seed.ts`) sets `verifiedAt: new Date()` on its demo `StudentParent` rows — those represent already-established relationships (and already have real `Consent` rows), not the self-reported-match flow, so they shouldn't land in the same pending state a real new signup would.
  - **Verified live**: reproduced the exact attack end-to-end in a real browser (two real accounts, student listing the attacker's own second email as guardian) — confirmed the `StudentParent` row lands with `verifiedAt: null`, the dashboard shows "pending" not "Give consent", `/consent/[studentId]` refuses to render the form, and zero `Consent` rows get created. Then simulated the admin-approval step (direct DB update, matching how payment verification works today) and confirmed the exact same student/parent pair could then successfully complete consent — proving the fix blocks the attack without breaking the legitimate path.
- **[Aug 19, 2026, Sprint 4]** **Sprint 4 confirmed complete**: Student Dashboard, Six-Stage Learning Path template, Stage Progress Tracking, Sequential Stage Unlocking, Online Learning Materials, Practice Activities, File & Media Uploads, and INSIGHT + INVESTIGATE built fully. No schema migration needed — `StageProgress`/`CurriculumModule`/`Lesson`/`Assignment`/`Submission`/`SafetyReview`/`AIDisclosure` were already shaped correctly from the Aug 13 schema draft; this sprint was pure application-layer + content work. Confirmed with the user up front: **Assessments (quizzes) are out of scope this sprint** (Sprint 6, per the build plan) — a stage completes on its core submission (+ a cleared safety review for INVESTIGATE), not a passing test score.
  - **New `src/lib/stage-progress.ts`**: `STAGE_ORDER`/`STAGE_SLUGS`/`STAGE_NUMBERS` (single source of truth — `prisma/seed.ts` now imports `STAGE_ORDER` instead of keeping its own copy), `ensureStageProgressInitialized(studentId)`, `getJourney(studentId)`, `completeStage(studentId, stageName)`. Stage unlocking is **lazy, not event-driven**: there's still no "on payment verified" hook (verification is a direct DB write until Sprint 6's admin UI exists), so `ensureStageProgressInitialized` runs reactively on every `/dashboard` and `/dashboard/[stage]` load and promotes INSIGHT from `LOCKED` to `CURRENT` the first time it sees a `VERIFIED` payment with nothing unlocked yet — same reactive-read pattern `dashboard/page.tsx` already used for registration/payment status.
  - **New `src/lib/project.ts`**: `getOrCreateStudentProject(studentId)` — the first real app-layer `Project`-creation code path (previously only `prisma/seed.ts` ever created one). **INSIGHT's first step lazily bootstraps the `Project`** (title/category, editable from the INSIGHT form) rather than pulling Sprint 5's "Project Submission Portal" forward — a deliberate scope call, not an oversight. Enforces the exactly-one-owner rule via a `zod` `.refine()` (CLAUDE.md's long-documented but until-now-unimplemented plan for this specific check — **`zod` added as a real dependency for this one use**, everything else in this sprint stayed with the codebase's existing manual-validation convention, deliberately not adopted project-wide). For a team, the first teammate to reach INSIGHT creates the shared `Project`; a create-race between two teammates is caught (`P2002` on `Project.teamId`) and self-heals onto whichever row won.
  - **Safety screening is project-level, not per-student** — `SafetyReview.projectId` is `@unique`, so on a team it's answered once and shared; CLAUDE.md's exact category list (humans/animals/health info/chemicals/biological materials/electricity/machinery/PII/environmental sampling/drones/AI/other) is a fixed checklist in `SafetyScreeningForm.tsx`, any checked box (or non-empty "other") sets `isHighRisk = true` and `status = PENDING_REVIEW`, which blocks `submitInvestigateAction` from completing the stage until an admin flips it to `CLEARED` (still a direct DB write this sprint — same pending→admin-approved pattern as `Payment.status`/`StudentParent.verifiedAt`, Sprint 6 builds the real review UI). No risk factors → auto-`CLEARED`, no admin involved. Once a `SafetyReview` row exists it's read-only to the student regardless of status — only an admin clears a `PENDING_REVIEW` one.
  - **New `src/components/journey/`**: `JourneyRail` (docs/design-system.md Section 12 — 166px sticky desktop rail collapsing to a 72px horizontal bottom strip at ≤820px, cobalt active state, coral only for the IP Checkpoint item, `aria-current="step"`), `StageLayout` (shared shell for every stage page), `JourneyOverview` (richer full-width version for the `/dashboard` hub), `LessonList`/`PracticeActivityList` (read-through `Lesson`/`Assignment` display — no per-item completion tracking, no model exists for that and none was added), `AiDisclosureFields` (shared required-on-every-final-submit form section, self-contained toggle), `SubmissionSummary` (read-only view for `COMPLETE` stages), `StagePlaceholder` (the 4 stages Sprint 4 doesn't build content for — IMAGINE/IP Checkpoint/ITERATE/IMPACT/INFLUENCE — still real, still unlockable via the normal sequential-unlock flow, just "coming in a later sprint" instead of a dead link).
  - **New route `src/app/dashboard/[stage]/`**: resolves a kebab-case slug to a `StageName` (`notFound()` on a bad one), enforces `LOCKED` → redirect to `/dashboard`, `CURRENT` → editable form, `COMPLETE` → read-only summary (a finished stage stays reviewable, it doesn't redirect away). `actions.ts` holds `saveInsightAction`/`saveSafetyScreeningAction`/`saveInvestigateAction`, each re-deriving student/stage/project state server-side rather than trusting anything from the client (same defense-in-depth convention as every other Server Action in this codebase). Draft vs. final submit is one `<form>` with two named submit buttons (`name="intent" value="draft"|"final"`) rather than two separate forms.
  - **`src/proxy.ts`** gained its first real per-role gate: `/dashboard/<stage-slug>` paths require `role === "STUDENT"`, read straight off the already-fetched Supabase session's `user_metadata.role` (no extra DB call at the edge) — matching the file's own long-standing note that per-role gating was deferred until a role-specific dashboard existed. Sequential-unlock (`LOCKED` stage) checking still can't happen here (needs a real Prisma read), so that stays in the stage page itself, same "server-rendered, not bypassable via direct URL" guarantee as everything else in this codebase.
  - **Content**: `Lesson.bodyMarkdown` for INSIGHT/INVESTIGATE is real short readings grounded in the already-approved copy in `src/components/curriculum/stages.ts` (not invented from scratch), rendered as plain blank-line-split paragraphs — deliberately not pulling in a markdown parser dependency for one field this sprint. New shared `src/components/design-system/Textarea.tsx` (same canonical treatment as `Input.tsx`, Section 17). `src/app/register/payment/ScreenshotUpload.tsx` generalized to accept `name`/`label` props (defaults preserve the original payment-screenshot call site) so INSIGHT's optional "photo of the problem" upload reuses it instead of duplicating a dropzone — the sprint's "File & Media Uploads" deliverable, a real use rather than a generic uploader nobody asked for.
  - **`prisma/seed.ts` made properly idempotent** (a real pre-existing gap, not introduced this sprint, but this sprint was the first to need re-running it repeatedly): `Project`/`SafetyReview`/`JournalEntry`/`TeamMembership`/`StudentProject` creates were previously unconditional `.create()`/`.createMany()` calls that only worked once on an empty database — running seed a second time crashed on `Project.individualStudentId`'s unique constraint. Now find-or-create / `upsert` / `skipDuplicates: true` throughout, verified by running seed twice in a row with zero errors and stable row counts.
  - **`ts-node` seed invocation needed `-r tsconfig-paths/register`** (added to both `package.json#prisma.seed` and the authoritative `prisma.config.ts`): `prisma/seed.ts` now imports `src/lib/stage-progress.ts`, which — like every other `src/lib` file — uses the `@/*` tsconfig path alias; `ts-node` doesn't apply tsconfig `paths` on its own the way Next's bundler does, so this silently only "worked" the first time by coincidence before failing consistently on every run after. `tsconfig-paths` added as a real devDependency (previously only present transitively via `eslint-plugin-import`) rather than relied on implicitly.
  - **Verified end-to-end against the live Supabase project**, same rigor as every prior sprint — but via **freshly created real Supabase Auth accounts** (Playwright + Admin API `generateLink`/`verifyOtp` magic-link pattern, same as Sprint 3), not the checked-in seed accounts: the seed script's demo students (`maya@example.com` etc.) have placeholder `supabaseUid: 'seed-...'` values with no real corresponding `auth.users` row, so logging into them for real would either fail outright or (worse) collide with the Aug 19 orphaned-`public.User`-row failure mode — worth remembering for any future test session that reaches for the seed accounts expecting a real login to work. Four fixtures, all torn down afterward (confirmed 0 remaining test rows): a fresh student walking the full INSIGHT → low-risk-auto-cleared-safety-screening → INVESTIGATE → IMAGINE-placeholder path and back to review a completed INSIGHT read-only; a second student hitting a high-risk safety screening, confirming `submitInvestigateAction` is blocked while `PENDING_REVIEW`, then a simulated admin-clear unblocking resubmission; two teammates confirming the shared `Project` gets created once and reused, and that the second teammate's still-`SUBMITTED` payment leaves her on the payment-pending dashboard notice, unaffected by her teammate's progress; and a `PARENT`-role session confirming `/dashboard/insight` bounces to `/dashboard`.
  - **Not done / deliberately out of scope this sprint**: Sprint 5 (Project Submission Portal, Innovation Journal, Speaking/Character/Ethics Challenges, IP Checkpoint content, Team Contribution Tracking, ITERATE/IMPACT/INFLUENCE) and Sprint 6 (Assessments/quizzes, admin Safety/Payment review UI) are still exactly as unbuilt as CLAUDE.md's sprint plan describes — this sprint didn't quietly pull any of that forward beyond the minimum INSIGHT project-bootstrap explained above.
  - **Sprint 4 is now fully done.** Next up: Sprint 5 — Project Submission Portal, Innovation Journal, Speaking Challenge Submissions, Character Challenges, Ethics Challenges, AI-Use Disclosure (already partly built this sprint as the shared `AiDisclosureFields` component — reuse it, don't rebuild), Safety Screening (already built), IP Checkpoint, Public/Confidential Project Controls, Team Contribution Tracking, and building IMAGINE, IP Checkpoint, ITERATE, IMPACT, INFLUENCE.
- **[Aug 20, 2026]** Two small follow-ups on top of Sprint 4, both same day:
  - **Unblocked the real `rainaartist@gmail.com` account** (the site owner's own test account, re-registered after the Aug 19 orphaned-`auth.users` incident) by manually verifying her `Payment` row (`SUBMITTED` → `VERIFIED`, `verifiedByAdminId` set to the seeded admin) — a direct DB write, same as every other "admin action" this codebase doesn't have a UI for yet until Sprint 6. This is the only gate `ensureStageProgressInitialized` checks before lazily unlocking INSIGHT, so no other change was needed for her account to reach the new dashboard.
  - **Redesigned `/dashboard`'s Student hub and the shared stage-page chrome** to match a client-provided HTML mockup (editorial dashboard with a big current-stage hero, a 6-card journey grid + IP Checkpoint band, a recent-activity panel, and a sidebar with a qualification-deadline card and a team/individual card) — the mockup's own CSS already used this codebase's exact token names 1:1, so this was a layout/composition change, not a new visual language. New `src/components/app/AppHeader.tsx` (shared sticky header — brand, Dashboard/Curriculum nav via the existing `NavLink`, profile avatar+initials, log out — used by both the dashboard hub and every `/dashboard/[stage]` page now, replacing `StageLayout`'s old bare Logo-only topbar) and `src/components/dashboard/` (`IntroSection`, `CurrentStagePanel`, `JourneyGrid`, `ActivityPanel`, `DeadlineCard`, `TeamCard`, sharing one `DashboardHub.module.css` since they're only ever composed together). `src/lib/stage-copy.ts` (new) adapts the marketing curriculum copy (`src/components/curriculum/stages.ts`) to the `StageName` enum so both the hub and the stage pages can show real approved headline/description text instead of the seed's placeholder `CurriculumModule.description` strings.
  - Every number on the redesigned hub is real, not mockup filler: current-stage progress is 0/50/100% derived from whether a `Submission` exists yet and its `isFinal` flag (there's still no per-lesson completion tracking to derive a finer number from — unchanged from Sprint 4's own scope decision), "Recent activity" reads actual `Submission` rows (a real, honest stand-in for an "evidence trail" until the Innovation Journal ships in Sprint 5, not fabricated content), the qualification deadline picks whichever of `Season.springQualifyDeadline`/`summerQualifyDeadline` is next relative to today, and the team/individual card reads real `TeamMembership`/`Team` data. The mockup's "Project"/"Team" nav items and "View evidence" button were dropped rather than kept as dead links — those pages don't exist until Sprint 5.
  - Verified via `tsc`/`eslint`/`next build` (all clean) and real Playwright screenshots of the live dev server logged in as `rainaartist@gmail.com` (both `/dashboard` and `/dashboard/insight`) — confirmed against the actual rendered page, not just reasoned about.
- **[Aug 25, 2026]** **Site owner asked for the IP Checkpoint removed entirely — the gate between IMAGINE and ITERATE no longer exists anywhere in the product.** Two decisions locked with the site owner up front, since this touched the confidentiality security rule: (1) projects stay **confidential-only, permanently** — there is no longer any mechanism, now or planned, for a project to become `PUBLIC`; (2) the journey is renumbered to 6 core stages with no gate: INSIGHT → INVESTIGATE → IMAGINE → ITERATE → IMPACT → INFLUENCE.
  - **Schema** (migration `20260825120000_remove_ip_checkpoint`, hand-written and applied via `prisma migrate deploy` — the shadow DB still can't replay the `auth.*` trigger migrations, so `migrate dev --create-only` P3006s on this repo for any migration, same as every migration since Aug 15): dropped `StageName.IP_CHECKPOINT` from the enum (Postgres can't drop an enum value in place, so the migration recreates the type and swaps every column over — `CurriculumModule`/`StageProgress`/`Submission`), dropped the never-wired `IPReview` table entirely, and dropped `Project.visibilitySetAt`/`visibilitySetByStudent` (they only ever existed to audit a student's explicit PUBLIC request at the checkpoint, so they're dead columns without it). `Project.visibility` itself is untouched and keeps its `CONFIDENTIAL` default as the fail-safe — same defense-in-depth principle as before, just with no write path that could ever flip it. Rows already referencing `IP_CHECKPOINT` (1 seed `CurriculumModule`, 5 seed `StageProgress` rows, 0 `Submission` rows — it never had real UI, confirmed before writing the migration) were deleted as part of the same migration, ahead of the enum swap.
  - **App code**: `STAGE_ORDER`/`STAGE_SLUGS`/`STAGE_NUMBERS` (`src/lib/stage-progress.ts`) and `STAGE_COPY` (`src/lib/stage-copy.ts`) dropped the gate entry — everything downstream (journey rail, dashboard journey grid, stage pages, sequential unlock) already derived from these single sources of truth, so no other unlock-logic changes were needed. `src/components/curriculum/stages.ts` lost `IP_CHECKPOINT`/`CURRICULUM_TIMELINE`/`isGate` entirely — `FrameworkCard` and `StageDetailList` now read `CURRICULUM_STAGES` directly instead of a spliced timeline, which also deleted a fair amount of `isGate`-conditional branching in both components. `JourneyGrid`'s separate coral "IP CHECKPOINT" band underneath the 6-stage grid is gone; the grid now renders all 6 stages with nothing below it.
  - **Homepage hero** (`JourneyHero.tsx`/`.module.css`, the scroll-jacking cube choreography): this was the most involved single piece, since the gate used to occupy its own beat (BEATS[3], reusing IMAGINE's rotation as a deliberate "pause") with a dedicated hexagon-badge visual (`.hexBadge`/`.stageGate`) layered across every responsive tier. Removed that beat and renumbered `BEATS`/`TOTAL` from 7 beats/`7.85` to 6 beats/`6.8`, keeping the same `1.05`-unit spacing and the same rotation values for every remaining stage (Iterate/Impact/Influence now just slot into the positions the gate used to sit between, rather than any rotation math changing) — verified visually via real screenshots mid-scroll, not just reasoned through. All `.stageGate`/`.hexBadge`/`.isGate`/`--gate-scale`/`--gate-badge-size`/`--gate-icon-offset` CSS deleted across the base rules and all 4 responsive tiers (1500px/900px/820px/460px) plus the reduced-motion fallback.
  - **Design system**: `docs/design-system.md` Section 11's canonical scroll sequence and Section 12's journey-rail spec both dropped the IP Checkpoint line; coral's role description across the doc, `globals.css`, and the `/design-system` style-guide page changed from "action + the IP Checkpoint" to "action, warnings, and rare contrast moments" — coral itself is untouched and still used for errors/action states elsewhere, it just lost this one specific reservation.
  - **Content**: updated copy on `/curriculum` (metadata description), `/faqs` (the curriculum-overview and public/private questions), `/terms`, `/privacy`, and `/competition-policies` (all three legal pages' confidentiality language, since "confidential by default, public if you ask at the IP Checkpoint" is now just "confidential, permanently"), `/how-it-works` (the Learn step's detail copy), and the dashboard hub's journey section intro (`src/app/dashboard/page.tsx` — this one was caught only during live-screenshot verification, not the initial text sweep, since it was a runtime-rendered string rather than something a plain grep for "IP Checkpoint" would have caught differently from the others — it wasn't; it just got missed on the first pass and found on the second).
  - **Verified**: `tsc --noEmit` and `eslint` both clean; `prisma db seed` re-run twice cleanly against the live Supabase project (idempotent, per the Aug 19 seed fix); real Playwright screenshots (fresh Admin-API-created test student, magic-link session via `@supabase/ssr`'s `createBrowserClient` loaded from esm.sh — same pattern as every prior sprint's live verification) of `/dashboard`, `/dashboard/insight`, and the homepage hero both at rest and mid-scroll, confirming the 6-stage journey grid/rail render with no gate remnants and the cube choreography's rotation math still lands cleanly on each stage. Test student and its `Enrollment`/`Payment`/`User`/auth account all cleaned up afterward.
  - **Not touched**: the historical status-log entries above this one (Aug 10 through Aug 20) describe the IP Checkpoint as it existed at the time and were deliberately left as-is rather than rewritten — this entry is the correction to read instead. The `ProjectVisibility` enum (`CONFIDENTIAL`/`PUBLIC`) and the `visibility` column stay in the schema even though nothing can currently set `PUBLIC` — same "build the column now, don't force a migration later" reasoning as every other Phase-2-ready piece of this schema, in case a future phase ever adds a real public-visibility flow.
- **[Aug 29, 2026]** The i3League company handed over their complete curriculum documentation — copied verbatim into `docs/curriculum/` (see `docs/curriculum/README.md`; that folder + the auto-memory `i3league-curriculum-docs` / `six-stage-journey` / `curriculum-four-layers` / `review-rubric-model` / `platform-evidence-model` notes are now the authoritative source for all curriculum-facing copy, ahead of anything in `src/`). First application of it: **reworked the student dashboard to describe the real working process instead of the marketing paraphrase.**
  - **`src/lib/stage-copy.ts` is now the canonical dashboard stage-copy source.** It no longer derives from `src/components/curriculum/stages.ts` (that marketing module is untouched and still owns the public `/curriculum` page). Each of the 6 stages now carries, straight from `docs/curriculum/03_SHOW` + `04_REVIEW`: `coreQuestion`, `description` (real work of the stage), `producesArtifact`, `submissionFormat`, `submissionChecklist[]` (the Layer 3 "Must include" list), `reviewerChecks[]`, `advancesWhen` (the Layer 3 "ADVANCE when" bar), `mentalTest`, and optional `depthNote` (HS/Stretch additions) / `finalsNote`. Also exports program-level constants: `PROGRAM_TAGLINE` ("Students build evidence, not just ideas."), `CURRICULUM_LAYERS` (LEARN→DO→SHOW→REVIEW), `OPERATING_CYCLE` (Feedback→Revise→Resubmit→Advance, revision never penalized), `REVIEW_DIMENSIONS` (the six), `ADVANCEMENT_GATE_NOTE` (floor-based gate, not a grade). `getStageCopy()` signature is unchanged for callers but now returns the full `StageCopy`.
  - **Dashboard hub** (`src/app/dashboard/page.tsx` + `src/components/dashboard/`): `CurrentStagePanel` now shows the stage's core question and, in place of the old "NEXT UP" block (which pulled a thin `Assignment` row — that DB query is removed), a "WHAT YOU'LL PRODUCE" block with the real required artifact + `advancesWhen`. `JourneyGrid` cards show each stage's core question instead of the marketing headline. New `StageProcessNote` component renders between the journey section title and the grid — the LEARN/DO/SHOW/REVIEW layer strip + the operating cycle. Journey section headline changed to the program tagline; intro line now says a revise-and-resubmit is expected, never penalized.
  - **Stage pages** (`src/app/dashboard/[stage]/page.tsx` + `src/components/journey/`): new `StageBrief` (core question + what-you'll-produce + the mental test as chips + depth/finals footnotes) renders at the top of every stage — built stages and placeholders alike. New `SubmissionChecklist` (SHOW — the Layer 3 "Must include" list) and `StageReviewNote` (REVIEW — reviewer checks, `advancesWhen`, the gate note, the operating cycle) render on every in-progress built stage after its form, and inside `StagePlaceholder` for the 4 not-yet-built stages — so IMAGINE/ITERATE/IMPACT/INFLUENCE now show what they'll ask for and how they're reviewed instead of just a "coming soon" line. `LessonList`/`PracticeActivityList` section labels retitled to "Learn — teaching notes" / "Do — practice activity". Shared styles in `src/components/journey/StageInfo.module.css`.
  - **Deliberately not done** (Sprint 5 scope, would be scope creep): no new submission form fields for stages 3–6, no `platform-evidence-model` evidence-ID/worksheet-field data model, no reviewer/admin UI, no schema changes. `prisma/seed.ts`'s 5 placeholder `CurriculumModule` descriptions for stages 2–6 are also untouched (they're not dashboard-visible — the dashboard reads `stage-copy.ts`, not `module.description`).
  - **Verified**: `tsc --noEmit`, `eslint`, and `next build` all clean. The build compiles both `/dashboard` and `/dashboard/[stage]` including all new components.
  - **[Aug 29, later — visual/design pass]** Site owner asked to make the dashboard "a lot cleaner, extremely user-facing, not super wordy but descriptive, matching the homepage design style." Did a full editorial pass against `docs/design-system.md` (warm paper on cool canvas, mono eyebrows, Chakra Petch headlines with negative tracking, offset cobalt planes for depth — used on only the 2 hero surfaces, not every card — one ink "signature dark moment", Pattern C connected plane for the journey, whitespace/typography before borders):
    - **`src/components/dashboard/DashboardHub.module.css` rewritten.** Tighter type scale matched to the homepage, `64px` section rhythm, restrained shadows. Hub sections: intro (short one-line lede, `Welcome back, {first}.`), current-stage hero (ink slab + paper + blue-pale progress rail; copy cut to headline + one-line core question + button; progress rail shows `%` + bar + `YOU'LL PRODUCE` artifact only — the long `advancesWhen` sentence moved off the hub to the stage page), a slim `LEARN → DO → SHOW → REVIEW` band (`StageProcessNote` slimmed from 4 paragraph blurbs to one mono chip row + one sentence), the six-stage `JourneyGrid` as one connected hairline-celled plane showing name + core question + status, and a bottom row of full-width `ActivityPanel` + a 2-up `DeadlineCard` (ink) / `TeamCard`.
    - **Stage-page guidance blocks de-wordied.** `StageBrief` trimmed to the core question + `You'll produce` + mental-test chips (+ optional one-line depth note). `SubmissionChecklist` is now a compact 2-column list with a format caption, no card wrapper. `StageReviewNote` cut from a reviewer-check list + two paragraph blocks to: `Advances when …` + the six `REVIEW_DIMENSIONS` as quiet chips + one `Feedback → revise → resubmit` line. Shared styles in `src/components/journey/StageInfo.module.css` — only `StageBrief` gets a surface (cobalt left rule), the other two are borderless.
    - **Title-case stage names.** Added `name` (`"Insight"`, `"Investigate"`, …) to `StageCopy`; `CurrentStagePanel`, `JourneyGrid`, `JourneyRail`, and the `[stage]` page heading now use it instead of the raw `StageName` enum / `CurriculumModule.title` (which is stored uppercase in the live DB) — so it's "Continue Insight", not "Continue INSIGHT".
    - **Verified with real authenticated screenshots** this time: Playwright (installed into the session scratchpad, not the repo) + Supabase Admin API `generateLink` magic-link → `verifyOtp` → `sb-<ref>-auth-token` cookie, reusing the existing real `rainaartist@gmail.com` account (VERIFIED payment, INSIGHT current). Screenshotted `/dashboard` and `/dashboard/insight` at 1300px, iterated on spacing/column widths/text orphans from what the shots showed. `tsc`/`eslint`/`next build` all clean. No repo deps added (playwright lives only in the scratchpad); no schema or data changes.
  - **[Aug 29, still later — sidebar app-shell redesign]** Site owner handed over an HTML mockup (a fixed-left-sidebar workspace: sidebar journey nav + sticky topbar + hero + six-stage node track + multi-panel grid) and asked to make the dashboard "more like this mockup." Two scope calls confirmed with them up front: (1) build the shell for **both** `/dashboard` **and** every `/dashboard/[stage]` page (one consistent chrome), and (2) **drop** the mockup panels we have no data for yet rather than filling them with placeholder/filler content.
    - **New `src/components/app/AppShell.tsx` (+ `.module.css`)** — the shared authenticated-Student chrome, a client component (needs `usePathname` for active nav + `useState` for the mobile drawer). Fixed **paper** sidebar (never a dark shell — design-system Section 18 rule 1): brand, "Student workspace" tag, `Overview` → `/dashboard`, the six-stage journey list (mono number + name + state; locked stages are plain `<span>`s not links, per "Sequential stage unlocking"), `Curriculum` → `/curriculum`, and a bottom student chip (avatar initials + name + `Grade N · Team/Individual` + Log out). Sticky topbar: mobile hamburger, `Workspace / {breadcrumb}` crumb, and a `Help` link → `/faqs` (a real page — kept the mockup's search/bell icons **out**, no functionality behind them). At `≤900px` the sidebar becomes an off-canvas drawer with an overlay; closes on nav-link click (each `Link` has `onClick={closeMenu}`) and Escape — deliberately **not** a `pathname` effect, which trips this repo's `react-hooks/set-state-in-effect` lint rule.
    - **`AppShell` replaces both the old `AppHeader` (top-only) and `JourneyRail` (separate 166px rail)** — the sidebar now *is* the journey rail, so they can't drift. Both files deleted, plus `src/components/journey/JourneyRail.module.css`. `StageLayout` rewritten to wrap its content in `AppShell` (drops the rail; keeps `activeStage` in its props for a possible future in-page stepper but no longer uses it — the sidebar derives active from the URL). `StageLayout.module.css` cut down to just the centered `max-width: 900px` reading column. `src/app/dashboard/[stage]/page.tsx` now also fetches a `teamMembership` to build the `studentMeta` string the shell needs.
    - **Hub (`src/app/dashboard/page.tsx` `StudentHub` + `src/components/dashboard/`) recomposed to the mockup's information architecture**, all real data:
      - `StageHero` (replaces `IntroSection` + `CurrentStagePanel`): paper editorial block (welcome eyebrow, the current stage's `headline` + `coreQuestion` + `description`, primary `Continue {stage}` CTA, `Season`/`Project` mono stats, big faint stage-number watermark) joined to the one ink "signature dark moment" half (`{n} of 06`, stage name, progress). A `StageProgressState` type (`"not-started" | "draft" | "submitted"` in `src/components/dashboard/types.ts`), derived **only** from whether the current stage's `Submission` exists and is `isFinal`, drives the hero's progress bar — the same coarseness the hub has always had, no fabricated %. (Superseded in detail by the "combine hero + de-dupe pass" entry below — the two halves were later merged into one bordered card and the early `Learn·Do·Show·Review` meter dropped for a plain progress bar.)
      - `StageNodeTrack` (replaces `JourneyGrid`): the six stages as circular numbered nodes on one connector line — cobalt fill = advanced, coral ring = current, hairline = locked. Locked = not a link.
      - `StageLayerStrip` (replaces `StageProcessNote`): the `CURRICULUM_LAYERS` (LEARN/DO/SHOW/REVIEW) as a static 4-card explainer. (Originally highlighted the active layer from `progressState`; that was dropped in the de-dupe pass below since the hero's progress bar already shows "where you are".)
      - `ArtifactPanel` (new): the current stage's `producesArtifact` + `description` + `submissionFormat` + a coarse 3-step readiness bar (Not started / Draft saved / Submitted) + the `submissionChecklist` as a plain 2-col "what it must include" list (no per-item checkmarks — that's the tracking we don't have) + `ADVANCEMENT_GATE_NOTE` + a **ghost** `Continue` (hero owns the one primary button per view).
      - `MentalTestCard` (new): the stage's `mentalTest` as a cobalt chip sequence, blue-pale surface with a cobalt left rule.
      - `ActivityPanel` / `DeadlineCard` / `TeamCard` kept, restyled onto the rewritten `DashboardHub.module.css` (shared by all `src/components/dashboard/` pieces). Deadline card stays the hub's one ink surface.
      - **Dropped from the mockup** (no data model yet, per the scope call): the "Previous gate / reviewer feedback" card, per-checklist-item ready/needs-work counts, and the "Learned / Did / Changed" structured evidence rows. Also dropped the mockup's non-functional search/notification icons and its "sample data" banner (our data is real).
    - **[Aug 29, follow-up tweaks from the site owner]** After seeing it live: (1) built a `PortfolioList` component (stage-by-stage `producesArtifact` + status), then **removed it** the same day — the owner judged it redundant with the six-stage journey track directly above it. Its `#portfolio` anchor and the hero's ghost `View portfolio` / journey-complete `View your portfolio` buttons went with it (hero now has just the one primary `Continue {stage}`); the `--green` token added only for its "complete" state was reverted too. `ActivityPanel` moved into the right column so the lower two-column grid stays height-balanced with only `ArtifactPanel` on the left. (2) `MentalTestCard` lost its cobalt left rule (plain blue-pale surface now). (3) The ink current-stage card lost its decorative rotated-square `::after`. (4) The hero's faint stage-number watermark was shrunk (`clamp(72px,8vw,116px)`) and restyled to match the homepage hero's scroll watermark (`.jwNum` in `JourneyHero.module.css` — cobalt `rgba(49,88,216,.125)`, `-0.075em` tracking, `.78` line-height).
    - **[Aug 29, combine hero + de-dupe pass]** Owner: "combine the welcome-back and current-stage boxes; make sure there isn't any repeated info." `StageHero` is now **one card** — the paper editorial half (welcome, stage `headline`, `coreQuestion`, `description`, primary CTA, Season/Project) sits flush against the ink half (border + offset shadow on the wrapping `.hero`, not the two halves), and the ink half uses `justify-content: space-between` so the label / stage name / progress spread top-to-bottom instead of the dark panel reading as empty. The old four-segment `Learn·Do·Show·Review` meter in the ink half is gone — replaced by a single progress bar + `%` + one-line note (`not-started` 0% / `draft` 50% / `submitted` 100%, from the same `StageProgressState`). De-dupe: the stage `description` now appears **only** in the hero (was also `ArtifactPanel`'s intro `<p>`); progress appears **only** in the hero (removed `ArtifactPanel`'s separate readiness bar + its `progressState`/`slug` props and the CSS for both); the LEARN/DO/SHOW/REVIEW concept appears **only** in `StageLayerStrip`, which is now a static explainer (dropped its `progressState` prop and the active-layer highlight that re-encoded "where you are"); the second `Continue {stage}` button in `ArtifactPanel` was removed (hero owns the one CTA). `ArtifactPanel` now leads with the produced-artifact name as its `<h2>` (no more generic "Your artifact" heading + separate name). The Project stat's empty fallback changed `"Not started"` → `"Untitled"` so it doesn't collide with the progress label. Deliberately kept: the current stage name showing in the sidebar nav + node track + hero — that's standard "you are here" affordance across nav/progress/subject, not content duplication. (The *visual* description here — one combined paper+ink card, `space-between` ink half, progress bar replacing the meter — is superseded by the "hero re-skin" entry below, which moved the hero onto the `SplitHero` shell. The de-dupe **decisions** in this bullet still hold.)
    - **[Aug 29, hero re-skin to match the marketing heroes]** Owner disliked the combined paper+ink-slab hero and asked for it to look "more like the heroes for the home pages." `StageHero` was rebuilt on the **shared `SplitHero`** shell (`src/components/design-system/SplitHero.tsx` — same component How It Works / Curriculum / Pricing / FAQs / Contact use): checkerboard-backed band on the canvas, mono cobalt eyebrow (`Welcome back, {first}`), big Chakra-Petch editorial headline (the stage `headline`), muted lede (the stage `description`), and a right-hand **stat box that mirrors `PageHero.module.css`'s `.statBox`** (paper, hairline border, hard `8px 9px 0 var(--blue-soft)` offset shadow, giant cobalt stage number, bold stage `name`, `coreQuestion` sub, plus a thin progress bar + `label · %`). Buttons + the `2026–2027 season · {project}` context line sit in the copy column. `SplitHero` gained one optional prop for this — `children`, rendered in the copy column after the lede (undefined for every existing marketing caller, zero change to those pages, verified: all five still 200 and `next build` clean). New `src/components/dashboard/StageHero.module.css` (own file now, not `DashboardHub.module.css`) only tunes the hero padding, steps the `h1` down from the marketing `--text-hero` since the app sidebar narrows the column, and styles the stat box. The old combined-card hero CSS (`.hero`/`.heroMain`/`.heroSide`/`.heroWatermark`/`.heroStats`/meter/progress) was deleted from `DashboardHub.module.css`. `StageProgressState` and the coarse `not-started`/`draft`/`submitted` → 0/50/100 mapping carry over unchanged; still the only progress read on the hub. **(Fully superseded by the "make it look like a standard dashboard" entry below — `StageHero` and its `SplitHero`/`children` coupling were removed; only `StageProgressState` + the 0/50/100 mapping survive.)**
    - **[Aug 29, "make it look like a standard dashboard"]** Owner: keep the site's design language but stop the top of the hub reading like a marketing landing page. The `SplitHero`-based `StageHero` (checkerboard band + `--text-hero` headline + stat box) was **removed** — `SplitHero`'s short-lived `children` prop was reverted with it, so that component is back to exactly its pre-reskin form and the five marketing pages are untouched. Replaced by three plain dashboard pieces in `src/components/dashboard/` (all on `DashboardHub.module.css`, all site tokens — paper, hairline `--line`, one hard `12px 14px 0 var(--blue-soft)` offset shadow, mono eyebrows, Chakra Petch values, cobalt progress, no checkerboard):
      - `DashboardHeader` — compact `STUDENT DASHBOARD` eyebrow + a `clamp(26px,3vw,34px)` "Welcome back, {first}." + one muted line.
      - `StatStrip` — the KPI row: one connected paper plane, three hairline cells (Stages cleared `n / 6`, Participation `Individual` or `{team} (n)`, Season `{label}` + project title). Deliberately holds only metrics the card below doesn't, so nothing repeats.
      - `CurrentStageCard` — the "resume where you left off" panel (the one shadowed surface): `CURRENT STAGE` eyebrow + `NN / 06`, stage `name`, `coreQuestion`, a `You'll produce — {producesArtifact}` line, full-width progress bar + `label · %`, and the single primary `Continue {stage}` button. Journey-complete variant: "All six stages cleared." + a `Review your portfolio` button to `/dashboard/influence`.
      - `TeamCard` deleted (folded into `StatStrip`'s Participation cell). `DeadlineCard` (ink) stays in the right column as the hub's dark moment. Shell gap tightened `--space-10` → `--space-8`, section-heading `h2` stepped down to `clamp(19px,2.2vw,24px)`, `.shell` max-width `1200` → `1160` (matches the site container).
    - **Verified** the same way as the earlier Aug 29 passes: `tsc --noEmit`, `eslint src/`, and `next build` all clean; real authenticated Playwright screenshots (scratchpad-only Playwright + magic-link cookie, `rainaartist@gmail.com`, VERIFIED/INSIGHT-current) of `/dashboard` at 1300px + 430px — HTTP 200, no console errors; marketing pages unaffected. No repo deps, no schema/data changes. The "N" badge over the sidebar avatar in screenshots is the Next.js dev-mode indicator, not UI.
- **[Sep 1, 2026]** Dashboard hub pass, five owner-requested changes (all `src/components/dashboard/` + `src/components/journey/`, `tsc`/`eslint`/`next build` clean, verified with authenticated Playwright screenshots as `rainaartist@gmail.com`):
  - **`DashboardHeader` removed; `CurrentStageCard` removed.** Both folded into a new **`DashboardHero`** — after two iterations the owner landed on "make the hero like the **home page**, not the other pages." It's now styled after `JourneyHero` (not `SplitHero`): full-bleed band on `--canvas` with the `Checkerboard`, a mono masthead strip (`I³ WORKSPACE` — hairline — `NN / 06`), a bracketed eyebrow `[ Your innovation journey ]`, a Chakra Petch headline with a cobalt `.accent` word ("Welcome back, **{first}.**"), a hairline **ink** progress bar (`{label} · {pct}%` from the same `StageProgressState` 0/50/100 mapping, still the only progress read on the hub), the one primary `Continue {stage}` CTA, and a `{stagesCleared} of {total} · you'll produce {artifact}` line. Journey-complete variant: "All six stages / **cleared.**" → `Review your portfolio`. Own file `DashboardHero.module.css`.
  - **Hero visual slot: not the `Cube`.** Owner iterated: per-stage icon that floats/spins → "3d icons" → use the `i3-stage-icons-3d.html` mockup → bigger → cleaner → **"1 square, rotate 360°"**. Final **`StageEmblem`** keeps only the mockup's symbol set (INSIGHT focus-frame + spark, INVESTIGATE magnifier, IMAGINE bulb + rays, ITERATE loop arrows, IMPACT target, INFLUENCE megaphone, + a `COMPLETE` checked-ring; `.accent`=cobalt / `.coral`=coral classes, thin 2.4 stroke) — the extruded-slab body (back plate + four side walls + depth stack) was dropped. It's now a **single flat panel** (`.spin`, 158px, `--paper` face, light cobalt border), printed on both sides (front face + `rotateY(180deg)` back face, `backface-visibility: hidden`) so the symbol stays readable through a **continuous `rotateY` 360°** spin (14s linear); it passes briefly edge-on twice per turn, which is inherent to a flat panel doing a full turn. `.float` adds a translateY bob (4.4s alternate); soft radial ground shadow; both animations off under `prefers-reduced-motion` (static `rotateY(-14deg)`). **Structural nodes must be `<div>`, not `<span>`** (inline spans ignore width/height) and `.float` needs its own `transform-style: preserve-3d`. `Cube` import dropped from the hero (primitive untouched, still used on the homepage).
  - **`MentalTestCard` removed** from the hub (deleted). The stage-page `StageBrief` still shows the mental test as inline chips — that's a separate block.
  - **Six-stage `StageNodeTrack`**: no code change — `.nodeDone` (cobalt-filled dot) / `.nodeCurrent` (coral-filled dot + ring) already existed; they just now show because of the data change below. Same states mirrored in the `AppShell` sidebar (`DONE` cobalt / `CURRENT` coral).
  - **`SubmissionChecklist` + `ArtifactPanel` checklists are now interactive** — new **`StageChecklist`** client component: every Layer-3 "must include" item is a real checkbox with a running "`N of M done` / `M−N left`" tally + progress bar. **No per-item field in the schema** (that's the Phase 2 platform-evidence-model), so ticks persist per-browser in `localStorage` keyed `i3league.checklist.{STAGE}` — a personal planning aid, not a reviewer-visible record. Loaded in a rAF callback inside the effect (not synchronously) to satisfy `react-hooks/set-state-in-effect` and keep the first client render matching SSR. Replaced the static bullet lists in both `StageInfo.module.css` `.checklist` and `DashboardHub.module.css` `.reqList` (both rule sets deleted).
  - **Data (direct DB write, same as every admin action without a UI):** `rainaartist@gmail.com` — `StageProgress` set to INSIGHT + INVESTIGATE `COMPLETE`, IMAGINE `CURRENT`, rest `LOCKED`. (Her INSIGHT/INVESTIGATE have no `Submission` rows, so those stage pages still render the form+checklist branch, not `SubmissionSummary` — a quirk of marking complete without a real submission, not a bug.)
  - **[Sep 1, later] Small-window pass + em dashes.** Verified no horizontal overflow on `/dashboard` and `/dashboard/[stage]` at 360/600/768/900px (real Playwright). Tightened for narrow tiers: `StageNodeTrack` `.track` → 96px node columns + `scroll-snap-type: x proximity` + `overscroll-behavior-x: contain` at ≤480 (it's a horizontal-scroll strip below 720); hub `.shell` / hero `.hero` / `StageLayout .content` / `StageBrief .brief` paddings + a couple of heading `clamp()`s stepped down at ≤460–600; `StageChecklist` `.list` grid min changed to `minmax(min(100%, 240px), 1fr)` so it can't overflow a very narrow column; `AppShell` topbar breadcrumb now `min-width:0` + ellipsis with the hamburger/Help pinned `flex:0 0 auto`. **Em dashes removed from the two visible strings** on these pages (`DashboardHero` stage line `—` → `:`; `SubmissionChecklist` lead line rephrased). Em dashes remain in code comments only (house style, not rendered).
- **[Sep 8, 2026] Dashboard hub restyled to the marketing-page vocabulary.** Site owner asked the `/dashboard` hub to match Home / Curriculum / Pricing exactly — spacing, offset-shadow families, colour tokens, fonts, page layout. `DashboardHub.module.css` + `DashboardHero.module.css` rewritten against the marketing components (nothing invented — values lifted verbatim):
  - **Bug fixed:** the hub referenced `var(--space-7)`, which `globals.css` doesn't define (the scale is 1-6, 8, 10, 12, 16, 20), so `.nextAction` / `.checklistCard` / `.submission` / `.deadline` were rendering with **no padding**. Replaced with explicit values.
  - **Layout:** `.shell` now `max-width: var(--container)` (1160) + `var(--gutter)` inset, matching every marketing `.inner`; section rhythm via a `clamp(56px, 6vw, 76px)` stack gap; heads use the `10px 0 0` kicker→heading spacing and `--text-stage` Chakra Petch headings over 650/10px mono cobalt kickers, identical to `ThreePhases` / `TeamPricingSection` `.head`.
  - **Hero:** `DashboardHero` already used the shared `SplitHero`; dropped the old 880px `innerClassName` cap so it's full `--container` like `PricingHero` / `CurriculumHero`, matched their hero padding (`88px … 82px`), and gave the emblem's `.visual` the stat-card "family" treatment (paper, 1px `--line`, `8px 9px 0 var(--blue-soft)`) so it reads as the same widget type as `FrameworkCard` / `.statBox`.
  - **Shadow families, applied with restraint (not every surface):** node `.track` = connected paper + `8px 9px 0 var(--blue-soft)` (stat-card); `.nextAction` = the one loud surface, Pattern-C `20px 22px 0 var(--blue-soft), 0 38px 68px rgba(16,33,61,.12)` (coral-soft variant for revision-requested); `.checklistCard` / `.submission` = Section-17 standard panels, `--radius-lg`, no shadow; `.deadline` = the page's single ink "dark moment" — `24px 26px 0 #c8d7f7, 0 38px 70px rgba(16,33,61,.17)` + the faint `I³` watermark, byte-for-byte the homepage `FinalCta` / Pricing `PaymentSection` panel recipe.
  - `.submissionStatus` squared from a `999px` pill to `--radius-sm` (the marketing system has no pill containers).
  - **Left as-is (owner's earlier revert):** the current-stage node stays coral, `StageEmblem` keeps its idle spin. Neither is in the marketing vocabulary strictly, but both were explicitly reverted last session.
  - Verified with real authenticated Playwright screenshots (`rainaartist@gmail.com`, VERIFIED / IMAGINE-current) at 1440 / 1000 / 430px, plus `tsc` / `eslint` / `next build` clean. Note: between ~900-1144px viewport the `SplitHero` inner hits its own 900px breakpoint early because `AppShell`'s 244px sidebar narrows `main` — a squished-but-readable 2-column band; below 900 the sidebar becomes a drawer and the hero gets its proper single column.
  - **[Sep 8, later] Six owner-requested hub changes, all screenshot-verified (1440 / 1120 / 1000 / 430) + `tsc`/`eslint`/`next build` clean:**
    1. **`AppShell` sidebar is now always on screen — the off-canvas drawer is gone.** It has three widths: full 240px (wide, not manually collapsed), or a **76px icon rail** (manually collapsed via a new chevron toggle in the brand row, persisted to `localStorage` `i3league.sidebarCollapsed` behind a rAF so SSR isn't blocked; OR forced by `@media (max-width: 1180px)`, where the toggle is hidden since you can't expand below that). The rail hides every text label + the wordmark, keeps icons + stage numbers. Removed: `menuOpen` state, the Escape-key effect, the overlay div, the hamburger button, and all the `@media (max-width: 900px)` drawer CSS.
    2. **Deadline moved to directly under the hero** and restyled as a full-bleed **navy Pattern-D band** matching the homepage `CurriculumBridge` ("THE SIX-STAGE FRAMEWORK" bar) — `background: var(--ink)`, mono label + date + "N days remaining" in one row, soft `0 14px 30px` shadow (no offset plane / watermark).
    3. **`DashboardHero` "Your innovation: …" line** now uses the homepage hero lede treatment (`JourneyHero.module.css .sub` — `#53627a`, regular weight, `--text-lede` / 1.65) instead of ink + semibold.
    4. **Current-stage section** is a white (`--paper`) full-bleed band; `StageNodeTrack` rewritten from circular dots-on-a-line to a **row of plain boxes**, each just the stage number + name (taglines/connector line removed). Done = cobalt fill, current = coral outline, locked = 55% opacity.
    5. **"What's left" section** is a light-blue (`--blue-pale`) band below current-stage. New **`StageProgressPanel`** (replaces `StageChecklistCard` + `SubmissionStatusCard`, both deleted): one boxed white surface with the Pattern-C `20px 22px 0 var(--blue-soft)` shadow; **requirements count + progress bar + ready/not-ready status at the top**, then a **single-column** list of **square** checkboxes. Every row is a `<Link>` to `/dashboard/{slug}#stage-work` (the form, for built stages) or `#stage-brief` (the description, for the not-yet-built stages — `workspacePending`). New `id="stage-work"` wrapper + `id="stage-brief"` on `StageBrief` are the anchor targets. Ticks still come only from real saved data (`getStageChecklist`), so finishing a piece on the stage page updates the list + count on next hub load — no manual checkboxes.
    6. **"Next action" section** is a white band below "what's left"; `NextActionCard` restyled to the homepage **`FinalCta` Pattern-E card** — ink card, light eyebrow + headline + one line + one light-on-dark button, `24px 26px 0 #c8d7f7` shadow + faint `I³` watermark.
  - `DashboardHub.module.css` rewritten around full-bleed `.sectionPaper` / `.sectionBlue` bands (each with a `var(--container)` `.inner`), replacing the old single `.shell` column. `page.tsx` `StudentHub` reordered to: hero → deadline band → current-stage (white) → what's-left (blue) → next-action (white).
  - **[Sep 8, later still] Follow-up polish:**
    - **`StageNodeTrack` reverted to numbered circles on a connector line** (the boxes were rejected): 44px circles, cobalt fill = done, coral fill + `coral-soft` ring = current, hairline = locked, uppercase name beneath. Scrolls horizontally under 720px (a circle+line row can't reflow into a grid).
    - **`StageProgressPanel`**: the outer box and the checkbox squares are now **hard-cornered** (`border-radius: 0`); the trailing per-row **arrows were removed**; each row now takes a **full-width `--blue-pale` highlight on hover** (negative inline margin so it bleeds to the panel's inner edge), matching How It Works' `.step:hover` path treatment.
    - **`AppShell` sidebar reworked into a rail + flyout (later same day, per the UVA-SIS reference the owner shared).** There is no longer a "full sidebar in the grid" mode. The sidebar always rests as a thin icon rail (`--rail-w` 72px / 64 on phones); the grid's first column is always that width, so nothing about opening it moves the page. The rail's top shows a **menu button, not the logo**. Pressing it sets `.shellOpen`, which widens the sidebar to `--panel-w` (268px) as a **`position: fixed` overlay on top of the page** (`z-index` above a `rgba(16,33,61,.45)` scrim) and reveals the logo + full labels. The flyout closes on a nav-link click, the scrim, or Escape; it does not persist across loads. `localStorage`/`COLLAPSE_KEY` and the viewport-default logic are gone. Base CSS = the rail; `.shellOpen .*` rules = the expanded panel. Rail rows are 44px centred targets; a hairline separator stands in for the hidden "Your journey" label. In the open panel the Dashboard row and the stage rows are all full-width, left-aligned (`.shellOpen` resets the rail's `width: 44px; margin: 0 auto` to `width: 100%; margin: 0`).
    - **Current-stage circle track** (`.track` in `DashboardHub.module.css`) carries the same connected-plane treatment as How It Works' `WhoItsFor` grid — `1px rgba(16,33,61,.15)` border, straight corners, `20px 22px 0 var(--blue-soft), 0 38px 68px rgba(16,33,61,.12)` (→ `9px 11px 0` ≤820px) — and its fill (plus the `.nodeDot` circles) is `var(--paper)`, the same as the section it sits on, so the box reads only through its border + shadow. `.trackNote` ("N done · N left") has `margin-top: 44px` so it clears that offset shadow.
    - **Learn / Do / Show / Review tabs, gating each stage page.** Site owner asked every `/dashboard/[stage]` page to be split into four clickable tabs matching the curriculum's own four layers (`CURRICULUM_LAYERS` in `src/lib/stage-copy.ts`), each one required before the next unlocks. New generic `src/components/journey/StageTabs.tsx` (+ `.module.css`) renders the tab strip (numbered, mono-labeled, cobalt active underline, lock icon on a locked tab, check icon on a done one) and owns the sequencing: **Learn** is always open; **Do** unlocks once Learn is marked read (a "Continue to Do" button); **Show** unlocks once Do is marked done (a "Continue to Show" button, optionally gated further by a real prerequisite via `doReady`/`doNotReadyHint`); **Review** unlocks only once a real final `Submission` exists (`submitted` prop) — the one gate backed by actual data, not self-reporting. Learn/Do have no per-lesson completion model (unchanged Sprint-4 scope decision), so their "done" flag is saved per browser in `localStorage` (`i3league.stageTabs.{STAGE}`), the same personal-planning-aid pattern `StageChecklist` already uses — not graded, not reviewer-visible. Wired into both built stage pages (`src/app/dashboard/[stage]/page.tsx`): **Learn** = `LessonList`, **Do** = `PracticeActivityList` (+ `SafetyScreeningForm`/`SafetyScreeningReview` for INVESTIGATE, whose presence is INVESTIGATE's real `doReady` gate), **Show** = the stage's guided form (`InsightForm`/`InvestigateForm`) + `SubmissionChecklist`, swapping to the read-only `SubmissionSummary` once complete, **Review** = `StageReviewNote`. `StageBrief` (core question/artifact/mental-test chips) stays above the tabs, page-level, since it applies to the whole stage rather than one layer.
    - While rebuilding this, noticed and fixed four pre-existing em dashes in real (non-comment) seed content — `prisma/seed.ts`'s INSIGHT/INVESTIGATE `Lesson.bodyMarkdown`/`Assignment.instructions` strings and two of Maya's demo `JournalEntry.text` rows — violating the Sep 8 2026 "no em dashes in user-visible content" rule; these had shipped before that rule existed. Fixed both in `seed.ts` (so future reseeds are correct) and, since re-seeding doesn't overwrite already-created Lesson/Assignment/JournalEntry rows (see the Aug 19 idempotent-seed note: they're guarded by a count/find check, not upserted), with a one-off direct `updateMany` against the live rows so the fix is live immediately, not just in the seed source.
    - **Verified end-to-end against the live Supabase project**: a throwaway verified STUDENT fixture (Admin API `createUser` + direct Prisma writes for `Student`/`Enrollment`/`Payment(VERIFIED)`/`StageProgress`, since the UI registration flow wasn't needed for this check) walked through Learn → Do → Show → final-submit via real Playwright clicks and form fills; confirmed Review stayed disabled the entire time until the final submit landed, then unlocked with all four tabs checked, the sidebar's stage indicator advanced to INVESTIGATE, and the Show tab correctly swapped to the read-only `SubmissionSummary`. Also checked the tab strip at a 390px mobile width (horizontal-scrolls cleanly, no layout break). Fixture and its auth account fully deleted afterward (confirmed 0 remaining `tabtest-` rows). `tsc --noEmit` and `eslint src/` both clean throughout.
    - **[Sep 8, 2026, later] Stage-page restyle to match a client-provided mockup.** Site owner shared an HTML mockup of a single-scroll `/dashboard/[stage]` layout (big stage hero with a "stage at a glance" side panel, a Learn/Do/Show/Review "layer progress" strip, module rows, an ink "artifact card" for the submission checklist, a "gate card" for review). Confirmed with them up front: keep the Learn→Do→Show→Review click-gating from the same day's earlier entry (don't flatten it into one always-visible scroll like the mockup) — just reskin the tabs and panels to match this visual language.
      - **New `StageHero`** (`src/components/journey/StageHero.tsx` + `.module.css`) replaces both the old plain title/description header in `StageLayout` and the separate `StageBrief` component (deleted). One editorial block: eyebrow (`Stage 0N of 06`), big title, core question, description, a "stage at a glance" side panel (producesArtifact + real lesson/activity counts + first 3 submissionChecklist items as a bullet list), and a bottom bar with the mental-test chips + a status badge (`Stage in progress` / `Stage complete`). Everything is real data from `stage-copy.ts` and real `curriculumModule.lessons/assignments` counts — nothing invented to match the mockup's shape. `StageLayout` simplified accordingly (dropped `eyebrow`/`description` props, keeps `title` only for the topbar breadcrumb).
      - **Watermark gotcha worth remembering**: a large decorative background numeral positioned `position: absolute` inside a padded flex/grid parent needs its inset (`top`/`right`) set to match the parent's own padding (`var(--space-8)`), not `0` — `0` measures from the padding *edge* while sibling grid content starts *inset by* the padding amount, so the numeral rendered 32px above/right of the sibling panel it was meant to sit behind, poking out as a distorted fragment. Fixed by setting `top`/`right: var(--space-8)` to match.
      - **`StageTabs`** tab-strip restyled to the mockup's "layer progress" look: number stacked above the label (was side-by-side), current tab gets a coral bottom bar (was cobalt), a done-but-inactive tab keeps a cobalt bottom bar — matching the mockup's `.layer-step.current`/`.done` distinction. Gating logic itself untouched.
      - **`SubmissionChecklist`** (Show tab) restyled into the mockup's ink "artifact card" — dark surface, kicker + artifact name + format line, then the existing `StageChecklist` in a new `variant="dark"` mode (light-on-ink checkbox/text colors, reusing `#c6d0df` from `FinalCta`'s existing muted-on-ink convention rather than inventing a new hex). This is the one ink "signature dark moment" on the page, consistent with the rest of the app's design system.
      - **`StageReviewNote`** (Review tab) gained a checkmark "gate checks" list rendering `copy.reviewerChecks` — a `StageCopy` field that already existed in `stage-copy.ts` but was never actually displayed anywhere until now. No new content invented; just surfaced existing real data to match the mockup's gate-card checklist.
      - **`LessonList`** (Learn tab) gained a numbered badge per lesson (`01`, `02`, …) matching the mockup's module-row numbering, via a new `.moduleItem`/`.moduleNum` grid in `ContentBlocks.module.css`. Lesson content itself (full teaching-note body) is unchanged — the mockup's short one-line module teasers don't apply since our lessons show full real content, not a link out to somewhere else.
      - **Verified** with real authenticated Playwright screenshots at 1440px and 390px mobile (`rainaartist@gmail.com` plus a throwaway fully-submitted fixture, cleaned up after), across all four tabs including a real final-submit unlocking Review. `tsc --noEmit`, `eslint src/`, and `next build` all clean.
    - **[Sep 8, 2026, later still] INSIGHT rebuilt to the real company curriculum docs, field-for-field.** Site owner handed over the actual Stage 1 documents (`docs/curriculum/01_LEARN/i3league-stage1-insight-notes.md`, `02_DO/i3league-stage1-activities.md`, `04_REVIEW/i3league-stage1-rubric.md`, `05_BUILD/i3league-stage1-worksheet-pack.md` + `i3league-stage1-submission-reviewer.md`) and asked for all of it, as exactly as possible. This is the first stage built to full depth rather than the earlier generic 2-lesson/1-assignment placeholder content.
      - **LEARN**: `prisma/seed.ts`'s INSIGHT lessons went from 2 generic readings to the real 5 topics (Observation Skills, Empathy Without Assumption, Problem Scope, Ethical Interviewing & Consent, Team Charter), each with all 8 sub-sections (What is it / Why it matters / Example / Common mistake / Try this / Key takeaway / Think about it / Use this in your project) verbatim from the notes doc. `LessonList` gained a generic `renderParagraph` helper that bolds a leading `Label:` on any paragraph matching that pattern — a real, reusable enhancement (not INSIGHT-specific), backward compatible with any lesson that doesn't use the convention.
      - **DO**: the 5 real named activities (3-Day Friction Hunt, Empathy Interview, Scope Narrowing Drill, Consent Script Practice, Team Agreement Session) replaced the single placeholder assignment, each naming its worksheet and what it produces, from the activities table.
      - **Live content refresh gotcha**: `prisma/seed.ts`'s Lesson/Assignment creation is guarded by a `count > 0` check (idempotent-seed pattern from Aug 19), so editing the seed script alone doesn't update rows that already exist in the live DB. Had to `deleteMany` the INSIGHT module's existing Lesson/Assignment rows via a one-off script before re-running `prisma db seed` — same pattern as the Sep 8 em-dash content fix. Worth remembering for any future stage content rewrite: editing `seed.ts` only affects fresh databases; a live one needs its stale rows cleared first.
      - **SHOW — the Insight submission form was fully rewritten** to match Part A of the submission-reviewer doc section-for-section: Section 1 (problem statement + who/where/when/why, with a live non-blocking "solution detector" warning that fires if the problem statement contains words like "app"/"platform"/"AI system"/etc, per the doc's own described platform check), Section 2 (3–5 observations), Section 3 (firsthand evidence: participant description, connection, what was learned, evidence type, the evidence itself, and 4 Responsible Research Confirmation checkboxes), Section 4 (Before/Now/What caused the change), Section 5 (why the scope is manageable), Section 6 (Team Confirmation — 5 checkboxes, rendered only when `isTeamProject` is true, read off the same `teamMembership` query the page already made), Section 7 (optional evidence notes). `InsightContent` (`actions.ts`) and `saveInsightAction`'s validation grew to match — every Section 1–5 field plus the 4 confirmation checkboxes (and the 4 team checkboxes, on a team project) are required on final submit, Section 7 stays optional. `SubmissionSummary`'s field list and `stage-checklist.ts`'s dashboard-hub checklist (`getStageChecklist`) were both updated off the new field names.
      - **Two deliberate simplifications from the doc**, both commented in `actions.ts` right on the `InsightContent` type: Section 2's "select 3–5 from your saved Friction Log entries" became one free-text field (no separate persisted Friction Log/observation-card entities exist yet), and Section 6's "Team Charter status, read from the saved charter" became a self-reported checkbox (no separate Team Charter entity exists yet). Both are the same deferred **platform-evidence-model** (evidence IDs, structured saved worksheet fields, system-assembled submissions) already flagged Phase 2 in memory — building that properly is a real architectural project, not something to improvise mid-form-rewrite.
      - **REVIEW**: `stage-copy.ts`'s INSIGHT `submissionChecklist` and `reviewerChecks` were re-aligned to the exact wording of the doc's "Final Submission Check" (10 items, first-person) and "Reviewer checks" (8 items) lists; `advancesWhen` tightened to the doc's exact ADVANCE line, and a new `revisesWhen` field added (REVISE line), shown in `StageReviewNote` when present. New optional `StageCopy.detailedRubric` type (`gateQuestion`, `notJudging`, `nonNegotiables`, and 6 `dimensions` each with its own `checks`/`note`/4 rating-level descriptors) populated for INSIGHT from the full rubric doc — the first stage with this depth; every other stage still only has the plain `REVIEW_DIMENSIONS` name list. New `StageRubricDetail` component renders it as one `<details>` disclosure per dimension (no client JS needed) below `StageReviewNote` in the Review tab, only when `detailedRubric` is present, so nothing changes for the other 5 stages.
      - **Explicitly not built** (Sprint 6 / Judge Portal territory, per CLAUDE.md's Deferred section, not something to improvise here): Part B ("Reviewer View" evidence snapshot), Part C (the reviewer-facing scored rubric form), Part D (automated gate logic / auto-advance-or-revise / the Evidence-ID system tying claims to evidence across stages). Today's real gate is still what it's always been in this codebase: a student's own final-submit calls `completeStage()` directly — there is no reviewer role or scoring step yet for any stage.
      - **Verified end-to-end against the live Supabase project**: a throwaway individual student fixture walked the real rewritten form — confirmed the solution-detector warning fires on "Students need an AI app..." and clears once reworded, filled and submitted all required Section 1–5 fields plus the 4 confirmation checkboxes, confirmed Section 6 (team confirmation) correctly does not render for an individual project, confirmed the post-submit `SubmissionSummary` shows every new field correctly, and expanded the new rubric disclosure (Evidence Quality) in the Review tab to confirm its checks/note/4 levels all render. Checked at 390px mobile too — no overflow. Fixture cleaned up after (0 remaining `tabtest-` rows). `tsc --noEmit`, `eslint src/ prisma/seed.ts`, and `next build` all clean.
      - **Not done this pass**: the team-confirmation path (Section 6) was verified by code review and the individual-path negative case (correctly hidden), not by walking an actual team fixture through a real submit — worth a real team-fixture pass before this ships to real users, given the extra required-field branch in `saveInsightAction`.
    - **[Sep 8, 2026, later still] Reorganized by task instead of by layer.** Site owner reviewed the Learn/Do/Show/Review tabs and asked for a different structure: each topic's Learn + Do + Show grouped together (so everything about "Empathy Without Assumption," say, is in one place), with Review kept separate and reachable only once the whole stage is submitted. This replaced the layer-based tabs built earlier the same day, not an addition on top of them.
      - **`StageTabs` simplified**, not just relabeled: it now takes a `tasks` array (each `{id, label, content}`) plus one `reviewContent`, rather than four hardcoded named slots. Every task tab is open from the start — there's no reason to force-gate independent topics against each other — only Review is locked, by the same real `submitted` (final-Submission-exists) signal as before. This also **deleted the old localStorage learn/do progress-tracking** (`i3league.stageTabs.*`) entirely; it's no longer needed now that tasks aren't sequentially gated.
      - **Real bug found and fixed while building this**: `StageTabs` used to conditionally render only the active tab's JSX (unmounting the others). Once a tab could hold live, uncontrolled form inputs (a task's Show fields), that would silently discard whatever the student had typed the moment they switched to another tab and back, since the input would remount from its original `defaultValue`. Fixed by always rendering every tab panel and toggling visibility with the `hidden` attribute instead. That in turn surfaced a second, subtler bug: `.panelInner { display: flex }` in `StageTabs.module.css` was overriding the `hidden` attribute's UA-stylesheet `display: none` (author CSS beats UA defaults regardless of selector specificity), so every task rendered stacked at once instead of only the active one. Fixed with an explicit `.panelInner[hidden] { display: none }` override — worth remembering for any other component that both sets its own `display` and relies on the `hidden` attribute.
      - **New `TaskSection`** (`src/components/journey/TaskSection.tsx`) — the per-topic Learn/Do/Show wrapper (`doItem` optional, for a topic with no separate activity). **New `LessonPanel`/`ActivityPanel`** (`src/components/journey/`) extracted from the now-deleted `LessonList`/`PracticeActivityList` as single-item renderers, since content is now grouped one-lesson-per-task instead of one list-of-all-lessons-per-layer. Lesson/Assignment ↔ topic pairing is positional (seed order), same simplification already used elsewhere; `lessonAt(lessons, i)` / `activityAt(assignments, i)` helpers in `page.tsx` do the lookup with a graceful fallback if a slot is empty.
      - **INSIGHT**: `InsightForm` now owns its own internal `StageTabs` (5 task tabs — Observation Skills, Empathy, Problem Scope, Consent, and Team Charter when `isTeamProject` — plus Review), since all 5 topics share one `<form>`/server action and the tab UI has to live inside that same client component. The submission-checklist ink card, optional evidence notes, AI disclosure, and Save Draft/Submit buttons sit **outside** the tabs as a persistent footer beneath whichever task is active — they're cross-cutting, not owned by one topic. The already-complete (read-only) case now renders the 5 tasks directly via the top-level `StageTabs` in `page.tsx` instead, each showing a `SubmissionSummary` filtered to that task's own fields; AI disclosure + submitted date + evidence notes moved into the Review tab's content, once, instead of being repeated per task.
      - **INVESTIGATE**: split across 2 tasks that are genuinely two different forms/server actions (`InvestigateForm`'s own `<form>`, and `SafetyScreeningForm`'s own separate `<form>` — HTML forms can't nest), so here `page.tsx` owns the top-level `StageTabs` directly rather than either form owning it. `InvestigateForm` wraps just its own fields in one `TaskSection`; the safety-screening task is a second, sibling `TaskSection`. The "complete the safety screening above" notice was reworded to "see the Safety Screening tab" since "above" no longer means anything once they're separate tabs.
      - **Verified end-to-end against the live Supabase project**, including the tab-switch/input-loss bug specifically: typed into Task 1's field, switched to Task 3, typed there, switched back to Task 1, and confirmed the original text was still there (this is what caught the bug before it shipped). Walked a full 5-task fill-and-submit on a throwaway individual fixture, confirmed Review unlocked with all the right content and each task tab now shows its own read-only recap. Separately walked INVESTIGATE's 2-task layout (advanced a fixture directly to INVESTIGATE via a direct `StageProgress` write) and confirmed both tasks render, the safety-screening notice text is correct, and the submission checklist shows on both tabs (it's a page-level persistent element, not owned by either task). Checked 390px mobile width for horizontal overflow (none). All fixtures and auth accounts cleaned up after (0 remaining `tabtest-` rows each time). `tsc --noEmit`, `eslint src/`, and `next build` all clean throughout; had to clear the `.next` Turbopack cache once mid-session after a module-resolution error kept referencing an already-deleted file.
    - **Sidebar logout is now a profile menu.** The always-visible logout button is gone. The profile bar at the bottom is a `<button>` (`handleProfileClick`): in the rail it opens the flyout and the menu at once; in the open flyout it toggles a "Logout" row that appears *above* the bar, separated by a hairline, with the chip's chevron rotating. Also fixed the long-standing bottom-anchoring bug — `.sidebar` now has `height: 100dvh` and `.nav` is `flex: 1 1 0`, so the profile bar actually sits flush at the bottom instead of floating mid-panel.
    - **[Sep 21, 2026] Stage sections moved into the sidebar, box removed, Back/Next paging.** Site owner asked for the per-topic tabs (Observation Skills, Empathy, Problem Scope, Consent, Review) to lose the outlined box and to live in the left panel, with next buttons to move between them.
      - `StageTabs` (`src/components/journey/StageTabs.tsx`) no longer renders a top tab strip or a bordered content panel. It portals its section list into the sidebar, under the current stage's row, and renders a Back / "Next: {section}" pager at the bottom of each section. Review stays locked (lock icon in the sidebar, no Next from the last topic) until a real final `Submission` exists.
      - Wiring: new `src/components/app/StageNavContext.tsx`. `AppShell` renders an empty slot `<div>` inside the active stage's `<li>` and exposes it (plus `closeMenu`) through the context; `StageTabs` uses `createPortal` into it. A portal keeps the tab state next to the panels it controls and avoids an effect-based registration step (the repo's `react-hooks/set-state-in-effect` lint rule). Sub-item styles live in `AppShell.module.css` (`.subList`/`.subItem`...) since they must respond to the rail vs. open-flyout states; `StageTabs.module.css` now only holds the content column and pager. In the rail the sections show as small numbers under the stage number; in the open flyout they show as indented labelled rows.
      - Unchanged on purpose: every panel stays mounted and is hidden with the `hidden` attribute, so typed answers survive moving between sections (re-verified: typed in Empathy, moved Next then Back, text still there).
      - Verified with authenticated Playwright (`rainaartist@gmail.com`, read-only): sections listed under Insight in the flyout and rail, Next/Back move between sections, Review locked before submit, no horizontal overflow at 1440 and 390, no console errors. `tsc`, `eslint src/`, `next build` clean.
    - **[Sep 21, 2026, later] Answer boxes emptied of instructions, example boxes squared, optional Evidence notes box removed.**
      - `Input` and `Textarea` gained an optional `hint` prop, rendered as normal text between the label and the box (linked via `aria-describedby`). The INSIGHT and INVESTIGATE stage forms' `placeholder` text (observation guidance, the problem-statement pattern, the participant example, the research and "what changed your mind" prompts) moved into hints, so every answer box now starts empty. Registration form placeholders were left alone (not stage answer boxes).
      - The lesson "example" callout (`.example` in `ContentBlocks.module.css`) is now a white rectangle with a hairline border and square corners instead of a rounded blue-pale block.
      - **Evidence notes free-text box removed.** Checked `docs/curriculum`: "Evidence Notes" is part of the stage's required artifact name (Validated Problem Statement + Evidence Notes), but that artifact is made of the observations, firsthand evidence, and reflection fields the form already collects. The separate "Anything else your reviewer should know?" box is Section 7 of the reviewer doc and is explicitly optional ("not required, to avoid filler"). It had been wired in as a REQUIRED field (client `REQUIRED_TEXT_KEYS` and the server final-submit check), contradicting the earlier "Section 7 stays optional" note, so a student could not submit without filling an optional box. Removed from `InsightForm`, `saveInsightAction` and the `InsightContent` type, the hub checklist (`stage-checklist.ts`), and the summary. Old saved JSON that still has an `evidenceNotes` key is harmlessly ignored. Do not re-add it as a required field.
  - **[Sep 21, 2026, later still] Insight (and every stage page) restyled to the new `docs/design-system.md` Section 21.** `StageHero` now sits on the shared `SplitHero` shell (checkerboard canvas band, mono eyebrow, display H1, muted lede) with a Pricing-style stat box (big cobalt stage number, artifact name, 64 x 4 rule, lesson/activity caption); the old "3 checklist bullets" list was dropped from the box since the checklist already shows those items. It also fixed two undefined tokens (`--space-7`, `--space-9`) that had left the old hero panel with no padding. `StageLayout` gained a `hero` prop and wraps page content in a full-bleed paper band (900px reading column) under the hero, matching the alternating-band anatomy. Back/Next pager now uses the shared `Button` (ghost / primary) instead of hand-rolled buttons. Learn/Do/Show labels and review labels are cobalt mono kickers; lesson, review and checklist body text moved to the Section 21.3 sizes and `--muted`; the INSIGHT rubric disclosures use the FAQ accordion recipe (paper list, stat-box shadow, drawn plus/minus); the INVESTIGATE submission-checklist card is the ink panel recipe (`24px 26px 0 #c8d7f7`, `I³` watermark). These are shared components, so INVESTIGATE and the placeholder stages change too. Verified with authenticated Playwright screenshots of `/dashboard/insight` at 1440 and 390px (no horizontal overflow, no console errors) and the Review panel; the INVESTIGATE ink card was not viewed live because that stage is locked for the test account. `tsc` and `eslint` clean.
  - **[Sep 21, 2026, single-page sections] All stage sections now live on one scrolling page instead of Back/Next pages.** `StageTabs` was replaced by `StageSections` (`src/components/journey/StageSections.tsx`) and a new `StageBand` (full-bleed band, 900px reading column). Bands alternate paper (white) and canvas (light blue) like the marketing pages; the hero above is canvas, so the first band is paper (`startTone` prop). The sidebar section list is now scroll links with a scroll-spy highlight (IntersectionObserver). Review is only rendered once a real final Submission exists (its sidebar entry stays locked until then). `StageLayout` no longer wraps content (its `.band`/`.content` css was removed); pages and forms supply bands, and the placeholder stages wrap in one `StageBand`. `InsightForm` is one `<form>` with a `.bands` (block) wrapper: project band, five section bands, then a footer band (AI-use disclosure, errors, Save draft / Submit). INVESTIGATE uses the same component (its ink checklist card is the footer band). The AI-use disclosure is now a coral box (coral-soft fill, coral hairline, `8px 9px 0` coral offset, square). Verified with authenticated Playwright at 1440 and 390px (no overflow, no console errors) and a live click test of the sidebar links. `tsc` and `eslint` clean.
  - **[Sep 21, 2026, paged again + autosave] Reversed the single-page layout: each section is its own page again** (Back/Next pager and sidebar links), still as full-bleed bands under the stage hero. `StageSections` is now the pager (all pages stay mounted and `hidden`, so one shared `<form>` keeps every field). **Insight's Review page is never locked and now holds the AI-use disclosure (coral box) and the Submit Insight button**; Save draft is gone. Project title and category moved onto the Problem Scope page. **Autosave**: `InsightForm` sends the whole form to the new `autosaveInsightAction` (`actions.ts`) 900ms after any change and immediately on blur, one save in flight at a time, with a status line in the pager row ("Saving..." / "All changes saved"). `saveInsightAction` and `autosaveInsightAction` share `persistInsight()`; autosave is always a draft, never redirects, never writes the AI disclosure (recorded only on final submit, so the AI answer is not restored after a reload), and sends a chosen photo once instead of on every save. **INVESTIGATE was not converted**: it still has its own Save draft button, keeps AI disclosure + Submit in its first section, and its Review stays locked until it is submitted (`reviewLocked` prop). Verified with a throwaway student in a real browser: autosave wrote after the pause and on blur, typed text survived moving between pages and a reload, Review showed Submit + AI box and no Save draft, and a full fill-and-submit set `isFinal`, completed Insight and unlocked Investigate. Fixture fully deleted afterward. `tsc` and `eslint` clean.
  - **[Sep 21, 2026, layer bands] Learn, Do and Show are now separate full-bleed bands on each stage page, alternating paper / canvas / paper**, with the section heading at the top of the Learn band and the Back / Next pager in its own canvas band below. `TaskSection` now builds those bands itself (using `StageBand`); `StageSections` only wraps Review in a band, and `InvestigateForm` wraps its AI disclosure and buttons in a canvas `StageBand`. Also this session: definitions in lessons are highlighted inline with `--paper-warm` (the "Not ready to submit" badge color), the section number sits in a boxed heading, the lesson example box is blue-pale with an offset shadow, Back is the strong ghost button with vertical-only hover, and all of it is documented in `docs/design-system.md` Section 22. `tsc` and `eslint` clean; verified in an authenticated screenshot of `/dashboard/insight`.
  - **[Sep 21, 2026, page bar] Navy page bar under the stage hero.** `StageSections` now renders a full-width ink bar (`#stage-bar`, Pattern D) listing every page of the stage as clickable numbered chips (`01 Observation Skills`, `02 Empathy Without Assumption`, ... `Review`) with the open page highlighted as a paper chip; locked Review is dimmed. It reads the same `active` state as the sidebar list. `StageSection` gained an optional `title` (full page name) beside the short sidebar `label`. Applies to every stage page that uses `StageSections` (Insight and Investigate). Documented in `docs/design-system.md` Section 22.1. Verified with an authenticated Playwright click test; `tsc`/`eslint` clean.
  - **[Sep 21, 2026, Insight content completeness] The Insight page now carries the full Stage 1 outline (guiding question, all five sections' Learn/Do/Show, the final submission, and the closing reflection).** Learn shows all eight labeled blocks per lesson (What is it? / Why does it matter? / Example / Common mistake / Try this / Key takeaway / Think about it / Use this in your project); the wording is the seeded curriculum text, with the company's em dashes replaced by commas per the project rule. Per-section Do detail (activity, worksheet, output, artifact contribution) and Show "Must include" lists are static data in `src/lib/stage-copy.ts` (`INSIGHT_SECTION_DETAILS`, and `StageCopy.finalSubmission` / `guidingQuestion`), not database rows, so no reseed is needed; `ActivityPanel` shows them instead of the DB assignment text for Insight. Review shows the output rollup, Final Stage 1 submission (artifact, format, Must include, minimum evidence standard, advancement check) and, after the rubric, the closing reflection (`StageFinalSubmission.tsx`). The hero shows "Guiding question: Whose problem is this, and how do I know?" (the dashboard hub still uses `coreQuestion`, "Is this a real problem?"). Verified by checking ~90 phrases from the outline against the rendered pages in an authenticated browser; the Team Charter section is not rendered for individual projects, so it was checked in code only.
  - **[Sep 21, 2026, lesson headings] Lesson subheadings trimmed.** Only Example, Try this, Key takeaway, and Use this in your project keep a visible heading in Learn; What is it?, Why does it matter?, Common mistake, and Think about it show their text with no heading (the text itself is unchanged and all still present). Controlled by `heading: null` per entry in `LESSON_SECTIONS` in `LessonPanel.tsx`; Section 22.2 of `docs/design-system.md` updated. `tsc`/`eslint` clean.
  - **[Sep 21, 2026, editorial split redesign] The stage pages were rearranged into an editorial split, with the same content.** Every Learn / Do / Show band is now a full-container two-column layout: a narrow sticky left **rail** (Learn: kicker + boxed number + section title; Do: kicker + activity name; Show: kicker + the section's "Must include" list) and a wide content column. Lessons read as: large highlighted definition, why it matters, the example box, the common mistake, Try this and Use this side by side, the key takeaway as a pull-quote, then the "think about it" question; the duplicate lesson title is gone (the rail heading is the title). The Do worksheet / output / contribution is a connected three-cell strip. The Review page is now one railed band per topic (Your submission when read-only, Final submission, How it's reviewed, Full rubric, Closing reflection, Submit) via the new `StageReviewPage`; `StageFinalSubmission`, `StageReviewNote`, `StageRubricDetail`, and the closing reflection now render content only, and long lists run in two columns. `StageBand` gained `rail` and `wide` props and a `RailHeading` helper; `TaskSection` gained `doTitle`. Verified by re-checking the outline's phrases against the rendered pages (all present) and authenticated screenshots at 1440 and 390px. Documented in `docs/design-system.md` Sections 22.1, 22.2, and 22.4.
  - **[Sep 21, 2026, rail polish] Stage page rail refinements.** Removed the boxed section number from the Learn rail; every rail title (Learn section title, Do activity name, `Must include`, and the Review band titles) is now one size (`clamp(22px, 2.4vw, 28px)`), so `RailHeading` no longer takes `number`/`small` and `TaskSection` no longer takes `number`. "Try this" and "Use this in your project" lost their headings and are plain paragraphs (Example and Key takeaway keep theirs). The Do band's Worksheet / Output / Artifact contribution boxes, and the Review page's required-artifact / format boxes, now use the Section 21.5 connected-grid box format: hard `20px 22px 0 --blue-soft` offset plane, soft grounding shadow, `--blue-pale` hover, and the cobalt underline accent. Documented in `docs/design-system.md` 22.1, 22.2, 22.4. Verified in an authenticated browser (heading sizes read as 28px; the hovered cell computed `rgb(237, 242, 252)`); `tsc`/`eslint` clean.
  - **[Sep 21, 2026, save status] The autosave status now reports whether the page is saved, and is centered on the pager row.** States: `Saved` (also a freshly opened page), `Not saved yet` from the first edit until the save runs, `Saving...`, and `Not saved. Check your connection.` (coral) on failure; edits made while a save is running stay "Not saved yet" and trigger another save. The pager is a `1fr auto 1fr` grid so the status is centered on the row regardless of Back/Next widths (stacks under 600px). `StageSections` gained `pagerNoteTone`. Verified with a throwaway student: the note's center matched the row's center exactly (756px), and the text went Saved, Not saved yet, Saved; fixture deleted. Documented in `docs/design-system.md` 22.3.
  - **[Sep 21, 2026, Insight page order] Consent now comes before Empathy.** Students were told to interview someone (Empathy) two pages before learning how to do it ethically (Consent). Insight's pages are now Observation Skills, Consent, Empathy, Problem Scope, then Team Charter on a team project. **Consent is Learn + Do only** (no Show step, no fields): the consent script is written before the interview, and its Do footer says the student confirms they followed it on the Empathy page. **The four Responsible Research Confirmation checkboxes moved to Empathy's Show page**, right after the Evidence field, because they describe that interview's evidence. The saved fields (`consentGiven`, `permissionToUse`, `privacyRemoved`, `rulesFollowed`) and the required-to-submit rules are unchanged, so no data migration. Reworded the Empathy Do description to start "Using your consent script from the previous page" and moved the consent-confirmation Must include item to Empathy.
    - **Gotcha for future sessions:** `INSIGHT_SECTION_DETAILS` (`src/lib/stage-copy.ts`) is now in **page order**, but the seeded `Lesson`/`Assignment` rows are in the company's original order (Observation, Empathy, Scope, Consent, Team). `src/app/dashboard/[stage]/page.tsx` therefore pairs a lesson/assignment by its own index and the details by their page-order index; they differ for Consent, Empathy and Scope. Reordering one without the other puts the wrong activity under a heading (this happened once mid-change and was caught in a screenshot).
    - `StageSection` gained an optional `pagerTone` (a page with no Show band ends on a canvas Do band, so its Back / Next row is paper), and `TaskSection`'s `show` is now optional. The hub's "Finish Insight" checklist follows the new order (the confirmations row is grouped under Empathy). Also this session: the open stage page is kept in the URL (`?page=empathy`), the Learn section is a numbered connected plane, Show pages are one column, and Investigate's copy still promises a "Research Brief" its form does not collect (open, not fixed).
  - **[Sep 21, 2026, Review page answers + calmer rubric]** The Review page now opens with one boxed **"Your answers"** band holding everything the student wrote for the stage, grouped by the page it came from (Observation Skills, Empathy, Problem Scope, Team Charter on a team project), plus the photo link and, once submitted, the AI-use disclosure and submitted date. New presentational `SubmissionAnswers` (`src/components/journey/`) and `src/app/dashboard/[stage]/summary-sections.ts` (section builders shared by the server page and the client form). On the live Insight form the box mirrors the form **as typed right now** (`InsightForm` re-reads the whole `<form>` on every change), so it is correct before autosave lands; once submitted it is built from the saved `Submission`. Investigate uses the same component (one section). `StageReviewPage` gained `startTone` so bands keep alternating under the answers band. The per-page read-only recaps in a completed stage were left as they were. The rubric band is quieter: the three non-negotiables are one line, each dimension opens to plain bullets plus its note, and the four level descriptions are behind a nested "See the four levels" toggle; "Reviewer checks" uses the same plain bullets (the ruled check-list styles were removed). Not shown live: a photo chosen but not yet autosaved. Verified with authenticated Playwright (draft and submitted states, 1440 and 390px, no overflow or console errors); `tsc`/`eslint` clean.
  - **[Sep 21, 2026, Review page order and de-duplication]** Review bands are now: Your answers → How it's reviewed → Full rubric → Closing reflection → Submit (live Insight only). Removed the "Final Stage N submission" band (it restated the hero's format and the Submit band); its one unique fact, that worksheets are not uploaded, is a footnote under the answers box (`insightKeepNote`). "Reviewer checks" shows only for a stage without a full rubric (the rubric asks the same questions per dimension). The closing reflection is one sentence that names the guiding question outright (owner preference over pointing at the hero); `finalSubmission.closingReflection` is now `{ text }` and `StageFinalSubmission.tsx` became `StageClosingReflection.tsx`. The "Feedback, revise, resubmit" line is hidden in How it's reviewed while a Submit band follows (`submitBelow`), since Submit says it. Verified in an authenticated browser (draft and submitted states): each of those lines appears once. `tsc`/`eslint` clean.
  - **[Sep 21, 2026, Submit gating and "what's left" popup]** The closing reflection now sits directly above the Submit button (inside the Submit band, so the live Insight review no longer has a separate reflection band; a submitted stage still does). The Submit button never submits until every required item is done: it is `type="button"` + `aria-disabled` (new `.button[aria-disabled="true"]` style in `Button.module.css`; native `disabled` was avoided because it blocks the click that has to open the popup). Selecting it while unfinished opens `MissingItemsDialog` (native `<dialog>`, `showModal`): every unfinished required item grouped by page, each linking to its exact field. `StageSections` gained a `ref` handle (`goTo(pageId, fieldName)`) that opens the page and focuses `[name=fieldName]`; `InsightForm` owns the ordered `REQUIRED_ITEMS` list (label, page, field) and the AI-use item (focuses whichever of tool / purpose is empty). The server still re-validates on a real submit. Verified in an authenticated browser: empty draft lists the items and saves nothing, links land on the right page with the right field focused (Evidence, Project title, AI tool), Escape closes, and a complete draft turns the button into a real submit; `tsc`/`eslint` clean. Investigate's own Submit was not changed.
  - **[Sep 21, 2026, alternation fix and Submit hint]** Verified every stage page's bands by computed background (all visible top-level bands, including the Back / Next row and the Investigate footer). Three places had two same-tone bands in a row and were fixed with `pagerTone: "paper"`: the Review page's Back row (a canvas Submit band or reflection sits above it; `StageSections` now sets this for Review itself) and both Investigate pages (research, whose Submit band is canvas, and safety, which has no Do band). All pages now alternate. The hint under Submit now reads "N required items left. Select Submit Insight to see where." and is linked to the button with `aria-describedby="submit-hint"`. When adding a band or page, re-check that neighbours differ.
  - **[Sep 21, 2026, Submit moved into the pager row]** The Submit Insight button now sits in the Review page's Back / Next row, on the right in line with Back (`StageSections` `reviewAction` prop replaces Next on Review). The "N required items left. Select Submit Insight to see where." caption was removed at the owner's request (it, and its `aria-describedby` / `reviewActionHint` plumbing, are gone); the popup is the only explanation of what is left. The reassurance paragraph ("Submitting doesn't mean your work has to be perfect...") is now always shown in the Submit band. Verified in an authenticated browser: Back and Submit share a vertical center and width on desktop, stack on a 390px phone without overflow, the popup still opens from the new position, and a complete draft really submits from it (Insight COMPLETE, Investigate unlocked). Supersedes the earlier "hint under Submit" entry.
  - **[Sep 21, 2026, security + efficiency pass over the stage forms]** Reviewed every text entry and server action on the stage pages, then fixed what was actually wrong and re-tested each finding as a real attack in a logged-in browser (not just by reading code). **Fixed:**
    - **No length limit on any text entry (the main hole).** Every stage field was `String(formData.get(x)).trim()` with no cap, so one crafted POST (or one scripted `value =` past the browser) could write megabytes into `Submission.content`, and autosave fires about once a second. New `src/lib/stage-field-limits.ts` holds `TEXT_LIMITS` as the single source of truth: the server actions enforce it (the real limit, since a POST never touches the form) and every `Input`/`Textarea` now sets a matching `maxLength`. Over-long input is **refused, never truncated**, so nothing typed is silently thrown away. Verified: 9,000 chars into a 2,000-cap field is refused and nothing is stored; exactly 2,000 saves.
    - **`Project.category` accepted arbitrary text on a draft.** Only the final submit checked `INNOVATION_FIELDS`, so a draft could persist any string into a field shown back on the dashboard. Now validated on both paths. Verified: an injected `<img src=x onerror=...>` category is rejected and the stored value is unchanged.
    - **A rejected photo silently threw away the typed text.** `persistInsight` returned early on a bad upload, so a too-large/wrong-type photo meant the whole autosave failed and the answers went unsaved. Now a draft still writes the text and reports the photo problem; a final submit still stops (nobody submits thinking a photo went with it).
    - **Autosave blamed the connection for refused saves.** Any error showed "Not saved. Check your connection." The client now shows the server's real message ("One of your answers is longer than this form allows...", "Please upload a PNG, JPEG, or WebP image.") and keeps the connection wording only for a genuine network failure.
    - **Investigate lost typed answers on a refused submit (real data loss).** React 19 resets a `<form action={...}>` after the action runs, including when it returns an error. Insight rides this out because autosave means the reset restores the saved draft; Investigate has no autosave, so its uncontrolled fields emptied. Its four fields are now controlled state. Verified before/after: the answers used to come back 0/0/0, now they survive.
    - **Missing security headers.** `next.config.ts` now sets `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy`, and a `Permissions-Policy`. `nosniff` also matters for `/uploads/*`. No CSP yet, deliberately: Next's inline bootstrap needs a nonce and a wrong CSP breaks the app silently.
    - **Efficiency:** autosave dropped a `teamMembership.findFirst` per save (the `Project.teamId` already says whether it is a team project, and it is only needed for final validation), and the `Project` row is now only written when the title or category actually changed instead of on every autosave.
  - **Confirmed safe, left alone:** submissions are keyed to the session's own `studentId` (no cross-student write path); `requireCurrentStage` still refuses any stage that is not `CURRENT`, so a locked or already-complete stage cannot be written by direct POST (verified: `/dashboard/imagine` redirects to the hub); no `dangerouslySetInnerHTML`, no raw SQL, no `eval` anywhere, and a stored `<img src=x onerror=...>` answer renders as literal text (verified: no execution); uploads validate declared type and size and derive the filename and extension themselves, so a `text/html` upload is refused and no attacker-controlled filename ever reaches the filesystem.
  - **Known and still open (not regressions, deliberately not changed here):** uploaded files under `public/uploads/` are served **unauthenticated** to anyone holding the URL (names are random, so not enumerable, but this is not access control) and runtime writes to `public/` will not work on the Cloudflare Workers deployment target: both resolve with the deferred R2 work, and payment screenshots have the same property. There is also **no rate limiting** on the server actions, so autosave can be hammered by a script; worth adding with the admin/Sprint 6 work. Verification used throwaway fixtures, all deleted afterwards (0 `tabtest-` rows remaining). `tsc`, `eslint src/`, and `next build` all clean.
  - **[Sep 21, 2026, "Finish Insight" checklist made accurate and complete]** The hub's What's-left panel could disagree with the stage form in both directions; it now matches exactly what `saveInsightAction` enforces, and every disagreement was reproduced in a browser before and after the fix.
    - **It could say "Ready to submit" while Submit was blocked.** `evidenceType` (the "choose one" strongest-firsthand-evidence radio) is required on a final submit but was checked nowhere in `getStageChecklist`. Now covered by the "Your strongest firsthand evidence, and which kind it is" row. Verified: everything filled except `evidenceType` reads 9 of 10 / Not ready, and Submit is disabled; both agree.
    - **A team could never reach "Ready to submit".** The Team confirmations row required `teamCharterComplete`, which neither the form (`TEAM_CHECK_KEYS`) nor the server requires; it is the curriculum's "if applicable" line. It is now listed as an **optional** row and the four real confirmations are what count. Verified with a real team fixture (team project, `Project.teamId` set): 11 of 11 / Ready with the charter box unticked, Submit enabled.
    - **Optional items were counted as requirements.** Investigate's "Conversations with people affected" is labelled "if any" and does not gate submission, so it kept the panel short of ready forever. `ChecklistItem` gained `optional`, shown with an `OPTIONAL` tag and left out of the count; `summarize()` now counts only non-optional items, so "N of M" and "Ready to submit" mean exactly what the Submit button allows. Insight's optional photo and "how that person is connected" rows are shown the same way.
    - **Rows now deep-link to the page that answers them.** `ChecklistItem` gained `page`, so a row goes to `/dashboard/insight?page=empathy#stage-work` instead of the top of the stage. Verified a link really opens that page (`section-empathy` visible).
    - **The AI-use disclosure is stated, not ticked**: it is only written at final submit, so it cannot be derived from saved data. A note under the list says so rather than adding a row that could never check off.
    - **Deliberately not changed, worth a decision:** `Project.category` uses `"Other"` as both a real choice and the not-yet-chosen placeholder (`getOrCreateStudentProject` falls back to it, and the form treats it as unchosen), so a student who genuinely picks "Other" sees that row untick again after a reload. The checklist matches the form here, so the two agree; fixing it properly means a separate sentinel for "not chosen yet". Verified end to end at 1440 and 390px (no overflow, no console errors); `tsc`/`eslint` clean; all fixtures deleted (users/students/teams back to the 20/11/1 baseline).
  - **[Sep 21, 2026, one list behind both "what's left" views]** The site owner spotted that the Submit popup listed 20 individual fields while the hub's "Finish Insight" checklist listed 10 grouped ones. They were two hand-maintained lists that had already drifted, so they are now one: **`src/lib/insight-requirements.ts`** holds every Insight requirement (`key` = the form field name and the `Submission.content` key, plus `label`, `page`/`pageLabel`, `kind`, `optional`, `teamOnly`), one entry per field so the popup can still jump to the exact box.
    - `getStageChecklist` builds the hub list from it, marking each item done from saved rows; `InsightForm` builds the popup from it, marking each item done from the live form. `REQUIRED_TEXT_KEYS`/`REQUIRED_CHECK_KEYS`/`TEAM_CHECK_KEYS` and the separate `REQUIRED_ITEMS` table are gone, so there is nothing left to drift. `title`/`category` live on `Project` rather than in `content`, and the checklist maps the "Untitled project"/"Other" placeholders to empty exactly as the form does, so both sides agree on whether they are answered.
    - The list is declared `as const satisfies readonly InsightRequirement[]` to keep literal key names (`InsightRequirementKey` types the form's `filled` record), and is read back through `InsightRequirementEntry` because `as const` otherwise drops `optional`/`teamOnly` from entries that omit them.
    - **Verified in a browser that the two are now identical**, not just similar: a part-filled draft gave hub "9 of 20 requirements complete / 11 left" and popup "11 things left to finish", with the same 11 labels in the same order (the hub additionally shows the 2 unfinished optional rows, by design). Team fixture re-checked: 11 of 11, Ready, Submit enabled, charter row optional.
    - **Fixed a pre-existing bug found on the way:** `StageProgressPanel` has always rendered `className={styles.progressLabel}` but `.progressLabel` was never defined in `DashboardHub.module.css`, so that span shipped as `class="undefined"` and only inherited the row's styling (it went unnoticed because the inherited look was close enough; it mattered once the OPTIONAL tag moved inside it). Rule added.
    - Checked at 1440 and 390px, no overflow, no console errors; `tsc`, `eslint src/`, `next build` clean; fixtures deleted.
  - **[Sep 21, 2026, `participantConnection` was wrongly optional]** The site owner questioned the OPTIONAL tag on "How they are connected to the problem". Checked against the source doc rather than the summary: `docs/curriculum/05_BUILD/i3league-stage1-submission-reviewer.md` item **7 is marked *(required)*** and contains **two** inputs, the participant label and "How is this person connected to the problem?", so the connection is required. It had never been enforced: it was missing from `saveInsightAction`'s final-submit check, had no `onChange` tracking in `InsightForm`, and was then carried into the new shared list as `optional: true`. Now required in all three (shared list, server validation, and the form's `filled` tracking). Insight's required count for an individual goes 20 → 21. Verified: with only that field blank, the hub reads "20 of 21 / Not ready", the popup reads "1 thing left to finish" naming that exact item, and Submit is disabled.
    - Re-checked every other field against the doc while there: Sections 1–5 and the four team confirmations are all enforced; **item 16 "Team Charter status" is correctly optional** (the doc describes it as a status *read from the saved charter*, not a required student action, and there is no Team Charter entity yet so it is a self-reported box); the **photo is our own addition and is not in the doc at all**, so it stays optional; item 18 is the one thing the doc marks optional and was already removed. Nothing else is missing. `tsc`/`eslint` clean.
  - **[Sep 21, 2026, checklist rows jump to the exact field]** Rows in the hub's "Finish Insight" checklist used to land at `#stage-work` (the top of the work area) of the right page; they now land on the specific box. Each requirement in `src/lib/insight-requirements.ts` resolves to an element id (`anchor`, defaulting to `key`; the photo row overrides to `photo`, the file input's id), carried through `ChecklistItem.anchor`, and `StageProgressPanel` links to `/dashboard/insight?page=<page>#<anchor>`. Investigate's research rows got anchors too.
    - `RadioGroup` gained an optional `id` (put on its `<fieldset>`) because it had none, so the two radio requirements (`category`, `evidenceType`) could not be targeted; InsightForm passes `id` for both.
    - Landed fields were first scrolling to the very top of the viewport, hidden under AppShell's sticky 68px topbar. A `:target { scroll-margin-top }` rule did **not** work, because the router performs the scroll rather than native fragment navigation, so `globals.css` sets `scroll-margin-top: 100px` on `input[id], textarea[id], select[id], fieldset[id]` directly. Verified: targets now sit at 100px, in view and clear of the topbar, on desktop and at 390px.
    - Verified by clicking real hub rows: Evidence, evidence type, Why the scope is manageable, Innovation area and the photo each open the correct page with their own element in view. One click in a repeated run (1 of 5, and a different row in an earlier run) did not register; it did not follow a particular row and there were no console errors, so it reads as the test clicking mid-render rather than a broken link, but it is worth a manual click-through. Links focus nothing (unlike the Submit popup, which focuses the field); that could be added. `tsc`/`eslint` clean.
  - **[Sep 21, 2026, confirmation heading on Review]** In the Review page's "Your answers" box, the four Empathy confirmation check rows now sit under a "Responsible Research Confirmation" heading (same wording as the Empathy form). `AnswerItem` gained a third variant, `{ heading }`, rendered as a small ink heading row (`.groupHeading`, `role="heading" aria-level={4}`) above the rows that follow it. `tsc`/`eslint` clean; not yet viewed in a browser.
  - **[Sep 21, 2026, checklist links focus the field, matching the popup]** Arriving from a hub checklist link now puts the cursor in the field, the same as the Submit popup. Both paths call one helper, `focusField(name, behavior)` in `StageSections.tsx` (scroll to the middle of the screen, then focus; a radio group is reached through its first option since a `<fieldset>` cannot take focus): the popup through `goTo`, and a one-time mount effect that reads the URL hash. Because they share the code they land identically (verified: the Evidence box sits at the same 445px either way). The hash comes from the URL, which anyone can edit, so it is passed through `CSS.escape` (an unescaped `#"]` would make `querySelector` throw) and `decodeURIComponent` is guarded against malformed `%` escapes; a hash that is not a field name (`#stage-work`) is a no-op. Verified all five sampled rows focus the right field on the right page, the popup still does, and `#"]`, `#%`, `#<script>...` and `#stage-work` all render with no console errors. This supersedes the "links focus nothing" note in the previous entry. `tsc`/`eslint` clean.
  - **[Sep 21, 2026, hero content audit]** Checked every value both heroes show against its source (curriculum docs and live database rows), across all six stages rather than Insight alone, since every stage page uses `StageHero`.
    - **Fixed: Insight's artifact name was wrong.** The hero (and, through the same field, the Investigate artifact card and the dashboard's "Submit your ..." card) read "Validated Problem Statement + Evidence". Every curriculum doc that names it says **"Validated Problem Statement + Evidence Notes"** (03_SHOW full requirements + master table, 04_REVIEW stage 1 rubric, 05_BUILD submission reviewer + worksheet pack, 02_DO activities), and this log itself recorded that name; nothing recorded a decision to shorten it, so it was drift. Restored in `stage-copy.ts` with a comment saying "Evidence Notes" is the deliverable's name, not a separate box.
    - **Fixed: unbuilt stages showed "0 lessons · 0 activities".** Imagine, Iterate, Impact and Influence have no lesson/activity rows yet, so the stat box printed a zero count that read as a broken page. The caption is now omitted when both counts are 0.
    - **Fixed: dashboard hero gave a false instruction.** With an untitled project it always said "not named yet; you'll title it in Insight", including after Insight was complete (reachable only through an admin override, since a normal submit requires a title). A finished Insight is read-only, so it now says just "not named yet" once Insight is complete. Verified all three states: Insight open (keeps the pointer), Insight complete (drops it), titled (shows the title).
    - **Confirmed correct, unchanged:** the other five artifacts match each stage's own "Required Artifact" line. Influence's "Final Pitch + Innovation Portfolio" is right: a second list ("Final Innovation Portfolio Contents") gives stage 6 as "Final Pitch + Reflection + Implementation/Sustainability Plan", but that is what stage 6 *adds to* the portfolio, not its required artifact. All six questions appear verbatim in the docs (Insight's guiding question from its LEARN notes; the other five are the core questions from `00_OVERVIEW`, shown under the "Guiding question" label). Lesson/activity counts match the database (Insight 5/5 with Team Charter last, so 4/4 for an individual and 5/5 for a team; Investigate 2/1, singular handled). The eyebrow ("Stage 0N of 06" + status), submission-format line, and the dashboard hero's name/team fields are all correct. Insight's description is a faithful paraphrase of the rubric and requirements, not a quote.
    - **Not changed, noted:** the dashboard stage card's tag words ("PEOPLE", "UNDERSTAND / LISTEN / DEFINE", ...) exist only in `StageCard.tsx`; the docs define no per-stage tag words. None contradict the core questions, though Iterate's omit "test" and Influence's omit "defend"/"sustain", both central to their questions. Also noticed just outside the hero: the deadline band picks spring, else summer, so after the summer deadline it would show that past date with "0 days remaining"; harmless today (next deadline April 30, 2027) and moot once a new Season exists. `tsc`/`eslint` clean.
  - **[Sep 21, 2026, Investigate built to the standard]** `/dashboard/investigate` rebuilt from the Stage 2 source text the owner supplied (01_LEARN notes, 02_DO activities, 03_SHOW, 04_REVIEW stage 2 rubric), following the Insight standard page for page.
    - **Pages (14), in working order, not the curriculum's listing order:** Research Questions, Bias Awareness, Data Privacy, Safety Screening, Evidence Collection, Data Literacy, Research Integrity, Triangulation, Citation, Root Cause, System Map, Stakeholders, Gap Analysis, Review. The curriculum lists the 7 LEARN topics then the 5 supporting activities, but its own rules say research questions come "before collecting evidence" and bias/privacy checks happen "before using" the research tools, and the mental test runs Question → Evidence → Triangulate → Root Cause → Gap. Order is one array, `INVESTIGATE_PAGES` in `src/lib/stage-copy.ts`; flagged to the owner, easy to reorder.
    - **Supporting activities** (Research Questions, Evidence Collection, Root Cause, Stakeholders, Gap) have no Learn band (no lesson copy was invented); they open on a Do band kicked "Do · Required supporting activity" with the curriculum's "Why it's needed" line. `TaskSection.learn` is now optional and band tones alternate from whichever layers exist; `pagerToneAfter(bandCount)` gives the Back/Next row's tone. The Show rail can now also carry the section's "The reviewer checks" list and a `showNote` (the minimum evidence standard on Triangulation, the HS note on System Map).
    - **System Map is HS CORE**: shown to every student (one common pathway), required only when `isHighSchoolGrade(student.grade)` (grade parses to 9 or more), optional otherwise (hub rows tagged OPTIONAL). Its Learn kicker reads "Learn · HS Core".
    - **One requirements list**, `src/lib/investigate-requirements.ts` (36 items for HS, 31 required for MS, one per field plus `safetyCleared` and the `evidenceTypes` multi-check needing at least 2): read by the hub checklist, the Submit popup, and `persistInvestigate`'s final check (the first stage whose server validation reads the shared list directly). There is no field-level Stage 2 submission template in the docs (Stage 1 had 05_BUILD's reviewer doc), so the fields are the smallest set that captures each "Must include" item and activity output; worth confirming with the company.
    - **Autosave, AI disclosure + Submit on Review, Submit popup, never-locked Review**, exactly as Insight (`autosaveInvestigateAction` / `saveInvestigateAction` share `persistInvestigate`). Old Save-draft button, first-page Submit, locked Review, the ink checklist footer, and the old 4-field content shape are gone (old drafts' keys are ignored).
    - **Safety screening kept** (CLAUDE.md rule, not a curriculum topic) as its own page before Evidence Collection. Its fields use `form="safety-screening"` to join an empty `<form>` rendered outside the shared form, so they are never autosaved or sent with the stage; `saveSafetyScreeningAction` now `revalidatePath`s instead of redirecting, so the student stays on `?page=safety`. `InvestigateForm.handleChange` ignores change events from fields that belong to another form.
    - **Lessons**: `LessonPanel` now renders rows in written order and shows any non-standard label as written, which Bias ("What is bias?", "Better version") and Citation ("Credible Sources (quick reference)", shown as a bold-labelled list) needed; Insight renders unchanged. Live DB: the old 2 Investigate lessons + 1 activity were deleted and reseeded as 7 lessons + 12 activities (same `deleteMany` then `prisma db seed` gotcha as Insight). Seed Lesson order is curriculum order; each page finds its lesson by `lessonIndex`, so there are no hardcoded index pairings in `page.tsx`.
    - Hero shows a new optional `guidingPrinciple` line ("Good research can change your mind."). Submission format is the curriculum's verbatim ("One polished Research Brief, approximately 4–8 pages or equivalent digital format..."), per the owner's text, unlike Insight's "typed answers" wording; the Review footnote says these answers make up the Research Brief.
    - **Verified** against the live Supabase project with throwaway fixtures (both deleted): HS student: all 14 pages render, band tones alternate on every page including the Back/Next row, autosave persists across reload, saving the safety screening stays on its page and keeps typed answers, popup lists 33 items and jumps to the exact field, hub reads 36 of 36 / Ready, real submit completes Investigate and unlocks Imagine, read-only view shows answers + rubric + closing reflection. MS student: high-risk screening goes to pending, popup lists only "waiting for an admin", hub 30 of 31 with System Map optional, forced final POST while pending is refused (stage stays in progress). No horizontal overflow at 390px, no console errors. `tsc` and `eslint` clean. **`next build` not run** (the owner's dev server was using `.next`).
    - Noted, not changed: with 14 sections the sidebar rail's section list is long, so stages 05-06 need a scroll in the rail on a ~900px-tall window.
  - **[Sep 21, 2026, Investigate regrouped to 7 pages]** The owner found 14 pages too many. Each supporting activity now sits on the page of the topic it works alongside, so pages drop from 13 + Review to **7 + Review**, all Learn → Do → Show except the last: Questions & Bias (Bias lesson; Research Question Planning + Bias Hunt), Privacy & Safety (Privacy lesson; Data Minimization; the safety screening is the Show band's second group), Collecting Evidence (Data Literacy lesson; Evidence Collection + Data Reality Check), Testing Your Evidence (Triangulation + Integrity lessons; Triangulate a Claim + Contradicting Evidence), Sources (Citation lesson; Source Audit), Root Cause & Systems (System Mapping lesson, HS Core; Root Cause Analysis + Map the System), Stakeholders & Gap (no lesson exists, so Do + Show only). Defined in `INVESTIGATE_PAGE_DETAILS` / `INVESTIGATE_PAGES` (`src/lib/stage-copy.ts`), replacing `INVESTIGATE_SECTION_DETAILS`.
    - New `PartStack` (`src/components/journey/`) stacks several lessons or activities in one band, each under its own heading (a supporting activity next to a topic's own activity is labelled "Required supporting activity"). The band heading is the lesson/activity name when there is one, the page name (Learn) or "Activities" (Do) when there are several. `investigate-parts.tsx` builds each page's Learn and Do content once for both the live form and the read-only view. The Show band groups its fields per activity under small mono headings, the same headings the Review answers box uses. `TaskSection` gained `showNotes` (a list), `doKicker`, and a plural worksheet footer.
    - Fields, requirements (36 HS / 31 required MS), server checks, autosave, and the safety `form`-attribute pattern are unchanged; only page ids/labels moved. Verified with a throwaway HS fixture (deleted): 8 pages in the bar, every page alternates paper/canvas including the Back/Next row, the safety screening saves in place on Privacy & Safety and keeps typed answers, hub 36 of 36 / Ready, a real submit completed Investigate (Imagine CURRENT, `isFinal` true), read-only recaps show answers, no overflow at 390px, no console errors. `tsc`/`eslint` clean; `next build` still not run (dev server running).

    - **[Sep 21, 2026, Research Question Planning detail]** The curriculum only names this activity (worksheet, output, "Why it's needed"). At the owner's request it now has a one-line description, **our wording**, built from what the Stage 2 rubric checks: "Before collecting any evidence, turn your Stage 1 problem statement into 2–4 focused, open questions your research can answer, including at least one about why the problem happens." Worth confirming with the company. (A longer steps + example version was tried and cut back to this line.)
      The curriculum's "Why it's needed" line for this activity is no longer shown (owner request; the one-line description covers it). Supporting status is now an explicit `supporting: true` on the activity, not inferred from having a `why` line.
    - **[Sep 21, 2026, Questions & Bias: one activity]** At the owner's request, Research Question Planning and Bias Hunt are one activity, "Research Question Planning + Bias Hunt" (descriptions run together; worksheets "Research Question Planner + Bias Check"; outputs joined). The Do band now reads as a single-activity page (activity name as the heading, no "Activities"/supporting label). The Show fields are unchanged (still two groups). The hero's "12 activities" still counts the seeded Assignment rows.
    - **[Sep 21, 2026, Collecting Evidence: one activity]** At the owner's request, Evidence Collection and Data Reality Check are one Do band, "Evidence Collection + Data Reality Check" (same pattern as Questions & Bias): the description opens with the old "Why it's needed" line reworded as an instruction ("Collect the actual evidence Stage 2 depends on."), then the Data Reality Check text; worksheets and outputs are joined with "+". No longer labelled a supporting activity. Show fields unchanged. `tsc`/`eslint` clean.
    - **[Sep 21, 2026, Testing Your Evidence split in two]** At the owner's request the `testing` page is now two pages, so Investigate has 8 pages + Review: **Triangulation** (`triangulation`: Source Triangulation lesson, Triangulate a Claim, Must include "More than one type of evidence" + "Triangulation of at least one important claim", the minimum evidence standard note, fields `triangulatedClaim`/`triangulationEvidence`/`triangulationResult`) then **Research Integrity** (`integrity`: Research Integrity lesson, Contradicting Evidence Challenge, Must include the challenging-evidence item, both reviewer checks, fields `challengingEvidence`/`challengingInterpretation`/`understandingChange`). Field keys and requirement counts are unchanged, so saved drafts are unaffected; an old `?page=testing` link just opens the first page. Updated in `stage-copy.ts`, `investigate-requirements.ts`, `InvestigateForm.tsx`, `summary-sections.ts`. `tsc`/`eslint` clean; not viewed in a browser.
    - **[Sep 21, 2026, one box per layer + grade-specific Root Cause page]** Owner rule: every page has at most one Learn, one Do and one Show box; two lessons means two pages. Root Cause & Systems and Stakeholders & Gap each had two Do bands; they are now one combined activity each ("Root Cause Analysis + Map the System", "Stakeholder Mapping + Existing Solutions & Gap Analysis"; descriptions are our wording built from the outputs and "Why it's needed" lines, worth confirming with the company). **Grade-specific pages instead of an HS note**: `InvestigatePage.middleSchool` holds overrides and `investigatePageDetail(page, isHighSchool)` returns what a student sees. High school gets Root Cause & Systems (System Mapping lesson tagged HS Core, combined activity, Root Cause + System Map fields, all required). Middle school gets a page named **Root Cause**: no Learn band (the only lesson is HS Core), Root Cause Analysis only, root cause fields only. The old "High school: the Research Brief must include the System Map..." note and the "(optional for you)" label are gone. `investigateRequirementsFor(false)` now leaves the five System Map items out entirely (was: listed as OPTIONAL), so middle school has 31 items (36 for high school), and page labels in the hub/popup come from `investigatePageDetail`. All readers (form, read-only view, Review answers, hub, server check) use the grade-aware detail. `tsc`/`eslint` clean; not viewed in a browser.
    - **[Sep 21, 2026, optional System Map for middle school]** Owner chose to keep an optional map for middle school. Their Root Cause page's Do box ("Root Cause Analysis") now ends with an optional extra-depth line, and its Show band adds a "System map (optional)" group with simpler questions (`MIDDLE_SCHOOL_SYSTEM_MAP` in `InvestigateForm.tsx`) saving to the same five `system*` keys. `investigateRequirementsFor(false)` lists those five as OPTIONAL again (36 items for everyone; 31 gate Submit for middle school). This supersedes the "left out entirely" part of the previous entry. `tsc`/`eslint` clean; not viewed in a browser.
    - **[Sep 21, 2026, HS Core tag removed]** The System Mapping lesson's Learn kicker no longer reads "Learn · HS Core" (owner request): only high school students see that lesson now, so the tag was redundant. The `tag` option on page lessons is kept for later use.
    - **[Sep 21, 2026, page bar on one line, buttons sized to their text]** The navy page bar's buttons were equal-width columns (min 180px), so "Sources" got the same box as "Stakeholders & Gap". Owner wants them sized to their text and all on one line. Now `flex: 1 1 auto`, no wrapping, tighter buttons (12.5px label, 10px mono `01` numbers kept per the owner, `0 10px` padding, 42px tall, 6px gaps), and the row spans the full page width instead of the 1160px container. Measured in headless Chromium with the real fonts: Investigate's nine buttons need about 1175px, which fits from roughly a 1260px-wide window up; narrower windows scroll the row sideways. `StageSections.module.css`/`.tsx` + design-system 22.1.
    - **[Sep 22, 2026, safety screening notice is a coral box]** The "Safety screening submitted... needs an admin" (and "wasn't cleared") notice was a coral strip nested inside a blue `Panel variant="selected"`. It is now one coral box with the AI-use disclosure's recipe (coral-soft fill, coral hairline, `8px 9px 0` coral offset shadow, square); the blue panel is gone. The "cleared" notice is unchanged. `.highRiskNotice` in `StageForm.module.css`.
    - **[Sep 22, 2026, Next action button lands on the action]** The hub's Next action "Continue" went to the top of the stage. It now uses the same link as the What's-left row for the next unfinished item (new shared `checklistItemHref` in `stage-checklist.ts`, plus `StageChecklist.nextIncompleteItem`): that page, focused on that field. "Ready to submit" (button now "Go to Submit {Stage}") and "submitted" go to `?page=review#stage-bar`. `focusField` (`StageSections.tsx`) falls back to an element id when the hash is not a field name: it scrolls that section to the top and focuses its first field, so the safety screening item links to a new `#safety-section` wrapper (not `#safety-screening`, which is the safety `<form>`'s own id) that works both as a form and as the saved notice. Verified in a logged-in browser as `rainaartist@gmail.com` (read-only): Continue → Privacy & Safety with the pending safety box in view; `?page=review#stage-bar` opens Review with the bar at the top; `?page=evidence#statistic` focuses that box. `tsc`/`eslint` clean.
    - **[Sep 22, 2026, full accuracy + security pass over the stage pages]** Owner asked for everything checked. `tsc`, `eslint`, and a full `next build` (run on a copy in the scratchpad, since the dev server holds `.next`) all clean. Read every server action in `dashboard/[stage]/actions.ts`; **five gaps fixed:** (1) the actions never re-checked the student's own payment, so a student whose payment was later set to REJECTED/REFUNDED could keep writing by direct POST even though the page redirected; `requireCurrentStudent` now requires a VERIFIED payment for the active season, per student. (2) A final submit could omit the AI-use disclosure answer entirely (`usedAi` missing was read as "no"); a final now needs an explicit yes/no. (3) Insight's `evidenceType` radio accepted any string; now only the three offered values (`INSIGHT_EVIDENCE_TYPES` in `insight-requirements.ts`, shared with the form). (4) An autosave landing just after a final submit could flip `isFinal` back to false and overwrite the submitted answers; drafts now `updateMany` with `isFinal: false` in the where clause (database-enforced) and are ignored once a final exists. (5) Two teammates saving the shared safety screening at once raced on the `projectId` unique; the loser's `P2002` is now caught and the first save stands. **Verified live** with throwaway HS (grade 10) and MS (grade 7) fixtures, both deleted after (0 `tabtest-` rows): all 8 pages + Review for both grades, one Learn/Do/Show box per page, bands alternate on every page, page bar on one line at 1440 and scrolling at 390 with no page overflow, autosave "Not saved yet → Saved" and persisted across reload, Submit popup count equals hub count and jumps to the right field (34 left/2 done of 36; MS 30 left of 31 with 5 OPTIONAL map rows), Next action links to the exact field / safety section / Review, no-risk screening auto-clears and high-risk goes PENDING with the coral notice, forced final while PENDING refused, forced final without AI answer refused, injected bogus `evidenceTypes` refused, 9,000-char autosave refused with the real message and prior value kept, stored `<img onerror>` rendered as text (not executed), locked stage URL bounces to the hub, REFUNDED payment bounces to the hub, logged-out → /login, security headers present, draft replay against a submitted stage changed nothing; then both grades really submitted (isFinal, AIDisclosure row, Investigate COMPLETE, Imagine CURRENT) and the read-only recap showed with no Submit button or editable fields. Also renamed the safety link target to `#safety-section` (`#safety-screening` was already the safety `<form>`'s id, so the two collided). **Still open, unchanged:** no rate limiting on the actions; `public/uploads` served unauthenticated (deferred with R2).
    - **[Sep 22, 2026, stages 3–6 built: Imagine, Iterate, Impact, Influence]** All four remaining stage pages now exist, to the Insight/Investigate standard (hero with guiding question, navy page bar, sidebar sections, one Learn / one Do / one Show box per page, alternating bands, autosave, Review page with answers box + rubric + AI disclosure + gated Submit popup, read-only recap after submit). Built as **one data-driven engine** (see the "Stage page standard" section) because Investigate's form had already become pure data. Content: lessons (12/12/17/18), activities and the four rubrics were parsed straight from `docs/curriculum` (em dashes rewritten) into `prisma/stage-content-3-6.ts`, `src/lib/stages/activities.ts`, `src/lib/stages/rubrics.ts`; the live DB's four placeholder modules were reseeded (12/12/17/18 lessons, 13/14/20/22 assignments incl. supporting activities). `stage-copy.ts` gained `guidingQuestion`, `revisesWhen`, `finalSubmission.closingReflection`, `detailedRubric`, and verbatim 03_SHOW `submissionChecklist`/`submissionFormat` for all four. **Our decisions worth confirming with the company:** (1) field labels and hints are ours, one field per Must include item / activity output (about 17/18/25/27 fields per stage); (2) supporting activities are joined onto a topic page as one Do box (Critical Assumption Selection → Assumptions; Participant Plan → Test Plan; Retest → Revision; Evidence Collection Plan → Baseline; Evidence Quality Check → Claims; Final Impact Conclusion is its own last page; Rehearsal → Demo; Evidence Audit → Defend; Mock Reviewer Q&A → Q&A; Final Ask Test → The Ask); (3) Imagine's Decision Matrix page comes before the Critical Assumption page per the curriculum's required sequence; (4) **HS CORE pages are hidden from middle school** (owner's "no shared page with an HS note" rule; unlike Investigate's System Map, no simplified MS version was written, so middle school gets nothing for Build vs. Buy, Experiment Controls, Causation, Indicators, Adoption, Scalability, ROI, Budget, Funding, Partners, Risk, Executive Summary), while STRETCH pages are shown to everyone as optional; (5) Iterate's fidelity and Impact's final conclusion are radio choices. **Page bar:** Impact and Influence have 19 pages for high school, which do not fit on one line even at 1440px, so the bar scrolls sideways there (Imagine/Iterate: 13, fits). **Verified live** with throwaway HS/MS fixtures (deleted): every page of all four stages renders for both grades with no missing lesson, bands alternate, hub count = popup count, autosave persists, real submits advanced the HS student through all four to journey complete (hub shows "All six stages"), MS Impact shows 13 pages / 19 required with HS pages hidden and Stretch optional and submits, read-only recaps show, no overflow at 390px, no console errors. `tsc`, `eslint`, `next build` clean. Not done: team-only confirmation for Influence ("every team member can explain their contribution") has no field; no file upload for Influence visuals (text description only).
    - **[Sep 21, 2026, Investigate formatting matched to Insight]** Compared every visible text style on all Investigate pages against all Insight pages in an authenticated browser. Fixed what differed: (1) a page with two lessons or two activities now renders **one Learn or Do band per lesson/activity**, each headed by its own name with its own "Not submitted" footer, exactly like a one-topic Insight page (`TaskSection` `learnParts`/`doParts` of `BandPart`; `PartStack` and the generic "Activities" heading were removed; the per-page `keep` list is gone, each Do band names its own worksheet); (2) "Why it's needed:" is plain description text, not bold; (3) the safety screening intro uses the muted body style (`StageForm .intro`); (4) the Show rail note matches the Must include text. Only remaining differences are tag-only (same look). Bands still alternate. Also: the owner's real `rainaartist@gmail.com` account (Investigate CURRENT) had an already-saved, auto-cleared safety screening, so the page showed "No risk factors flagged" instead of the options; at the owner's request its `SafetyReview` row was deleted (direct DB write), so the 11 options show again on Privacy & Safety.
- **[Sep 22, 2026, Innovation Journal built]** First of Sprint 5's four schema-ready-but-unbuilt features (Innovation Journal, Speaking/Character/Ethics Challenges) to actually get a UI. New route `/dashboard/journal`, reusing `AppShell` chrome (a new sidebar nav link, between Dashboard and the six-stage list) — a running, student-authored, dated log a student keeps alongside their project, separate from the six stages' own submissions.
  - **`src/lib/journal.ts`**: `JOURNAL_ENTRY_TYPES` (the 12 string values `JournalEntry.entryType` allows per its schema comment — thought/observation/research/sketch/experiment/failure/result/feedback/decision/improvement/mentor_feedback/ai_use — there's no native Prisma enum for this column, so this list is the one place that set is defined) with human labels, and `getJournalTimeline(studentId)`, which fetches every version of every entry and groups them by `entryGroupId` into `{ latest, history }`, newest group first.
  - **CLAUDE.md's core rule enforced at the write path, not just documented**: "Innovation Journal is append-only: edits create a new version; don't overwrite/delete history." `src/app/dashboard/journal/actions.ts` exposes exactly two actions — `createJournalEntryAction` (fresh `entryGroupId`, version 1) and `reviseJournalEntryAction` (same `entryGroupId`, version = current max + 1) — and neither can update or delete an existing row; there is no PATCH/PUT/DELETE path anywhere in this feature. `reviseJournalEntryAction` re-confirms the client-supplied `entryGroupId` actually belongs to the signed-in student before appending to it (same defense-in-depth every other server action in this codebase applies to a client-supplied id) rather than trusting it blindly.
  - Gated the same way as the rest of the curriculum: a local `requireVerifiedStudent()` (mirroring `dashboard/[stage]/actions.ts`'s `requireCurrentStudent`) requires this student's own payment be `VERIFIED` for the active season, re-checked server-side independent of the page. Not gated by stage — the Journal is a cross-stage, ongoing artifact, so unlike the stage forms there's no `requireCurrentStage` check.
  - An entry needs at least text or a photo (not both required); text is capped at 5,000 chars (`JOURNAL_TEXT_LIMIT`, enforced server-side, matched by the textarea's `maxLength` — same "server is the real limit" convention as `TEXT_LIMITS` in `stage-field-limits.ts`, kept in a separate constant since this is one field on an unrelated model, not a per-stage-field map). The optional photo reuses `ScreenshotUpload` (already generalized via `name`/`label` props) and `saveUploadedFile(file, "journal")` — no new upload code.
  - **Real bug caught by live testing, fixed before shipping**: the revise form's "tell the parent to close this form" logic first lived in a render-time state derivation (the same safe pattern `JournalComposer` uses to clear itself after a successful save) — but that pattern is only safe when a component derives its *own* state during render; calling the *parent's* `setRevising(false)` from inside the child's render triggered React's "Cannot update a component while rendering a different component" warning, caught in a live headless-browser console-error check, not by code review. Fixed by moving that one call into a `useEffect` keyed on the action's `state` object reference — calling a parent callback in response to a finished action is a legitimate effect, unlike setting local derived state in an effect (which this codebase avoids elsewhere per the `react-hooks/set-state-in-effect` lint rule).
  - **`RadioGroup` (`src/components/design-system/RadioGroup.tsx`) gained an optional controlled `value` prop**, alongside its existing `defaultValue`. Needed because React 19 resets a `<form action={...}>` after every call, success or error (the same behavior that forced Investigate's fields to become controlled, per the Sep 21 2026 entry above) — an uncontrolled entry-type radio would silently revert to nothing typed on a validation error. Every composer/revise field (`entryType`, `text`, `entryDate`) is controlled for the same reason; a successful create clears the composer on purpose (including remounting `ScreenshotUpload` via a `key` bump, since it keeps its own internal file state), while an error leaves everything as typed. Existing `RadioGroup` callers are unaffected (the prop is optional, `defaultValue` behavior unchanged when `value` is omitted).
  - Timeline UI (`JournalEntryCard.tsx`): each entry shows its type tag, date, and latest text/photo; a "Show earlier versions (N)" toggle reveals every older version underneath (oldest history stays visible, never trimmed); "Add an update" opens an inline revise form pre-filled with the current version's values.
  - **Verified end-to-end against the live Supabase project**: a throwaway fixture (real `auth.users` row via the Admin API, real `Student`/`Enrollment`/`Payment(VERIFIED)`, magic-link session via `@supabase/ssr`'s `createBrowserClient` from esm.sh — the same pattern this codebase has used for every prior sprint's live verification) walked a real headless browser through: empty state ("No entries yet"), creating an entry, revising it, confirming the revised text became the latest content while the original stayed intact and readable under "Show earlier versions," and a direct Prisma read afterward confirming exactly 2 rows sharing one `entryGroupId` (versions 1 and 2) — the append-only guarantee holds at the database level, not just in the UI. Checked at 390px mobile width (no horizontal overflow, radios/textarea/history all stack cleanly) and confirmed zero console errors after the render-time-setState fix. Fixture fully deleted afterward. `tsc --noEmit`, `eslint src/`, and `next build` all clean.
  - **Not done / deliberately out of scope this pass**: no way to delete a stray entry (matches the append-only rule — there's genuinely no "undo," only writing a corrective new version), no linking a journal entry to a specific stage or submission (the platform-evidence-model's evidence-ID system is the deferred, correct way to do that — see [[platform-evidence-model]] — not something to improvise here), no admin/coach visibility into a student's journal (Coach is documented as read-only on a *roster*, not specifically the journal, and no coach dashboard exists yet regardless). Speaking Challenge, Character Challenge, and Ethics Challenge remain unbuilt (schema-only), as does Team Contribution Tracking.
  - **[Sep 22, 2026, same day, later] Split into a list/landing page + a dedicated "add entry" page, with search/filter/sort — at the site owner's request.** The Journal previously showed the composer inline at the top of the timeline; it's now two routes: `/dashboard/journal` is the landing/list view (a prominent "Add entry" button, three honest at-a-glance stats, and the searchable/filterable/sortable timeline), and `/dashboard/journal/new` is a focused, single-purpose page for writing one entry, reachable only from that button.
    - **New `/dashboard/journal/new/page.tsx`**: the same auth/payment/`AppShell` boilerplate as every other dashboard page (this codebase's established convention — each stage page and the hub duplicate this block rather than sharing a helper, so this follows suit rather than introducing a new abstraction), rendering `JournalComposer` alone with a "Back to journal" link.
    - **`JournalComposer` now navigates away on success** (`useRouter().push("/dashboard/journal")` inside a `useEffect` keyed on the action's `state` reference) instead of resetting itself to log another entry in place — it's a single-purpose page now, so "done" means "back to the list," not "clear and stay." Same effect-not-render-time-derivation reasoning as `ReviseForm`'s fix earlier this session: navigating is a side effect on something outside this component, not a derivation of its own render output.
    - **New `src/app/dashboard/journal/JournalList.tsx`** (client component): takes the full `groups` array the server already fetched and does search, type filtering, and sorting entirely client-side — deliberately not a server round trip, since a student's own journal is a small, personal dataset and instant filtering matters more here than for a shared/paginated list. Search matches an entry's *current* text and type label (not older versions' text — searching "did I write about X" almost always means "what does it say now"). Type filter renders as pills with a live count per type (e.g. "Observation (4)"), which doubles as a quick, honest signal to a middle/high schooler of what they have and haven't logged yet — not a graded requirement, just a mirror. Sort is Newest first (default) / Oldest first. An empty-results state offers "Clear search and filters."
    - **`JournalEntryCard` gained an "Edited" badge** next to the type tag whenever an entry has history, so a revised entry is visible at a glance without expanding "Show earlier versions" first.
    - **List-page header adds three real stats** (entries total, logged this week, last entry as "today"/"yesterday"/"N days ago") — computed server-side from the same `groups` the timeline renders, not fabricated or gamified beyond plain counts. Deliberately no streak/badge/points system: matches this codebase's plain-factual tone elsewhere (no invented gamification not asked for), while still giving a younger student a lightweight, honest sense of their own consistency.
    - **Real bug caught by the lint step, fixed before it shipped**: the stats calculation initially used `Date.now()` directly in the server component body, which `eslint-plugin-react-hooks`'s purity rule flags (`Cannot call impure function during render`) — fixed by computing `const now = new Date()` once up front, matching the existing `now = new Date()` convention already used in `dashboard/page.tsx`'s deadline calculation, and threading it through rather than calling the clock from inside a nested callback.
    - **Verified end-to-end against the live Supabase project** with a fresh throwaway fixture: clicked "Add entry" from an empty list, confirmed it navigated to `/dashboard/journal/new`, saved an entry, confirmed the redirect back to `/dashboard/journal` and the new entry showing in the timeline; logged a second entry of a different type; confirmed the stats row, that searching for a word only in the second entry's text hid the first, that clicking a type filter pill isolated that type and hid the other, and that switching to "Oldest first" actually reordered the cards (caught and fixed a test-script bug along the way — an unscoped `li` selector was matching the sidebar's own stage-list items, not the timeline; rescoped to the card text elements); revised the first entry and confirmed the "Edited" badge appeared and the original text was still readable under "Show earlier versions"; confirmed a direct Prisma read showed exactly 3 rows across 2 distinct `entryGroupId`s (2 versions on the first entry, 1 on the second) — append-only holds under the new flow too. Checked both pages at 390px mobile width (no overflow, "Add entry" and the filter pills wrap cleanly) with zero console errors. Fixture fully deleted afterward. `tsc --noEmit`, `eslint src/`, and `next build` all clean.
  - **[Sep 22, 2026, same day, later still] Restyled to match `docs/design-system.md` exactly, at the site owner's request ("follow design.md and a lot better looking").** The previous pass had the right structure but ad-hoc CSS (bordered mini-cards per entry, a plain `<select>`, hand-rolled shadows) instead of the doc's actual recipes. Rebuilt against Sections 21.2, 21.5, and 21.6 specifically, reusing the exact box families the rest of the site already ships:
    - **New `JournalHero.tsx`/`.module.css`**: the list page's header is now the real `SplitHero` shell (checkerboard canvas band, mono eyebrow, Chakra Petch H1, muted lede — same component `DashboardHero`/`PricingHero`/`CurriculumHero` all use), with a right-slot stat box built to the **exact `PricingHero.module.css` `.statBox` recipe** byte-for-byte (paper, `1px --line`, `8px 9px 0 --blue-soft`, mono dim label, `clamp(64px,7vw,92px)` Chakra Petch numeral — cobalt here instead of Pricing's ink, since this is a count not a price — a `64x4px` cobalt rule, mono caption) showing entries-total / this-week / last-entry, all real numbers already computed server-side.
    - **The timeline is now one connected Pattern-C plane** (Section 21.5's most-reused box: paper, `1px rgba(16,33,61,.15)`, `20px 22px 0 rgba(49,88,216,.1)` plus `0 38px 68px rgba(16,33,61,.12)`, `9px 11px 0` under 820px) — the **exact same recipe as the dashboard hub's own `StageProgressPanel`** — instead of a stack of separately bordered/shadowed cards, which the doc's Section 18 rule 7 ("prefer one large shared surface over many small floating cards") and Section 19's anti-slop guardrails both call out directly. Each entry is now a hairline-divided **row**, not a card: `JournalEntryCard`'s outer element lost its own border/shadow, and its hover highlight bleeds to the plane's edges via the same negative-inline-margin technique `.progressItem` uses.
    - **Search input** rebuilt to the Section 17 form-input recipe exactly: 47px min-height, paper fill, `1px --line-strong`, 8px radius, 15px Hanken Grotesk, `--dim` placeholder, focus = 2px `--blue` border (padding trimmed 1px so nothing shifts) plus a `0 0 0 3px --blue-pale` halo.
    - **Sort control**: the plain `<select>` from the earlier pass is gone (this codebase has never used a native `<select>` anywhere — everywhere else uses `RadioGroup` or a styled toggle, since native selects can't be skinned to the system consistently) — replaced with a two-button segmented toggle (Newest first / Oldest first), paper/blue-pale/blue states matching the button motion timing (`.16s`) used everywhere else.
    - **Type filter pills kept as pills deliberately** — Section 19 calls out "excessive pills" as an anti-pattern, but Section 21.6 and the doc's own "avoid pill-shaped containers **except when the semantics truly call for a pill**" carve-out is exactly this case (a toggle-filter group), so this is the one considered pill use on the page, not a default.
    - **Text links** (View photo, Add an update, Show/Hide earlier versions, Clear search and filters) now use the **Section 21.4 "contact email" link recipe**: `--blue-dark`, 650 weight, a 2px `--blue-soft` bottom border that solidifies to `--blue` on hover — replacing the earlier plain `text-decoration: underline` buttons.
    - **Real bug caught by a live screenshot, not code review**: both submit buttons (`Add entry`, `Save as new version`) were stretching to the full width of their flex-column forms — the design system is explicit that "Form submit … is left-aligned and sized to its content, never a full-width bar" (Section 21.6). A flex column's default `align-items: stretch` was doing this silently; neither `tsc` nor `eslint` catches a CSS layout rule, only the rendered screenshot did. Fixed with one shared `.submitBtn { align-self: flex-start }` class on both buttons.
    - **New-entry page**: the composer is now wrapped in the shared `Panel` component with `prominent` (the `8px 9px 0 --blue-soft` treatment Section 17 reserves for "the one primary surface on a page") instead of sitting bare on the band — this page's one job is that form, so it earns the one prominent surface.
    - **Verified end-to-end against the live Supabase project again after the restyle**, same fixture pattern: full create → search → filter → sort → revise → history flow re-confirmed working (one test-script update needed for the new sort-toggle buttons in place of the removed `<select>`), a direct Prisma read still showing exactly 3 rows across 2 `entryGroupId`s, zero console errors this run (the earlier `NaN` console lines were Next's own dev-mode indicator, confirmed by the corrected screenshot method, not app noise), 390px mobile checked on both pages, and the submit-button fix confirmed visually (left-aligned, sized to its label) on a fresh screenshot. One transient test flake (a `waitForURL` timeout on the second entry, no app-side cause found — fixture was cleaned up and the run repeated cleanly) — noted, not treated as a real bug since it didn't reproduce. `tsc --noEmit`, `eslint src/`, and `next build` all clean; fixture fully deleted afterward.
- **[Sep 22, 2026, Sprint 6 requirement noted: suggested per-stage/per-section timeline]** Owner asked whether the dashboard should show students a suggested timeline (how long each stage/section should take) and whether an October start leaves enough time to finish by April for MS/HS students. Discussed and agreed this is real, useful content, but it's Deadline Tracking (explicitly a **Sprint 6** item, not built yet) — not something to bolt onto today's already-large stage-page diff. Decision: **hold implementation for Sprint 6**, recorded here so it isn't lost. When that sprint starts, build it as:
  - **Static display only at first** (no Season schema change needed): a "Suggested: about N weeks, most students spend X–Y hrs" line per stage, sourced from `stage-copy.ts` alongside `guidingQuestion`/`coreQuestion` etc. — content, not tracking.
  - Optional follow-up once real deadline logic is built: Season-driven target-completion dates per stage (same pattern as `springQualifyDeadline`/`summerQualifyDeadline`), shown as "Target: by Nov 13" rather than a hard gate — advancement stays floor-based/reviewer-driven per the existing rubric model, this is guidance only, never a lock.
  - **Recommended pacing worked out in this conversation** (for an Oct 1 – Apr 30 program, ~24–26 usable weeks after breaks/exams, 2–4 hrs/week typical workload, same calendar for MS and HS — depth differs via CORE/HS-CORE/STRETCH tags per [[curriculum-depth-tags]], not a different schedule):
    - Insight: ~2 weeks (~3–5 hrs) — Oct 1–16
    - Investigate: ~4 weeks (~6–10 hrs) — Oct 19–Nov 13
    - Imagine: ~3 weeks (~5–7 hrs) — Nov 16–Dec 11 (spans Thanksgiving)
    - Winter break / catch-up buffer — Dec 14–Jan 8
    - Iterate: ~5–6 weeks (~10–15 hrs), the stage to protect most since it needs real Build→Test→Learn→Revise→Retest elapsed time — Jan 11–Feb 12
    - Impact: ~4–5 weeks (~8–12 hrs), also needs elapsed real-world time for a measurable result, not just form-filling time — Feb 15–Mar 12
    - Influence: ~3 weeks (~6–9 hrs), should include one full pitch rehearsal + the required Mock Reviewer Q&A before submit — Mar 15–Apr 2
    - Revision/resubmission/final-prep buffer — Apr 5–30 (target first-completion ~early/mid April, not April 29, so Feedback→Revise→Resubmit has real room)
  - Student-facing framing should stay simple (just the week estimate + a line like "some stages include waiting time for interviews, testing, or feedback, so you won't be working on the platform every day") rather than surfacing the full hour-by-hour breakdown above.
- **[Sep 22, 2026, sidebar rail: two fixes]** Two owner-reported issues with `AppShell`'s collapsed icon rail (`src/components/app/AppShell.module.css`), both affecting every `/dashboard*` page, not just the Journal:
  - **Dashboard/Journal icons read as "squished."** Measured live in a browser (not just read from CSS): both icons render as correct 44×44 rows with proportionate 19×19 SVGs, no distortion — the actual problem was `.nav`'s `gap: 4px` leaving almost no breathing room between the two top-level nav rows. Bumped to `gap: 8px`. Verified the fix live (measured an 8px gap between the two elements' bounding boxes) before telling the owner it was resolved; a follow-up screenshot they sent still showed the old spacing, which turned out to be a stale/unrefreshed browser tab (confirmed by diffing the two screenshots pixel-for-pixel — the icon region was byte-identical between "before" and "after," meaning the second one never actually reloaded the page).
  - **The rail was expanding to show the active stage's live section-dot list** (the "dots on a thread" navigator from docs/design-system.md Section 22.1) whenever a student was on that stage's own page, since `openStages` defaulted the active stage to open on mount — this made the collapsed rail's height and spacing jump around depending on which page you were on. Owner wants the rail to **always** load at the compact, stage-numbers-only spacing shown in the "before" screenshot. First attempt hid `.subNavSlot` in the rail entirely via CSS (`display: none`, only shown under `.shellOpen`) — too blunt: it also silenced the row's own click-to-expand interaction, which the owner still wanted ("now the stages are not clickable showing the sections in a collapsed panel"). **Correct fix**: `.subNavSlot`'s `display: flex` is back (rail can still show expanded content), and instead `AppShell.tsx`'s `openStages` initial state was changed from defaulting the active stage to open, to always starting `{}` (every stage collapsed on load, active or not). Clicking any unlocked row still toggles it open/closed in place, in the rail itself — only the automatic pre-opening on page load is gone.
  - Verified live against the running dev server on `/dashboard/insight` (01 both current and active): on load, rail shows Dashboard, Journal, 01–06 at the compact spacing with zero `.subNavSlot` elements in the DOM; clicking the 01 row renders the live section-dot navigator inline (5 dots, confirmed via a DOM query) with the rail's height growing only then; clicking again collapses it back to zero. `tsc --noEmit`, `eslint src/`, and `next build` all clean.
  - **Follow-up same day: the Dashboard/Journal gap (8px, from `.nav`) didn't match the gap between stage rows (`.stageList` still had its old `gap: 4px`)**, so the rail read as two different rhythms stacked on top of each other even though each individually looked fine. Set `.stageList`'s rail gap to 8px too, matching `.nav` exactly (`.shellOpen .stageList`'s own `gap: 2px` for the open flyout was untouched — it already overrides independently). Verified with a fresh screenshot: Dashboard→Journal and every 0N→0N+1 stage gap now read as the same spacing top to bottom.
  - **Real follow-up bug, same day: the icons visibly compressed specifically when a stage was clicked open on a narrow/short viewport** (reported at 382px wide — a phone). Took several rounds to pin down because it never reproduced in this session's own desktop-sized (1280×900) Playwright tests, and two of the owner's screenshots in between turned out to be genuinely stale browser tabs (confirmed by diffing them pixel-for-pixel against the previous ones — byte-identical). The actual bug only showed up once the owner ran `getComputedStyle` directly in their own browser's console (the eventual right diagnostic, after screenshots alone couldn't settle it): `.navItem`'s computed `height` was `19px`, not the `44px` the CSS declares.
    - **Root cause**: `.navItem`/`.stageRow` set an explicit `height: 44px` but no `flex-shrink: 0`, inside `.nav`, a scrollable (`overflow-y: auto`) flex **column**. A flex item's default `flex-shrink` is `1`. Once a stage's expanded section-dot list pushed the rail's total content taller than the actual viewport — which only happens with limited vertical space (a phone) **and** a stage expanded, never on a tall desktop test window — the flex algorithm shrank every shrinkable row instead of the column actually scrolling, compressing the 44px icon boxes down to 19px: the bare SVG icon's own intrinsic min-content size, with nothing left to shrink further.
    - **Fix**: `flex-shrink: 0` added to `.navItem`/`.stageRow` and to `.stageList` itself (so the whole list can't be shrunk as a unit either). `.nav`'s own `overflow-y: auto` is what should absorb any overflow now, not row compression.
    - **Verified precisely, not just visually**: reproduced the owner's exact viewport (382px wide, deviceScaleFactor 2, a short 700px-tall window) with `/dashboard/imagine` and Imagine's stage 3 expanded — before the fix this setup would have shown the same 19px collapse; after, `getBoundingClientRect().height` for both Dashboard and Journal reads a solid `44` regardless of how much dot-list content is expanded above or below them. `tsc --noEmit`, `eslint src/`, and `next build` all clean.
    - **Process note for future sessions**: when a visual bug won't reproduce in an automated desktop-sized test but a real screenshot keeps showing it, don't keep guessing at CSS from screenshots — ask for `getComputedStyle`/`getBoundingClientRect` values pasted from the person's own browser console (`copy(JSON.stringify(...))` + paste is fast and unambiguous) before spending more rounds on pixel analysis, especially once viewport size or device pixel ratio might be the actual differentiator, since screenshots taken at different zoom/DPR aren't reliably comparable pixel-for-pixel.
- **[Sep 22, 2026, Journal "add entry" page restyled + optional entry title]** Owner asked for `/dashboard/journal/new` to match `docs/design-system.md`, for its "Add your first entry" button to read exactly like the homepage's "See How It Works," for the form to actually use the page width, and for a field reorder plus a new optional title field.
  - **Full width fix**: the page was a `Panel` capped at `max-width: 680px` inside a plain band, which is what left the empty right-hand column. Rewritten on the shared `StageBand`/`RailHeading` recipe from `src/components/journey/StageBand.tsx` (already documented in design.md Section 22.1 as "heading stacked above content, both at the full 1160px container" — the same pattern every Learn/Do/Show band on the stage pages uses), so the rail carries the Back link, kicker ("Journal"), title ("Log a new entry"), and lede, and the composer form sits in `.main` at the full container width. `.composerPanel`'s 680px cap and the now-unused `.composerRow` grid were removed from `Journal.module.css`.
  - **Button match**: `buttonStyles.ghostStrong` (`Button.module.css`, already documented as "the same visual weight/press motion as .primary... the way 'See How It Works' sits next to 'Register Today'") is now applied to the empty-state "Add your first entry" link in `JournalList.tsx`, alongside `variant="ghost"`. Verified live: both buttons' computed `box-shadow` read identically (`rgb(141, 152, 167) 0px 5px 0px 0px`, i.e. `var(--dim)`).
  - **Field reorder + optional title**: `JournalComposer` (create) and `ReviseForm` (revise, inside `JournalEntryCard.tsx`) now order fields Date → Title (optional) → entry type → text → Photo, each on its own full-width line (the old `entryDate`/photo two-column `.composerRow` grid is gone). `JournalEntry.title String?` added to the schema (migration `20260922120000_add_journal_entry_title`, a plain nullable `ALTER TABLE ... ADD COLUMN`, applied live via `prisma migrate deploy` — no shadow-DB issue this time since it doesn't touch `auth.*`) and threaded through `src/lib/journal.ts` (`JOURNAL_TITLE_LIMIT = 120`, enforced server-side same as `JOURNAL_TEXT_LIMIT`), `actions.ts` (both `createJournalEntryAction`/`reviseJournalEntryAction`), and the timeline UI (`JournalEntryCard`'s `EntrySnapshot` type, shown as a bold headline above the entry text, and on each older version under "Show earlier versions" too, since the Journal's append-only history should stay legible with a title as much as the current version). One em dash in the "What happened?" hint (user-visible text) was also rewritten to a colon per the site-wide no-em-dash rule, caught in the same pass.
  - **Restarted the already-running local dev server mid-session**: found the user's own `next dev` (PID 90830) already listening on port 3000 from before this session started. Per this file's own established gotcha ("Next's dev server doesn't pick up a `prisma generate` that happened after it started"), the new `title` column 500'd (`PrismaClientValidationError: Unknown argument title`) against that stale process's cached Prisma Client until it was killed and restarted — confirmed live end to end afterward.
  - **Verified end-to-end against the live Supabase project** with a throwaway individual-student fixture (real `auth.users` row via the Admin API, real `Student`/`Enrollment`/`Payment(VERIFIED)`, magic-link session via `@supabase/ssr`'s `createBrowserClient` from esm.sh — the same pattern every prior sprint's live verification in this log has used): confirmed the empty-state and "See How It Works" box-shadows match exactly, confirmed the new-entry form renders at the full 1160px container width (not 680px) at 1440px, confirmed field order (Date, Title, entry type, text, Photo) at both 1440 and 390px with no overflow, and filled and submitted a titled entry, confirming the title appears as a headline above the entry text in the timeline. Fixture (`Student`/`Enrollment`/`Payment`/`StageProgress`/`User`/auth account) fully deleted afterward, confirmed via a direct count back to the pre-test baseline (11 students, 24 users). `tsc --noEmit`, `eslint src/`, and `next build` all clean.
  - **[Same day, later] Three follow-up owner requests on the new-entry page**: (1) Date and Title moved onto one row (Title wide, Date a fixed 150px column via a new `.dateTitleRow` grid in `Journal.module.css`, wrapping to one column under 560px) — then, on request, flipped so Date sits on the **right** and Title on the left. (2) The Add entry button moved to the right edge of the form (`.submitRight { align-self: flex-end }`, layered on top of the existing `.submitBtn`, so the shared class still governs the revise form's left-aligned "Save as new version" button unaffected). (3) The Title field's hint line ("A short label for this entry...") was removed at the owner's request. Also fixed the rail lede ("A dated note in your own words...") wrapping narrow even though the rail is now the full 1160px container — it was still using the shared `.lead` class's `max-width: 640px` (needed elsewhere, for the narrow centered empty-state text on the list page); added a `.leadWide { max-width: none }` modifier for this one use instead of loosening the shared class. All verified live at 1440/390px with a throwaway fixture.
  - **[Same day, later still] Added a "which stage is this about" tag to journal entries**, at the owner's request. `JournalEntry.stage StageName?` added to the schema (migration `20260922130000_add_journal_entry_stage`, nullable `ALTER TABLE ... ADD COLUMN` referencing the existing `StageName` enum, applied via `prisma migrate deploy`). `src/lib/journal.ts` gained `JOURNAL_STAGE_OPTIONS` (built from `STAGE_ORDER` + `getStageCopy(stage).name`, so it can't drift from the one real stage-name source) and `isJournalStage`/`journalStageLabel` helpers, mirroring the existing entry-type pattern.
    - Both `JournalComposer` (create) and `ReviseForm` (revise) gained a `RadioGroup` — "Which stage is this about?" — with the six stages plus an explicit **"General (not tied to a stage)"** option (empty-string value, reads back as `null` server-side) rather than forcing every entry onto one of the six. `JournalComposer` takes a new `defaultStage` prop, set by `new/page.tsx` from `journey.find(item => item.status === "CURRENT")?.stage` — pre-selects the student's current stage (the common case) while staying fully changeable. `ReviseForm` pre-selects whatever the entry was already tagged.
    - `actions.ts`'s shared `readEntryFields` validates `stage` against `isJournalStage` (or accepts empty → `null`) and both `createJournalEntryAction`/`reviseJournalEntryAction` write it. Timeline UI: `JournalEntryCard` shows a new `.stageTag` pill (cobalt outline, distinct from the existing mono `.typeTag`) next to the entry-type tag whenever a stage is set, and each older version under "Show earlier versions" includes its own stage label in its meta line — same "history stays as legible as the current version" reasoning as the title field.
    - **Verified end-to-end against the live Supabase project** with a throwaway fixture: confirmed Insight (the fixture's real current stage) is pre-selected on `/dashboard/journal/new`, submitted an entry tagged Investigate instead and confirmed the "INVESTIGATE" pill renders correctly in the timeline, and confirmed the revise form pre-selects the entry's saved stage ("Investigate") rather than defaulting back to the student's current stage. Restarted the already-running local dev server again for the same reason as the title-field migration (stale Prisma Client). Fixture fully deleted afterward. `tsc --noEmit`, `eslint src/`, and `next build` all clean.
  - **[Same day, later] Title made required; timeline rows collapse to just the title, with an arrow to expand.** Two owner requests: (1) the title field (added earlier this session as optional) is now required on both the create and revise forms — label dropped "(optional)", `Input`s gained `required`, and `actions.ts`'s shared `readEntryFields` now returns "Give this entry a title." when it's blank rather than silently writing `null`. The `JournalEntry.stage` column stays nullable in the schema (a title-less legacy row is still possible if one existed before this change), so `JournalEntryCard` falls back to "Untitled entry" for that edge case rather than rendering blank.
  - (2) `JournalEntryCard`'s row is now a collapse/expand accordion instead of always showing everything: collapsed, a row shows only its tags, date, title, and a chevron; the entry's text, photo, "Edited"/history controls, and the inline revise form only render once expanded. The whole summary line is a real `<button aria-expanded>` (not just an icon) so clicking anywhere on the row toggles it, keyboard-operable by default. New CSS: `.rowSummary` (button reset, full-width flex row), `.rowSummaryMain` (tags + title stacked), `.chevron`/`.chevronOpen` (rotates 180° open), `.rowBody` (the expanded content, `margin-top` reset on its own first child to avoid doubling up with `.rowText`'s existing margin). Verified live: two entries created (one via native-required-field validation confirming an empty title blocks submit), both timeline rows render collapsed with just tag/date/title, clicking a row reveals its body text and "Add an update" link, and clicking again collapses it back — confirmed by checking the body text's visibility before/after/after-again, not just a screenshot. `tsc --noEmit`, `eslint src/`, and `next build` all clean. No schema migration this pass.
  - **[Same day, later] Journal search fixed — it was never searching the one thing every row now shows.** Owner reported search "doesn't work." Root cause: `JournalList`'s search haystack was still `text + entryType label` only, from before the title field existed — the row's own title (the thing collapsed rows show, per the change directly above) was never included, so searching for a title typically found nothing. Also fixed as part of the same pass, since the owner asked for "related content, not just the exact same word typed": the old check was a single `haystack.includes(wholePhrase)`, so a two-word query only matched if those words appeared adjacent and in that exact order in the text.
    - `matchesQuery()` (new, in `JournalList.tsx`) splits the query into words and requires each one to appear **somewhere** in the haystack (still a substring match per word, so "test" still matches "testing"), independent of order — "cause root" now matches an entry whose text says "root cause," the same as "root cause" does. It also applies a light stem (strip a trailing "s"/"es") per word, so "observations" and "observation" match each other without needing an exact plural/singular match.
    - The haystack itself now joins title, text, the entry-type label, the stage label (via the existing `journalStageLabel`), and the entry's date spelled out (`formatSearchDate`, "September 22, 2026") — so a student can find an entry by typing its title, a word from its type ("fail" finds "Something that didn't work"), its tagged stage ("insight"), or its date, not only a phrase copied verbatim from the body text. Deliberately still scoped to the current version only, not older revisions (unchanged reasoning from the original comment: "did I write about X" means "what does it say now").
    - **Verified live** with three entries covering these cases: word-order-independent multi-word search (query "Interview Notes User" still finds an entry titled "User Interview Notes"), plural/singular stemming both directions, a partial-word match against a type label ("fail" → "Prototype Failure"), a reversed two-word body-text query ("cause root" → "Root Cause Research"), a stage-label search ("insight"), and a nonsense query correctly returning zero rows. Fixture deleted afterward. `tsc --noEmit`, `eslint src/`, and `next build` all clean. No schema change; client-side search logic only.
  - **[Same day, later] Owner reported search "still not working" after the fix above.** Re-tested against the owner's own real journal entries (`rainaartist@gmail.com`, real data, not a fixture) with actual simulated keystrokes (`pressSequentially`, not `.fill()`) rather than reasoning about the code — every query tried (word-order-independent multi-word, plural/singular) matched correctly, and every "surprising" multi-entry result turned out to be a real match on inspection (e.g. "Changed The Problem One" matched two entries because both genuinely contain all four words — "changed," "problem," and "one" are common enough to show up honestly, not a bug). Two real gaps were found and fixed anyway, since a repeated "not working" report after a verified-correct fix usually means either a stale bundle or a real gap the first fix didn't cover:
    - **Typo tolerance added.** The word-matching from the previous fix still required an exact (or singular/plural-stemmed) substring, so a misspelled word — a real thing a middle/high schooler searching their own writing will do — found nothing, which reads exactly like "doesn't work." New `levenshtein()` (classic edit-distance) and `typoBudget()` (allowed edits scaled to word length: 1 for ≤4 letters, 2 for ≤8, 3 beyond) in `JournalList.tsx`; `matchesQuery()` now falls back to comparing a non-matching query word against every real word in the entry (split out separately as `haystackWords`) by edit distance, only when the fast exact/stemmed substring check fails. Verified live: "cafateria" (misspelled) and "lunhc" (misspelled) both correctly found the real entries mentioning "cafeteria"/"lunch"; "intervew" found the entry mentioning "interview"; a genuine nonsense query still correctly returned zero results (the per-word edit-distance budget doesn't degrade into "everything matches").
    - **Restarted the local dev server** on the chance the "still not working" report was against a stale Fast Refresh bundle from the earlier same-session edits, rather than the code itself — cheap to rule out, and this file already documents Fast Refresh occasionally needing a hard restart after several rapid edits. Re-verified afterward against a completely fresh page load.
    - `tsc --noEmit`, `eslint src/`, and `next build` all clean. No schema change.
- **[Sep 22, 2026, Team Contribution Tracking built]** The last unbuilt schema-ready Sprint 5 item that had real content to build from — `TeamContribution` ("Only meaningful when Project.teamId is set. Tracks what each individual teammate contributed, independent of the shared project content."). New route `/dashboard/team`, reusing `AppShell` chrome. Before building, checked `docs/curriculum/` for Speaking Challenge / Character Challenge / Ethics Challenge content (the other three remaining Sprint 5 items) the way every other feature this sprint was grounded in the docs — found nothing at all beyond the bare Prisma models (`mediaUrl`, `response`), unlike everything else built this sprint. Flagged this to the owner rather than inventing prompts/rubric text; they chose to skip those three for now and build this one instead, which does have real schema support and no missing-content problem.
  - **New `src/lib/team-contribution.ts`**: `CONTRIBUTION_TEXT_LIMIT = 2000` (same "server is the real limit" convention as `JOURNAL_TEXT_LIMIT`/`TEXT_LIMITS`), `getTeamContext(studentId)` (the student's team + shared `Project` + full teammate roster with role labels, or `null` for an individual student or a team whose project doesn't exist yet — see `getOrCreateStudentProject`), and `getTeamContributions(projectId)` (every contribution across the whole team, newest first, with each row's student name resolved).
  - **Visibility is whole-team, not per-student** — unlike the Journal (scoped to one student), the point of this feature is a shared, honest record of who did what, so every teammate sees every contribution, not just their own. **No edit/revision path**: `TeamContribution` has no version field in the schema (unlike `JournalEntry`'s `entryGroupId`+`version`), so `addTeamContributionAction` (`src/app/dashboard/team/actions.ts`) only ever inserts — a teammate who wants to add detail logs a new entry rather than editing an old one, the same append-only spirit as the Journal, just without a formal version chain since the schema doesn't have one.
  - **Gating**: `requireTeamStudent()` re-checks this student's own `VERIFIED` payment for the active season (per student, never per team, CLAUDE.md "Team billing/access independence") AND that they're actually on a team with a shared Project, independently of the page — a direct POST from an individual student, or a team student whose payment later moves to REJECTED, is refused server-side. Rate-limited the same way as `journal/actions.ts`'s `JOURNAL_WRITE_LIMIT` (20/min), reusing the existing `src/lib/rate-limit.ts`.
  - **UI**: `TeamHero` (reuses `JournalHero.module.css`'s exact `SplitHero` + stat-box recipe rather than duplicating it — a second file with byte-identical CSS would just be drift waiting to happen), a plain roster list (name, role label, a "You" tag on the signed-in student's own row), an always-visible single-field composer (`Textarea` only — `TeamContribution` has just one real field beyond who/when, so unlike the Journal's fuller form this didn't earn its own `/new` page), and the contribution log as one connected Pattern-C plane (same recipe as Journal's timeline / the hub's `StageProgressPanel`) with teammate filter pills (the one deliberate pill use, per design-system Section 21.6's toggle-filter carve-out). Per the `no-left-bar-boxes` memory, the composer sits in a plain `Panel` with `prominent` (the hard offset-shadow treatment) rather than `variant="selected"`'s cobalt left rule.
  - **Nav**: `AppShell` gained a third top-level link ("Team"), shown only when `isTeamProject` is true — an individual student never sees it, matching the feature's whole "only meaningful for a team project" premise.
  - **Verified end-to-end against the live Supabase project** with two real throwaway teammate fixtures sharing one Team/Project (real `auth.users` rows via the Admin API, magic-link session via `@supabase/ssr`'s `createBrowserClient` from esm.sh — same pattern as every other sprint's live verification in this log; used `playwright-core` + system Chrome via `channel: "chrome"` instead of downloading Playwright's own browser binaries, to keep the one-off verification lightweight): both teammates logged a contribution, each immediately saw the other's on reload (shared visibility confirmed), the teammate filter pills correctly isolated one person's rows and hid the other's, the roster showed the role label + "You" tag correctly, no console errors, and no horizontal overflow at 390px. Both fixtures (Student/Enrollment/Payment/StageProgress/Team/Project/TeamContribution/User rows, plus their `auth.users` accounts) fully deleted afterward. `tsc --noEmit`, `eslint src/`, and `next build` all clean.
  - **Sprint 5 status**: Project Submission Portal (folded into Insight's lazy bootstrap, Sprint 4), Innovation Journal, AI-Use Disclosure, Safety Screening, Team Contribution Tracking, and IMAGINE/ITERATE/IMPACT/INFLUENCE are all built. Speaking Challenge, Character Challenge, and Ethics Challenge remain unbuilt — schema-only — pending real content from the company (no prompts, format, or rubric exist anywhere in `docs/curriculum/` for these three).
- **[Sep 23, 2026, Sprint 6 started — Admin foundation]** Built the first slice of Sprint 6: real-time Admin Dashboard core, replacing the "direct DB write" pattern this log has used for every payment/safety/parent-link approval since Aug 15. Confirmed with the owner up front: CLAUDE.md's Sprint 6 line still names "IP Review Workflow" as a deliverable, but that's stale — the IP Checkpoint was removed entirely on Aug 25, so there's nothing to review; not built, per that correction.
  - **`src/lib/admin.ts`**: `requireAdmin()` (redirects to `/login`/`/dashboard` unless the session's `public.User` row is `role: "ADMIN"` with a real `Admin` profile — a live Prisma read, same defense-in-depth convention as `requireCurrentStudent`) and `getAdminOverviewCounts()`. **`src/proxy.ts`** gained an edge-level `/admin` gate reading `user_metadata.role`, same shortcut-plus-real-check pattern as the existing `/dashboard/<stage>` gate.
  - **`src/lib/audit.ts`**: `AuditLog` has existed in the schema since Aug 13 with zero write path — `logAdminAction()` is the first one, called after every admin write in the three queues below (`PAYMENT_VERIFIED`/`PAYMENT_REJECTED`, `SAFETY_REVIEW_CLEARED`/`SAFETY_REVIEW_REJECTED`, `PARENT_LINK_VERIFIED`/`PARENT_LINK_REJECTED`).
  - **`src/components/admin/AdminShell.tsx`**: deliberately simpler than the Student `AppShell` — five flat nav items (Overview/Payments/Safety reviews/Parent links/Users) don't need a collapsing icon rail, so this is a plain always-on paper sidebar reflowing to a horizontal scrollable bar under 860px.
  - **`/admin`** (Overview): KPI strip (pending payments/safety reviews/parent links + student/parent/coach totals) linking into each queue.
  - **`/admin/payments`, `/admin/safety`, `/admin/parent-links`**: real approve/reject queues, each a connected Pattern-C plane (`src/app/admin/AdminHub.module.css`, mirroring `DashboardHub.module.css`'s recipe). These close the exact gaps the log has repeatedly worked around by hand: `Payment.status` SUBMITTED→VERIFIED/REJECTED, `SafetyReview.status` PENDING_REVIEW→CLEARED/REJECTED, `StudentParent.verifiedAt` (the Aug 19 self-consent-bypass fix's missing other half). Safety queue shows the flagged categories via a new **`src/lib/safety-screening.ts`** (`SAFETY_ITEMS`, moved out of `SafetyScreeningForm.tsx`).
  - **Real bug caught by live testing**: importing `SAFETY_ITEMS` straight from the `"use client"` `SafetyScreeningForm.tsx` into the server-component admin page 500'd at runtime (`SAFETY_ITEMS.filter is not a function`) — a server component importing a non-component named export from a client module gets a broken client-reference proxy for it. Fixed by moving the plain array into its own non-client lib module; worth remembering for any future case of a server component needing a constant that currently lives in a `"use client"` file.
  - **`/admin/users`**: read-only directory (`UsersDirectory.tsx`, client-side search + role-filter pills, same pattern as the Journal's own list) — no create/change-role action, since Admin/Judge provisioning is still explicitly Phase 2 (Aug 15 entry).
  - **Real mobile bug caught and fixed**: `AdminShell`'s mobile nav row (≤860px) squished every nav item toward zero width instead of scrolling, because `.navItem` lacked `flex-shrink: 0` — the exact same bug class `AppShell.module.css` already documents fixing once. Even after that fix, the page still overflowed at 390px by 23px: `.brandRow` (159px) + `.sidebarBottom` (230px, both `flex: 0 0 auto`) left `.nav` almost no room. Fixed with a `≤480px` rule hiding the admin's name in the profile chip (keeping just the avatar) and tightening the logout button — found by diffing `getBoundingClientRect()` on every element against the viewport edge, not by guessing from a screenshot alone (a lesson this log already recorded once, Sep 22 sidebar-icon entry — repeated here because reasoning from a screenshot alone stalled again before the precise measurement found it).
  - **Real, more serious bug caught during live verification: the first version of the verification script used `.first()` to click a queue's Verify/Clear button, assuming its own freshly-seeded fixture would be the only pending row.** It wasn't — payments/safety/parent-link queues sort by creation time, and other pending rows (a seed demo parent link, and, from concurrent real usage during testing, a genuine team payment with reference "admin-bypass" and a real student's genuinely-flagged safety review) sorted earlier and got auto-verified/cleared as a side effect. **All three were caught and reverted** (found by querying for `verifiedByAdminId`/`reviewedByAdminId` values pointing at a test admin id that no longer exists, since every fixture's `Admin` row is deleted at cleanup) before this entry was written, restoring each to its exact prior state (`SUBMITTED`/`PENDING_REVIEW`/`verifiedAt: null`). The verification script was then rewritten to target the exact `<form>` holding its own fixture's record id (`form:has(input[name="paymentId"][value="..."])`) instead of `.first()`. **This is the load-bearing lesson from this session**: any future live-verification script that clicks an admin approve/reject action on a shared queue must target its own fixture's row by exact id, never by position — a live dev database can have real concurrent pending rows at any time, and a wrong click there is a real, consequential action, not a harmless test artifact.
  - **Also cleaned up in the same pass**: 11 orphaned test `Admin`+`User` rows and a stray `Student`+`StageProgress` set left behind by earlier debugging runs whose cleanup scripts didn't fully account for `StageProgress`'s FK (a student's own trip to `/dashboard` during the "non-admin blocked" check lazily creates all 6 `StageProgress` rows — real app behavior, not a bug, but cleanup has to delete them before the `Student` row). Confirmed clean afterward: exactly 1 `Admin` row (the real seed admin) and 0 stray `tabtest-`/`admintest-`/`diagadmin-` rows or auth accounts remain. **Not touched**: a set of pre-existing stray test auth accounts (`jtest-*`, `insighttest-*`, `debug-insight-1@example.com`) that predate this session — flagged to the owner rather than deleted, since their origin and safety to remove weren't established this session.
  - Verified via `tsc --noEmit`, `eslint src/`, and `next build`, all clean; end-to-end against the live Supabase project with a real throwaway admin fixture (promoted from STUDENT to ADMIN via direct Prisma write, since the Aug 16 trigger hardening correctly rejects `role: "ADMIN"` via self-serve `user_metadata` — Admin/Judge provisioning is still unbuilt, Phase 2, so this is the only way to get an admin session for testing) walking Overview → Payments (verify) → Safety (reject) → Parent links (verify) → Users (search) → non-admin-blocked check → 390px mobile check, then a direct Prisma read confirming exactly the fixture's own rows changed and the `AuditLog` recorded exactly those 3 actions.
  - **Sprint 6 next up**: School Management, Season Management, then Online Assessments/Retakes and Deadline/Qualification Eligibility Tracking, per the Aug 16-agreed sequencing (admin foundation → pending-review queues → School/Season management → Assessments/deadlines last).
- **[Sep 23, 2026, later] Users directory made approved-only; Season page can now create and switch seasons.** Two owner requests. On starting this session, `/admin/schools`, `/admin/season` (edit-in-place), `/admin/users` (with a Participants/Schools tab split), and a `StudentParent.rejectedAt`/`rejectedByAdminId`/`rejectionReason` migration were already built and sitting uncommitted in the working tree from a prior session that hadn't logged them yet — noting that here so a future session isn't confused about when School Management showed up.
  - **Users directory now only lists approved people**, the one real approval this codebase tracks per role: a student with a `VERIFIED` payment for the active season (never `PENDING`/`SUBMITTED`/`REJECTED`/`REFUNDED`, never no enrollment), or a parent with at least one admin-verified `StudentParent` link. A user with no profile row at all ("Registration incomplete") is filtered out the same way a rejected one is. Coach/Judge/Admin are unaffected — CLAUDE.md: Judge/Admin are never self-serve so a profile row existing already means approved, and Coach verification is still unbuilt (Sprint 7) — so having a `Coach` row is the only bar there is today. Applied in `src/app/admin/users/page.tsx` to all three views: the People tab (`directory`), the Participants tab's individuals list, and each team's member roster (`directoryTeams`) — a team with zero approved members is left out entirely rather than shown with an empty roster. A rejected/pending student or parent still has their own queue (Payments, Parent links) to be found in; they just don't belong in a directory of approved people.
  - **Season page can create a new season and switch which one is active**, replacing the old "isn't built yet, would need a new Season row" note. `createSeasonAction` (`src/app/admin/season/actions.ts`) always creates the new row `isActive: false` — never live on submit — and, on by default, clones the current active season's `CurriculumModule`/`Lesson`/`Assignment` rows into it inside the same transaction, because every stage page looks up its module by the *active* season's id, so an activated season with nothing cloned into it would 404 every stage instantly. `activateSeasonAction` flips exactly one season to active (a transaction: deactivate all, activate one) and deliberately does not migrate any student's `Enrollment`/`Payment` across seasons — the page's own confirm dialog states that a student already enrolled in the outgoing season will need a fresh enrollment/verified payment in the new one before their dashboard reads as paid for it, so an admin can't activate a new season expecting continuing students to be unaffected. Both actions log to `AuditLog` (`SEASON_CREATED`/`SEASON_ACTIVATED`), shown in a new site-wide "Season log" panel (separate from the per-season "Edit history" panel, since a creation/activation event isn't scoped to one season's own id the way a field edit is).
  - **Verified end-to-end against the live Supabase project**, not just reasoned about: a throwaway admin fixture (STUDENT→ADMIN promotion, matching the Sep 23 admin-foundation entry's approach — Admin/Judge self-serve is still correctly blocked by the Aug 16 trigger hardening) confirmed via real Playwright clicks (system Chrome, magic-link session) that Priya Patel (REJECTED payment) and a SUBMITTED-payment student were absent from `/admin/users`, Sam Okafor (VERIFIED) was present, and a parent with only an unverified link was absent. Separately walked the full season flow through the real UI: created a throwaway `2099-2100-TEST` season, confirmed all 6 `CurriculumModule`s cloned with matching lesson/assignment counts (5/5, 7/12, 12/13, 12/14, 17/20, 18/22) via a direct Prisma read, activated it (confirmed the "Active" tag moved and the season log recorded it), then activated `2026-2027` back and confirmed via Prisma that it's the one active season again before deleting the test season, its cloned curriculum, and the admin fixture. One real bug caught along the way: the proxy's `/admin` edge gate reads `user_metadata.role` off the Supabase session, independent of the `public.User.role` DB promotion — a fixture promoted only in the DB bounces at the edge before `requireAdmin()` ever runs, so the admin-fixture setup script now also calls `admin.auth.admin.updateUserById(..., { user_metadata: { role: "ADMIN" } })`.
  - `tsc --noEmit`, `eslint src/app/admin`, and `next build` all clean.
- **[Sep 23, 2026, later still] Users directory gained an Unapproved tab and a Past-seasons archive.** Owner correction on the entry above: creating/activating a season is right, but the point of asking was that the Users page needed to stay usable across a rollover — keep all the historical per-season data, not just the live one — and needed an explicit place to see who *isn't* approved, not just who is.
  - **Unapproved tab** (`src/app/admin/users/page.tsx`'s `unapprovedStatus()`, new `DirectoryUnapproved` type in `UsersDirectory.tsx`): everyone the People tab's approved filter leaves out, with a plain status line instead of being hidden — "Payment rejected" (+ the rejection reason), "Awaiting payment verification", "Payment refunded", "Not enrolled", "Awaiting parent-link review", "Parent link rejected" (+ reason), "No student linked yet", or "Registration incomplete". Every branch mirrors the exact condition that excluded that user from `directory`, so the two can't drift into disagreeing about why someone isn't approved. Same search/role-filter/sort controls as the People tab; explicitly read-only — the real approve/reject actions still live on the Payments and Parent links queues, this is a reference list only, as asked.
  - **Past seasons tab**: once a season is archived (a new one activated in its place via `/admin/season`), its full roster doesn't just disappear from Users — every student's `Enrollment`/`Payment` row is permanent regardless of which season is active (`activateSeasonAction` never touches them), so this tab reads that history straight. A season picker (pills, one per inactive `Season` row) selects which archived season's roster to view: name, email, grade, school, team (or Individual), and whatever payment status they ended with — deliberately unfiltered/not approved-only, since this is a historical record of who was in that season, not a live approval gate. Each row links to `/admin/users/[id]`, which already reads a student's full unscoped `enrollments` history (no code change needed there). Empty state explains what will show up here once a real rollover happens. Parent/Coach/Judge/Admin aren't season-scoped rows in this schema, so a past season's roster is students/teams only — noted as a real (not accidental) scope limit, not silently missing.
  - **Verified end-to-end against the live Supabase project**: a fresh throwaway admin fixture confirmed via real Playwright the Unapproved tab correctly lists Priya Patel ("Payment rejected"), the SUBMITTED-payment student ("Awaiting payment verification"), and Kim Ellis ("Awaiting parent-link review") — the same three real accounts the approved-only filter excludes, this time showing why instead of just being absent — while a real approved student stayed out of the list (confirmed against the actual rendered rows in a screenshot, not `body.textContent`, which turned out to include the People tab's still-hidden RSC hydration payload and produced one false-positive during testing — a test-harness artifact, not an app bug). Separately created a real throwaway archived season with a real historical `Enrollment`/rejected `Payment` on an existing student (Jordan Ellis) and confirmed the Past seasons tab's pill, roster row, and payment status all render correctly. Both fixtures fully deleted afterward (`prisma.season.count()` back to 1, active season still 2026-2027).
  - `tsc --noEmit`, `eslint src/app/admin/users`, and `next build` all clean.
- **[Sep 23, 2026, later still] Users split into three pages — current season, each past season, and All seasons — replacing the single-page tabs from the entry above.** Owner correction: the "Past seasons" tab and its own season picker inside one page wasn't quite it; a season rollover should give the outgoing season its **own page**, with the same People/Participants/Schools/Unapproved split every other page has, not a tab buried inside the current one.
  - **New `src/app/admin/users/data.ts`**: the one query layer behind all three pages (`loadDirectory(scope)`, `scope` = `{kind:"current"}` / `{kind:"past"}` / `{kind:"all"}`), so "approved" can't drift between pages the way it would if each route re-implemented its own version. Current and past both scope every query to one `Season` row; past additionally drops Parent/Coach/Judge/Admin from both the People and Unapproved tabs (none of those roles are season-scoped rows in this schema — a parent's own verification was never tied to a season — so "who's on this closed season's roster" is students/teams only, not a live approval queue) and restricts Unapproved to students who had a real enrollment attempt that season, not the current page's "not-yet-enrolled at all" case. The All page rolls every season into one row per person: a student is approved if they were ever `VERIFIED` in *any* season, using a "representative" enrollment (their most recent `VERIFIED` one, or their most recent of any status if never verified) to show a season label and, when they've done more than one, a "(N seasons total)" note; Schools on the All page stays the same all-time counts it already was (School rows were never season-scoped to begin with); a season-scoped page's Schools tab now counts only students verified *that* season, so a school with zero participants that season doesn't clutter it.
  - **New `src/components/admin/SeasonSwitcher.tsx`**: real `<Link>` navigation (not client tab state) between the three pages, reusing the admin queues' existing `.filterPill` toggle-group look. Shown at the top of all three pages.
  - **Routes**: `/admin/users` (current season, unchanged URL), `/admin/users/season/[seasonId]` (a past season; redirects to `/admin/users` if the id turns out to be the still-active one, so there's never a second URL for the same page), `/admin/users/all`. `UsersDirectory.tsx` lost the "Past seasons" tab, `pastSeasons`/`seasonRosters` props, and `SeasonRosterEntry`/`SeasonSummary` types from the entry above — it's back to exactly the four tabs, plus a new optional `note` banner prop a scoped page uses to explain what it leaves out.
  - **Verified end-to-end against the live Supabase project**: a throwaway admin fixture plus a throwaway archived season with two real enrollments — Jordan Ellis `VERIFIED` (to prove a past season's People tab and an All-seasons "2 seasons total" note both work) and Maya Chen `REJECTED` that season only, while she's independently `VERIFIED` in 2026-2027 (to prove the All page's "approved if verified *anywhere*" rule: she correctly shows approved on All despite one rejected season, and correctly shows `REJECTED`-that-season on the past season's own Unapproved tab). All three pages, the switcher, the season-scoped Schools count, and the historical rejection reason all confirmed via real Playwright clicks; one test-script false failure (checking for "Maya Chen" in a `body.innerText.split("Participants")[0]` slice, which cut before the People tab's own rows since "Participants" is also a tab-button label appearing earlier in the DOM) was traced to the test, not the app — confirmed by a direct screenshot of the People tab showing her, and by `unapproved`/`directory` being exact complements by construction (every user lands in exactly one, so her absence from Unapproved already proved she's in People). Fixture and archived season fully deleted afterward (`prisma.season.count()` back to 1, active still 2026-2027).
  - `tsc --noEmit`, `eslint src/app/admin src/components/admin`, and `next build` all clean.
- **[Sep 23, 2026, later] Admins can delete an account.** Real deletion, not a status flag — the closest existing "delete" pattern (Parent-link reject) went from a real delete to `rejectedAt` specifically to keep history, so this is the first genuine delete path in the app. Nothing in `schema.prisma` has `onDelete: Cascade` set (checked — zero occurrences), so a plain `prisma.user.delete()` fails on the first foreign key it hits; this walks the whole dependency graph by hand.
  - **`src/lib/delete-account.ts`** (`deleteUserAccount(userId)`): one Prisma transaction, children before parents, covering every table that references Student/Parent/Coach/Judge/Admin/User — for a Student that's Enrollment+Payment, TeamMembership, StageProgress, Submission (+ its AIDisclosure/SpeakingSubmission/CharacterChallenge/EthicsChallenge), Attempt, JournalEntry, QualificationRecord, Certificate, CompetencyRating, Consent, MediaConsent, TeamContribution, StudentProject, StudentParent, and their individual Project if they have one (a **team** project is left alone — a Coach's Team also survives, just loses its `coachId`, set to null rather than the team being deleted). Notification and their own AuditLog entries (as actor) go last, then the User row. Once the DB transaction commits, the Supabase Auth account is deleted last, never first, via a new `src/lib/supabase/admin.ts` (service-role client — first real app-code use of `SUPABASE_SERVICE_ROLE_KEY`, reserved for this since Sprint 1) — if that step fails, the DB side is still cleanly gone rather than the reverse, since an orphaned `auth.users` row is exactly what broke signups in the Aug 19 2026 incident.
  - **`src/app/admin/users/[id]/actions.ts`** (`deleteUserAction`): three guards before anything is touched — can't delete your own account (checked server-side, not just hidden in the UI), can't delete the last remaining Admin account (`prisma.admin.count() <= 1` refuses), and the submitted `confirmEmail` must match the target's email exactly. Logs `USER_DELETED` to `AuditLog` (role as `targetType`, the User id as `targetId`) *before* deleting, since the actor (the admin doing this) still exists but the target is about to stop existing — same reasoning already used for `targetId` elsewhere in this table (a plain string, never an enforced FK).
  - **New `DeleteAccountDialog`** (`src/components/admin/`): every other destructive admin action here (reject a payment, reject a parent link) only needs a typed reason, because those are recoverable. A deletion isn't, so this asks for one more thing: typing the account's exact email to enable the submit button (the "type it to confirm" pattern, same reasoning as GitHub/Vercel's delete-repo flow) — client-side gate only, `deleteUserAction` re-checks the real match server-side. Wired into `/admin/users/[id]` as a new bottom section; that page hides the trigger entirely (not just disables it) when viewing your own account.
  - **Verified end-to-end against the live Supabase project** with a throwaway admin as the actor (never touching either real admin account) and five throwaway targets — a Student built with a full dependency tree (verified enrollment+payment, an individual Project with a final Submission + AIDisclosure, a JournalEntry, StageProgress, and a verified StudentParent link to a throwaway Parent with Consent/MediaConsent), a Coach assigned to a throwaway Team, a Judge with an assignment and score, and a second throwaway Admin. Real Playwright clicks through the actual dialog (typing each exact email) confirmed: the self-delete guard renders instead of the dialog on the actor's own page; all five deletes redirect to `/admin/users`; a direct Prisma read afterward confirmed 22 of 22 real checks (every dependent row gone, the Coach's Team surviving with `coachId: null`, all 5 `AuditLog` rows written with the correct role/email) — one test-script check that queried `AuditLog` by the wrong id (Student profile id instead of User id) was a test bug, not an app bug, confirmed by re-querying with the right id; and the Supabase Admin API confirmed all 5 `auth.users` rows gone too. The "last admin" refusal was verified by code review rather than a live click (the real admin count is 2-3, so there's no safe way to actually reach the `<= 1` state without touching real accounts). All fixtures — including the actor admin and a throwaway Team the Coach test needed — fully deleted afterward; DB counts back to the exact pre-test baseline (2 admins, 36 users).
  - `tsc --noEmit`, `eslint src/app/admin src/components/admin src/lib/delete-account.ts src/lib/supabase/admin.ts`, and `next build` all clean.
- **[Sep 23, 2026, later] Admin gap pass: season progress, submissions, teams, refunds.** Audited `/admin` against what a season-based, multi-role competition needs. Added: `/admin/progress` (season roster with payment, consent, team and stage state, a per-stage funnel, filters for unpaid / consent missing / no activity in 14 days / all six complete, and an admin-only CSV export at `/admin/progress/export` that is audit-logged and neutralises spreadsheet formulas); `/admin/submissions` (+ `[id]` detail: every stage submission for the active season, filterable by stage and draft/submitted, with the AI-use disclosure; read-only, since no reviewer scoring model exists); `/admin/teams` (rosters, join codes, per-member payment, and coach assignment via `assignTeamCoachAction`, which is what gives a coach their read-only roster); a Refund action for VERIFIED payments (`refundPaymentAction`, sets `REFUNDED`, which the stage actions already treat as locked, per student only); and season-scoped counts on the Overview. `RejectDialog` gained optional `verb`/`lead` props for the refund reuse. Shared query layer: `src/lib/admin-season.ts`. "All six stages complete" is shown as the stage half of qualification, not "Qualified", because assessments are not built; `QualificationRecord` still has no write path. Verified: `tsc`/`eslint` clean, roster query run against the live DB, routes redirect when logged out. **Not verified in a logged-in admin browser session.** Still not built (deferred per this file): audit log viewer, Judge/Admin provisioning, coach verification (Sprint 7), assessment management, notifications.
- **[Sep 23, 2026, later] Pre-launch lockdown, copy accuracy pass, private uploads, stage-form rate cap.**
  - **Lockdown:** `src/lib/launch.ts` (`isPlatformOpen()`, env `PLATFORM_OPEN=true` to open; default closed). While closed, `src/proxy.ts` sends every non-admin signed-in user from `/register`, `/consent` and `/dashboard/*` back to `/dashboard`, which shows a "Coming soon" card (`src/app/dashboard/page.tsx`). Signup and login still work. Nothing was deleted; setting the variable restores every flow. **Before opening, walk the full new-family path end to end** (student register, team, parent link + admin approval, consent, payment + admin verify, Insight submit, next stage unlocks); those flows are not exercised while closed.
  - **Copy:** Home, How It Works, Curriculum, Pricing and FAQs were rewritten from `docs/curriculum` (stage questions, topics, artifacts, review gate). Removed claims with no basis in the docs or the build (Speaking/Character/Ethics challenges, assessments and retakes). Enrollment now opens October 1, 2026 (`Season.openDate`, seed and live row); the homepage hero note is Season-driven instead of hard-coded. Legal pages (`/terms`, `/privacy`, `/competition-policies`) still hold placeholder text pending the real documents; they now have the site header/footer, a white background, and the coral draft box. `/design-system` returns 404 in production.
  - **Uploads are private.** Files are written to `./private-uploads/<subdir>/` (gitignored), not `public/`. The stored URL format `/uploads/<subdir>/<file>` is unchanged; `src/app/uploads/[...path]/route.ts` serves it after checking the session: admin sees all, a student sees their own payment screenshot / journal photos / Insight photo (and a teammate on the same project sees the shared project's Insight photo); everyone else, and every miss, gets the same 404. Strict filename pattern blocks path traversal. Verified live: anonymous 404, owner 200 (`Cache-Control: private, no-store`), another student 404, traversal attempts 404. `saveUploadedFile` is still the single swap point for real R2; R2 itself still needs Cloudflare credentials.
  - **Rate cap:** every stage save (autosave, draft, final, safety screening) is limited to 120 per minute per student (`assertStageWriteAllowed` in `dashboard/[stage]/actions.ts`, same in-memory limiter as the Journal and Team pages). Verified by review only; the lockdown blocks a live test.
